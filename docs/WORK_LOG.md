# Gangnam Dream Work Log

> 최신 작업만 이 파일에 역순으로 기록한다. 2026-05-16부터 2026-07-24 USER-P0K까지의 원문은
> [`history/WORK_LOG_2026-05-16_to_2026-07-24.md`](history/WORK_LOG_2026-05-16_to_2026-07-24.md)에 손실 없이 보존했다.
> 과거 근거는 기본 컨텍스트에 넣지 말고 먼저 `rg -n "<키워드>" docs/history/`로 찾는다.

## 2026-07-26 (Codex — ORDER-49 선택의 고민 장부화 P2)

### 사랑과 유혹은 문장만이 아니라 실제 후속과 결산에 남는다

- 다은 최종 배신 선택의 허위 자산 이득 문구를 제거했다. 결혼 준비의 작은
  예식과 풀패키지는 어느 쪽도 도덕적 정답으로 서술하지 않고, 각각 줄어든
  관계의 상처와 가족 사진으로 남는 실제 기억을 함께 보여 준다.
- `daeun_married + used_daeun_as_means` 상태에서 30억에 먼저 도달해도
  다은 최종 대면을 건너뛰지 않는다. 기존 최종 선택이 끝난 뒤에만 엔딩
  캐스케이드가 다시 열리며 실제 `game_over` 신호 회귀 검사로 잠갔다.
- 다은 이별 변주 두 개를 3년차 후일담에 연결했다. 재혁의 300만원 판돈은
  “거의 전부”라 부르지 않으며, 보증 서명은 기존 협박 후폭풍과 재혁 엔딩
  기억이 읽는다. 아버지 소개인 단서는 상철의 t104 추리 전에 열릴 수 있다.
- 단일 선택 연결 장면에서 화면 QA가 다음 사건까지 과진행하던 결함을
  수리하고, 요청한 사건과 실제 열린 사건 ID가 다르면 즉시 실패하도록 했다.
- 한국어·영어 결별 56면과 약혼·결혼 76면, 정점 체인 32개, 대표 아크
  2경로, 엔딩 구분성 35종·실제 라우팅 12종, 밸런스·전략 발산,
  한영 1,565개와 Godot 54개 스크립트를 검증했다. 전체 감사는
  `✅ 감사 통과`였고 사용자 소유 `project.godot`은 제외했다.
- 결혼 비용·재혁 판돈 수치는 외부 표본 근거 없이 바꾸지 않았다. 이후
  플레이테스트가 해당 선택의 무망설임을 입증할 때만 별도 밸런스 오더로
  재개한다.

## 2026-07-26 (Codex — ORDER-48 정점 산문·엔딩 기억 P1)

### 큰 선택과 마지막 장면은 요약문이 아니라 실제 기억으로 남는다

- 다은의 조기 연애 확정은 계산기 불빛·멈춘 손·붉어진 눈가와 존댓말의
  박자로 늘렸고, 이혼은 계약서·도장·식은 차와 담판의 침묵으로 벌렸다.
  초기 선택지의 괄호형 관계 결과 예고는 한영 모두 제거했다.
- White 엔딩은 “아무도 밟지 않았다”는 검증 불가능한 선언 대신 이삿짐
  상자·오래된 수첩·아버지 약속·상철과의 거리·깨끗한 손을 실제 경로
  기억으로 회수한다. `empty_house`, `mental_break`, `career_burnout`도
  아버지·다은·현수·직업 선택에 따라 다른 후일담을 갖는다.
- 안정·정석·투자·비정석·균형·조기 은퇴 결산은 서로 다른 사물과 하루의
  동작으로 분리했다. 34개 `description_if_known`은 본문 복제가 아닌
  짧은 코다로 축약했고 핵심 엔딩 9종에 실제 선택 기억을 연결했다.
- 회귀 감사가 White 시각 앵커와 허위 무결 문장, 필수 기억 키, 코다 길이,
  한영 키 패리티를 잠근다. 다은 확정 3면·이혼 4면·엔딩 14면을 한국어와
  영어 1280×800에서 렌더해 문단 분할·잘림·버튼/CG 충돌을 확인했다.
- 사건 1,565개 정합·호칭, 정점 체인 32개, 엔딩 35종 구분성, 영문
  커버리지, Godot 54개 스크립트 컴파일을 포함한 전체 감사가
  `✅ 감사 통과`로 끝났다. 사용자 소유 `project.godot`은 수정 범위와
  커밋에서 제외한다.

## 2026-07-26 (Codex — USER-P0S 실플레이 가독성·인과·경마 P0)

### 한 사건의 글·조건·역할·장소·소리가 같은 사실을 말한다

- StoryMode의 긴 한영 본문과 결과를 실제 폰트·창 높이·사용자 글자
  크기로 계측해 여러 페이지로 안전 분할했다. 크기나 언어를 바꿔도 같은
  원문 위치를 유지하며, 추가 페이지는 배경·초상·CG·오디오 큐를 반복
  재생하지 않는다.
- 결과창의 정확한 호감도 수치와 관계 등급 해설을 제거했다. 선택의 의미는
  사람의 반응과 후속 장면으로 읽히며 숨은 시스템을 공략표로 만들지 않는다.
- `story_rules.json`의 사건 ID에 실행 가능한 `all/any` 선행조건, 현장
  참여자 역할, 기대 배경·초상·앰비언스를 결합했다. 일반 평가기는
  직업·숙련·주차·플래그 조건을 읽고 감사기는 한영 사건과 A/V 계약을
  교차 검증한다.
- 회사 팀장 사건은 실제 사무직만 통과한다. 지연 재회는 편의점에서 신촌역
  버스정류장으로 옮겨 민준=승객, 지연=운전자 역할을 고정했다. 청담
  한식당은 전용 식당 배경을 쓰고 커피를 받거나 솔직한 대화를 이어 간
  경로만 열린다.
- 경마 첫 체험의 가짜 고정 손실을 제거하고 실제 경마 미니게임으로
  넘겼다. 결과 화면은 좌우/Tab·확인·취소로 다음 경주와 나가기를 고르며,
  주간 결산은 카지노가 아니라 경마장 장소와 행동 원장을 유지한다.
- 추상적인 주간 문구를 구체적인 한국어·영어로 낮추고 영어 `파장` 표면은
  `WAVE`로 정렬했다. 일본어 UI 키도 같은 변경만 동기화했다.
- `StoryPlaybackCheck`, `CoreChoiceSliceCheck`, `InputMatrixCheck`,
  `ImmersionLoopCheck`, 사건 정합·시각·오디오 계약, 두 240주 대표
  경로와 전체 `audit.sh`를 통과했다. 54개 GDScript 강제 컴파일도
  깨끗했다.
- 사용자·Claude 소유 `project.godot`, 엔딩·다은 아크·자산 요청 문서와
  진행 중인 `ORDER-48` 변경은 이 커밋에서 제외한다.

## 2026-07-25 (Codex — USER-P0R 고시원 공용 주방 장소 P0)

### 현수와 라면을 끓이는 새벽은 민준의 개인실이 아니다

- `hyunsu_study_together`가 "고시원 공용 주방"을 명시하면서
  `goshiwon_room`을 고정해 침대와 개인 책상을 보여 주던 원인을
  재현했다.
- 같은 신촌 고시원의 낡은 벽·형광등·한밤 색온도를 잇는 좁은 공용
  주방을 전용 배경으로 제작했다. 싱크대·건조대·2구 조리기·선반·
  공용 냉장고를 물리적으로 연결하고 사람·침대·개인 책상·상표·판독
  문자는 배제했다.
- 사건 산문·선택·효과와 기존 고시원 룸톤·주전자·컵 폴리는 유지하고,
  사건 배경과 이미지 레지스트리·시각 계약만
  `goshiwon_shared_kitchen`으로 정렬했다.
- 1280×800 한국어 StoryMode에서 현수 초상·하단 대화창을 실제 합성해
  장소 동사와 원근이 읽히는 것을 확인했다. 사건별 회귀 계약은 이 장면이
  다시 개인실로 돌아가면 실패한다.
- 모드 매니페스트 244장, 시각 계약 236건, 해상도 기준선, 장면 오디오,
  한영 표면과 Godot 전체 감사를 검증했다. 사용자 소유
  `project.godot`과 진행 중인 `ORDER-48` 파일은 포함하지 않았다.

## 2026-07-25 (Codex — USER-P0Q 상철 사무실 오발 기침 P0)

### 두 사람뿐인 사설 사무실에 화면 밖 제3자의 기침이 들리지 않는다

- `office` 장소음이 공용 `public_interior` 사람층을 자동 호출했고, 그 녹음의
  14.7초 지점에 있던 실제 기침이 상철과 민준의 대화에 섞이는 원인을
  재현했다.
- 상철 첫 만남 4장면에만 `suppress_human_ambience` 계약을 적용했다.
  `amb_office_room.wav`는 장면 사이에서 이어지고 빗길 차량·종이컵·종이
  폴리와 무음악 연출도 그대로 유지한다.
- 이미 재생 중인 사람층이 페이드 중 기침을 흘리지 않도록 명시적 억제는
  해당 플레이어를 즉시 정지한다. 다른 사무실·학교·공공청사의 사람층은
  바꾸지 않았다.
- 장면 계약·BGM 연속성·오디오 자산 139개·녹음/샘플 출처·한영 표면과
  Godot 54개 스크립트 컴파일을 포함한 전체 감사가 `✅ 감사 통과`로 끝났다.
- 사용자 소유 `project.godot`과 진행 중인 `ORDER-48` 파일은 수정·
  스테이징하지 않았다.

## 2026-07-25 (Claude — 각본 리뷰 gapfill·ORDER-50 발행)

### 리뷰 미완 4조각을 채우고 잔존 신규 발견을 오더로 묶었다

- 세션 한도로 미뤘던 민준 목소리 스레드·2장·5장·gap감사를 소형 4에이전트 워크플로로 채웠다(44개 재시도 없이 표적 실행).
- 판정: 민준의 '몸'(33세·6년 빚·50만원)은 전 루트 일관하고 무릎 회상 회수는 육화의 모범. 2장은 확장의 장 정체성 뚜렷. **5장은 데드존이 아니라 의도된 하강**(뿌리는 줄지만 dik·회수로 두꺼워짐). gap감사는 그물 밖 8표면 확인 — 대부분 양질이나 chain_events·비인물 콜백 419건 미검토.
- **잔존 신규 발견 → ORDER-50 발행:** A(high) 프롤로그 동기 문장이 `arc_chapter1_close`·`arc_year1_close` 회고에서 '30억. 5년.' 숫자로 덮여 ORDER-23 동기 각인과 정면 모순(ORDER-47 이후에도 실파일 잔존 확인) B(medium) 작성형 루트 변주의 자기계발 설교 미끄러짐 C(medium) 5장 삼중 유혹 중복 D 다은/지연 잔존은 ORDER-48 범위에서 검증.
- Codex ORDER-48 WIP 커밋 후 트리가 정리된 창에서 인덱스 배선. `SCRIPT_REVIEW`는 ORDER-48 파일 범위라 건드리지 않고 발견을 ORDER-50 자기완결형으로 담았다. 커밋은 Codex WIP를 피해 큐/문서만.

## 2026-07-25 (Codex — USER-P0O/P0P 퍼블리셔·인물 연기 P0)

### 첫 화면은 승인된 브랜드로 시작하고, 피곤한 민준은 아픈 사람처럼 보이지 않는다

- 사용자 승인 1024×1024 RGBA `JUNPAC GAMES` PNG를 퍼블리셔 정본으로
  등록했다. 순백 전면 배경과 로고가 함께 사라진 뒤에만 강남드림의 어두운
  타이틀 필름이 시작되며, 과거 JPEG 보드와 코드 네이티브 초승달은 제거했다.
- 1280×800과 3840×2160 실제 프레임에서 흰 배경, 중앙 정렬, 로고 비율과
  안전 영역을 확인했다. 첫 30초 런타임 검사와 키아트 자산 감사도 새 PNG만
  허용하도록 잠갔다.
- 민준 피로 초상은 같은 33세 얼굴·검은 크루넥을 유지하면서 두 눈을 분명히
  뜬 하향 시선, 병색 없는 눈 밑 그림자, 척추는 세우고 어깨만 이완한 연기로
  교체했다. StoryMode 합성과 27개 크롭 회귀 화면에서 정체성과 하단 크롭을
  육안 확인했다.
- 인물마다 자세가 같아 보이지 않도록 민준·다은·지연·현수·재혁·상철·아버지의
  자세 실루엣을 정본화했다. 감정은 구부정한 상체 하나가 아니라 시선·턱·손·
  호흡·체중 이동으로 먼저 표현한다.
- 첫 30초·키아트·활성 이미지 243장·한영 표면·입력·저장·오디오·엔딩과
  Godot 54개 스크립트 컴파일을 포함한 전체 감사가 `✅ 감사 통과`로 끝났다.
- 사용자 소유 변경 `project.godot`과 진행 중인 `ORDER-48` 파일은 이 완료
  범위에 포함하지 않았다.

## 2026-07-25 (Codex — ORDER-47 편성·구조 P1)

### 240주의 장면이 같은 말을 연달아 하지 않고, 장 경계가 실제 달력에 선다

- 1장 환불선에 t35 직접 선택과 t33 Echo를 배치하고 2장 t87 직접 주를
  이동했다. 직접 결정 52회는 유지하며 장별 밀도를 13/9/10/10/10으로,
  Echo를 21회로 잠갔다.
- 1장 결산은 대포통장 선택을 상태별로 회수하고 현재 주거를 고시원으로
  오인하지 않는다. 보스 주를 실제 12월 1주차인 t45로 맞췄으며 2·4장
  결산의 보편 상태 변주와 결과문도 장 질문을 이어받게 보강했다.
- 3장 말미의 계산기·장부 장면을 4장으로 넘겨 22주·3주·6주·12주 간격으로
  분리했다. 2장 비교 장면 창, 4장 회고 창, 5장 고자산 공백과
  “1년 남았다” 연속 발화도 재배치했다.
- 대표 A/B 경로 아크 잼 0, 추정 223~246분, 장별 무작위 창 최소 1개,
  한영 1,565개, 비주얼 계약, 경제·종결 밴드, 실제 Godot 55개 스크립트
  컴파일을 포함한 전체 감사가 `✅ 감사 통과`로 끝났다.
- 사용자 소유 변경 `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-25 (Codex — ORDER-46 랜덤 풀 위생 P1)

### 짧은 사건이 보상 출처를 발명하거나 플레이어를 훈계하지 않는다

- 결과문에 수입 근거가 없던 직업·정치·코미디·정보·레버리지 사건의 단발
  금전 효과 5건을 제거했다. 프로필 확인에 투자감각이 오르던 모순, 앱 설치
  전 수익 확정, FOMO 금액 10배 오차와 국회의원 즉시 당선 스케일도 한영
  산문·효과·종결 경로에 맞춰 수리했다.
- 초기 에코 콜백 400개에서 마지막 격언을 걷어내 행동과 대화가 장면을
  닫게 했다. 후기 콜백 162개의 두 번째 분기는 자동 칭찬을 제거해 유보·
  비용이 남도록 했고, 대표 첫 분기 3건도 자기계발 결구를 낮췄다.
- 파손 문장과 사전형 선택 라벨을 교정하고,
  `random_pool_hygiene_audit.py`를 전체 감사에 연결했다. 이 게이트는
  한영 핵심 계약, 사건 수, 초기·후기 콜백 줄 예산을 함께 잠근다.
- 장기 밸런스 밴드와 5전략 종결 다양성, 이벤트 디렉터, 한영 커버리지,
  한글 누출, JSON, GDScript 전체 컴파일을 재검증했다.
- 사용자 소유 변경 `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-25 (Codex — ORDER-45 호칭·스피치 정본 P0)

### 관계의 시간이 말투에서 되돌아가지 않는다

- 한지연은 연애 확정 전 존댓말, 확정 후 “오빠”+반말로 거래·재회·데이트·결혼·
  엔딩을 정렬했다. 김다은→김민준 “민준씨”+존댓말, 민준→두 여성·임상철
  존댓말 기조도 콜백과 정점까지 전수 교정했다.
- 한국어 필드에 “오빠”가 있는 지연 대사는 같은 영어 필드에 `oppa`를 갖도록
  맞췄다. `arc_jiyeon_05_epilogue`의 의미 오역과 데이트 문장부호도 함께 수리했다.
- `speech_register_audit.py`를 전체 감사에 연결해 사건 29계약·엔딩 4계약과
  KR↔EN 동일 필드 호칭을 자동 검사한다. 한영 커버리지·한글 누출·정점 32개·
  GDScript 55개 컴파일을 포함한 전체 감사를 통과했다.
- `home-peaks`, `ending-p0/p1`, `namsan`, `first-snow`, `amusement` 영어 실렌더를
  확인했다. 한국어 `breakup` QA에서는 선택 전 이미 종결 상태가 서는 기존 진행
  부채를 별도 발견했으며, 이번 텍스트 변경의 회귀로 판정하지 않고 구조 수리 대상으로 남겼다.
- 사용자 소유 변경 `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-25 (Codex — ORDER-44 정합 위생 P0)

### 외부 테스트에서 즉시 신뢰를 깨는 정합 오류 18군을 닫았다

- 아버지 사망 후 `gangnam_dream`·`late_call`과 현재 시점의 생활·관계·전화
  사건에서 다시 등장하던 경로를 차단했다. 30억 달성 뒤 아버지가 사망한 런은
  `empty_house`로 귀결되며, 회복된 관계 플래그만으로 고인을 호출하지 않는다.
- 한지연·최재혁 성씨를 정본에 맞추고, 현수는 `STORY_BIBLE`의 회계 직무 궤적으로
  통일했다. 다은 후속 두 장면, 현수 잔존 경로, 상철 진실·NG+·첫 만남 경합을
  실제 생산 플래그와 발화 창에 맞췄다.
- 도달 불가였던 `startup_exit` 엔딩 변주를 실제 소유 엔딩으로 옮기고,
  `jiyeon_left` 이후 “한지연과 함께” 오발화를 막았다. 38세 마감, 6년 공백,
  보증 사기 연대, 강남 첫 기억과 서울 첫해 시제를 현재 정본으로 정렬했다.
- `EndingRouteIdentityCheck`를 11개 경로로 확장하고 정점 체인·엔딩 구분 감사
  계약을 새 라우팅에 맞췄다. 한영 JSON·커버리지·한글 누출·아크 흐름·스토리
  정합·문서 매니페스트와 전체 감사로 회귀를 확인했다.
- 사용자 소유 변경 `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-24 (Claude — 전체 각본 리뷰·ORDER-44~49 발행)

### 유저 요청으로 전 게임 스토리·각본을 정독 판정하고 오더로 묶었다

- 유저 7기준(지루함·로맨스·민준 몰입·반전·추리·기승전결·선택 고민) + 챕터 4질문(구분·흐름·장별 기승전결·분량)을 워크플로로 스레드별 전담 정독했다. 실제 이벤트 산문 전문 ~680사건을 읽고 채점·본문 인용·적대 검증했다.
- 완료: 9스레드(상철·재혁·다은·지연·아버지·현수·엔딩·랜덤풀·편성) + 1·3·4장 심층 + 5장 경계. 미완(세션 한도 반복): 민준 목소리 스레드·2·5장 심층·gap감사 — 8:10 이후 워크플로 resume로 부록 예정.
- 판정 요약: 강점=아버지 스레드(4.0)·상철/재혁 반전(4)·정점 산문·장 경계 3→4. 최대 약점=선택의 고민(다은 2/랜덤풀 2, 사랑·정직이 수치 상위호환 — 데모 R1과 동일 축) + 플레이 도달 확정 버그 다수.
- 근거 원장 `docs/SCRIPT_REVIEW_2026-07-24.md` 생성. 오더 6종 발행:
  - **ORDER-44 [P0]** 정합 위생 — 죽은 아버지 재등장(gangnam_dream·late_call 가드 누락)·이지연/이재혁 오기·현수 요리↔회계·다은 money_gap/trace 도달불가·상철 진실 증발·시제 동요.
  - **ORDER-45 [P0]** 호칭 정본 붕괴 전수(지연·다은·민준→상철 반말 이탈).
  - **ORDER-46 [P1]** 랜덤 풀 위생(근거 없는 금전·산문/효과 오차·파손 문장·설교투 격언).
  - **ORDER-47 [P1]** 편성/구조(1장 보스 결산·환불선 t25~48 밀도·중반 계산장면 6연속·4장 함몰·4→5 '1년 남았다' 3연속).
  - **ORDER-48 [P1]** 정점 산문 밀도 역전(조기 고백·이혼 상실 압축·S+ 엔딩 무변주·결산 6종 템플릿).
  - **ORDER-49 [⚠P2·밸런스/유저 승인]** 선택의 고민 장부화 — 산문 정렬(무수치)만 선제, 수치 변경은 외부 표본 데이터+유저 승인 후.
- 전부 콘텐츠 동결 준수(신규 사건 0, 문장 수리·플래그 연결·창 재배치·dik 재배치). 커밋은 Codex WIP를 피해 리뷰/오더/인덱스 문서만.

## 2026-07-24 (Codex — USER-P0M 엔딩 35종 전용 아트)

### 마지막 삶 35개가 텍스트 전에 서로 다른 결말로 보인다

- 전용 이미지가 없던 엔딩 14종을 신규 제작하고, 반사·행동·단계 구분·가림이 틀린 기존 4종과 본문상 55세 거실이어야 했던 `orthodox_hollow`를 교체했다. 활성 35종은 모두 고유한 CG 키·파일 경로·SHA-256과 정확한 1280x800 PNG를 소유한다.
- 엔딩 UI의 코드 도형·공용 무드 카드·배경 크롭 폴백과 관련 SVG 4종을 제거했다. 이미지는 비율을 보존해 전부 보여 주고, 왼쪽 아래 2차원 스크림만 사용해 얼굴·손·핵심 소품을 가리지 않는다.
- 35개 전부에 카메라·시선·손·의상 계약을 기록했다. 정신과 의사와 업계 동료를 B급 장면 배우로 등록하고, 신규 14개 엔딩도 장소에 맞는 기존 실녹음 환경음을 배선했다.
- 한영 `ending-all` 35장씩 총 70장과 종횡비 회귀 검사를 통과했다. 엔딩 구분성은 `shared_visual_groups=0`, `symbols=0`, `dedicated_cg=35`이며 전체 감사는 사건 1,565개, 영어 한글 누출 0, 활성 CG 74개, GDScript 55개 컴파일까지 `✅ 감사 통과`로 끝났다.
- 사용자 소유 변경 `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-24 (Codex — USER-P0L 장기 맥락 아키텍처)

### 새 세션이 기록의 무게가 아니라 현재 작업에서 시작한다

- 기존 `CLAUDE.md`와 큐를 합쳐 기본 부팅에 약 419KB가 필요하던 구조를 16.9KB로 줄였다. `CLAUDE.md`는 현재 상태·불변 규칙·운영·검증만 남기고, `CONTEXT_INDEX.md`가 서사·루프·아트·오디오·UI·현지화·출시 작업별 필수 정본을 단계적으로 안내한다.
- `context_manifest.json`에 정본 소유자, 작업 프로필, 문서 생명주기, 기본 로드 금지 이력, 크기 예산을 기계 판독 가능하게 고정했다. `context_manifest_check.py`는 92개 문서 분류, 부팅 예산, 관리 문서 링크, 핵심 시간축을 검사하며 전체 `audit.sh`의 첫 게이트가 됐다.
- 738,138바이트 `WORK_LOG.md` 원문은 `docs/history/WORK_LOG_2026-05-16_to_2026-07-24.md`로 손실 없이 보관하고 최근 로그를 새로 시작했다. 큐·결정·릴리스·과거 감사는 기본 컨텍스트가 아니라 키워드 검색 대상이 됐다.
- 저장소 안에 얇은 `.codex/skills/gangnamdream-dev` 스킬을 만들고 `~/.codex/skills/gangnamdream-dev`에 연결했다. 스킬은 세계관을 복제하지 않고 부팅·큐 선언·정본 라우팅·표적 QA·사용자 변경 보호만 가르친다.
- 낡은 `CANON_MAP.md`의 `5 years, 60 turns`를 실제 정본인 `5 years, 240 weeks`로 정렬하고, 코드와 문서가 충돌할 때 코드를 자동 정답으로 취급하지 않는 판정 순서를 명시했다.
- 스킬 공식 validator, 문서 게이트, 실제 Godot 경로의 전체 감사를 통과했다. 사건 1,565개, 엔딩 35개, 영어 한글 누출 0, 55개 GDScript 전체 컴파일이 유지됐고 사용자 변경 `project.godot`은 건드리지 않았다.
