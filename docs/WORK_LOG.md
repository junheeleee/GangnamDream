# Gangnam Dream Work Log

> 최신 작업만 이 파일에 역순으로 기록한다. 2026-05-16부터 2026-07-24 USER-P0M까지의 원문은
> [`history/WORK_LOG_2026-05-16_to_2026-07-24.md`](history/WORK_LOG_2026-05-16_to_2026-07-24.md)에 손실 없이 보존했다.
> 2026-07-25의 오래된 항목은
> [`history/WORK_LOG_2026-07-25.md`](history/WORK_LOG_2026-07-25.md)로
> 차례로 옮긴다.
> 과거 근거는 기본 컨텍스트에 넣지 말고 먼저 `rg -n "<키워드>" docs/history/`로 찾는다.

## 2026-07-31 (Claude — 현황 대시보드 `tools/project_dashboard.py`)

- 사용자 질문: *"게임개발 진행상황을 파악하거나 각 이벤트 선택지에 따른 결과
  마인드맵이나 각종 컨텐츠, 일정, 인물정보를 쉽게 판단할 수 없을까?"*
- **손으로 그린 현황판을 만들지 않았다.** 그런 문서는 만든 날에만 맞다. 대신
  저장소를 읽어 한 장으로 생성하는 도구를 만들었다 — `CODEX_QUEUE.md` 표(오더),
  래칫 기준선 셋, `identity_signature.json`(주연 여섯 서명), `narrative_spine.json`
  (5장이 여닫는 동사), `demo_core_loop_v2.json`(24주 번들 56개), 그리고 모든
  KR 이벤트의 `choices`·`follow_up_event`로 만든 **선택 체인 그래프**.
- 체인 탐색기는 `ORDER-60` 배치 2의 **지배 선택 판정을 브라우저에서 다시 돌린다.**
  판정 로직이 감사 도구와 화면 양쪽에 있으므로 정의가 바뀌면 둘 다 고쳐야 한다.
  현재 그래프(2비트 이상 체인 65개, 211비트)에서 11건이 정답 선택으로 걸린다.
- **개발용 화면임을 페이지 상단에 명시했다.** `tint`·`route_*`·정확한 수치를 그대로
  보여 주는데 이는 플레이어에게 절대 노출하지 않는 값이다. 도구가 정본 규칙을
  우회하지 않도록 경고를 화면이 소유한다.
- 출력은 `build/`(gitignore)이므로 저장소에는 생성기만 들어간다. `audit.sh`에
  넣지 않았다 — 판정을 내리지 않는 생성기이며 게이트에 넣으면 시간만 든다.
- **사용자 지적으로 배선을 고쳤다** — *"저렇게 만들면 코덱스가 매번 업데이트도
  어려울뿐더러 저 세션 닫으면 못 보는 대시보드 아니야?"* 맞다. 아티팩트 링크는
  남지만 **내용은 올린 시각의 스냅샷이고 Codex는 아티팩트를 올릴 수 없다.**
  `.github/workflows/dashboard.yml`을 추가해 `main` 푸시마다 CI가 다시 만들어
  Pages에 올린다. **갱신을 사람이나 에이전트가 맡으면 잊는 순간 거짓말이 되므로
  저장소가 바뀌는 사건에 직접 걸었다.** 페이지 상단에 생성 시각·커밋 SHA를 찍어
  신선도를 화면이 스스로 증명하게 했다.
- 저장소가 공개라 Pages도 공개다. 같은 값이 이미 `content/*.json`에 있어 새로
  새는 정보는 없지만 검색되는 렌더 페이지는 성격이 달라 `noindex`를 넣었고,
  **켜는 행위를 사람 손에 남겼다** — 워크플로만으로는 아무것도 공개되지 않는다.
- 검증: 실제 렌더를 Chromium으로 확인했다(라이트·다크 양쪽, `data-theme` 포함).
  한국어 어절 중간 줄바꿈(`word-break:keep-all`)과 `ORDER-57`이 `ORDE/R-57`로
  쪼개지던 것을 고쳤다. 검색·선택 배선은 DOM 덤프로 확인했다(`daeun` → 12체인).

## 2026-07-30 (Claude — ORDER-60 배치 2 부분: 도덕 축 전 구간 측정)

- 배치 1의 `착한 쪽이 비용 없이 tint를 얻는다`가 계통인지 확인하려고 전 구간을
  기계로 쟀다. `tint 양수·음수 효과 없음`은 부정확해서(프롤로그가 반례였다)
  **지배 선택**으로 정의했다 — 모든 축에서 우월하고 후속·플래그도 갈리지 않는 선택.
- **전 구간 413/1,484(28%)가 순수 정답 선택이다.** 지배 선택은 639건이고 그중
  226건은 후속·플래그로 길이 갈려 대가가 있다. 프롤로그의 3/4는 우연이 아니었다.
- **데모 V2는 2/33(6%)로 예외적으로 건강하다.** `ORDER-57` 재구축이 이 축에서
  실제로 효과가 있었다는 첫 측정 증거다. 걸린 2건도 `tint`가 0이라 도덕 축이 아니다.
- 전형은 대가 부재가 아니라 **이중 보상**이다 — `arc_father_04_visit`는 `tint +9`에
  `mental +23`, `arc_daeun_proposal_answer`는 `tint +8`에 `mental +25`. 다만 감정의
  정점이라 `mental` 상승 자체는 옳고, 고칠 것은 밀어낸 것의 부재다.
- **배치 2를 완결했다.** 사용자가 범위 A(V2만 판정)를 택했다. 남은 기준을 재니 데모 V2는 **정합은 통과하고 밀도는 미달**이다 — 자산 미등록 0, EN 43/43 한글 누출 0, 루트 무결성 0건인데 **`existing_roots` 장면 27개 중 21개가 1비트**이고 **도달 이벤트 43개 중 39개에 `direction`이 없다(91%)**. `core_loop_v2_events.json` 신규 16개는 전부 0이다. 번들 56개 중 27개는 `action_id` 행동이라 애초에 장면이 아니다. `ORDER-57`은 구조를 옳게 짓고 아직 채우지 않았으며, 이는 E의 `장면 밀도 REWORK`가 이미 소유한다.
- 원래 멈췄던 이유: `runtime_default=false`라 판정한 V2가 지금 출고되는
  것이 아닌데, 폴백 구간은 주차가 아니라 플래그·아크로 걸려 정확히 열거되지 않는다.
  `min_turn<=24` 같은 필터는 후반 사건을 대량으로 끌어와 **방어할 수 없는 숫자라
  쓰지 않았다.** 범위 선택지 A/B/C를 사용자에게 올렸고 권고는 A(V2만 판정)다.
- P0은 없다. 판정표는 [`DEMO_TIER_AUDIT.md`](DEMO_TIER_AUDIT.md).

## 2026-07-30 (Claude — ORDER-60 배치 1 프롤로그 재판정)

- 프롤로그 11장면(`story_flashforward`~`story_prologue_meal`)을 2026-07-30에 선
  정본 여덟으로 재판정하고 [`PROLOGUE_TIER_AUDIT.md`](PROLOGUE_TIER_AUDIT.md)에
  판정표를 남겼다. **판정 오더라 콘텐츠·코드는 고치지 않았다. P0은 0건이다.**
- **계통 결함 하나**: 도덕 축이 대가를 읽지 않는다. 선택이 있는 4장면 중 3장면에서
  착한 쪽이 비용 없이 `tint`를 얻는다. `story_prologue_dad`는 한쪽이 순수 이득
  (`mental +5`·`tint +2`·아버지 호감 +12), 다른 쪽이 순수 손실이다. **산문은
  이미 대가를 안다** — 결과문이 스스로 `거짓말이었다`라고 쓴다. 수치만 모른다.
  `story_prologue_meal`은 실제 교환이 있어 대조군이 된다.
- `direction` 누락 4건(`story_arrival`·`story_prologue_dad`·`story_prologue_goal`·
  `story_prologue_meal`). 무릎·상환 체인 7장면은 전부 보유하는데, 하필 상품 정의를
  지는 뒤쪽 네 장면만 비었다. 렌더러는 이미 있어 수리 비용이 가장 낮다.
- 정본 쪽 공백 하나를 발견하고 **같은 날 사용자 승인으로 해소했다**:
  `SCENE_TIER`가 체인을 한 장면으로 세는지 규정하지 않아, T1의 `체인 2~4개`를
  문자대로 읽으면 11이벤트 프롤로그가 너무 길어서 미달이 됐다. §0을 세워
  **체인 하나가 장면 하나, 계약은 체인 전체에, 길이 상한 폐지**로 정했다.
  다시 판정하니 **프롤로그는 이미 T1이고 밀도 계약을 전부 충족한다** — 남은
  것은 연출 커버리지와 도덕 축 둘뿐이다. `[첫 실행 재조정]` 표시가 실제로
  작동한 첫 사례이며, 추정 수치가 방어되지 않고 고쳐졌다.
- 통과: 자산 10종 전부 등록, 한영 11/11에 한글 누출 0, 아버지 병 언급은
  `STORY_BIBLE.md:93`과 일치, 플래그 9개 전부 독자 보유, 번역체·설명조 없음.
- 조사 함정: **플래그는 `conditions`가 아니라 dik 산문 키로 소비된다.**
  `conditions`만 훑으면 살아 있는 플래그를 휴면으로 오판한다.

## 2026-07-30 (Codex — ORDER-57 D 1~20주 기다림·충돌 완결)

- 다섯 번째 달은 모든 경로의 실용 제안 5개에 실제 영수증이 있는 관계
  후속과 돈 인물 후속을 각각 최대 하나 더한다. 다은·지연·상철·재혁
  후속은 이전 선택·연락 수단·관계 단계를 읽고, 모두 20주 같은 배타
  자리를 요구해 한 경로에서 사람 장면은 최대 하나만 고른다.
- 한빛유통은 제출·면접을 마친 경로에만 17주 화요일 채용 문자를 보낸다.
  수락 시 월급 224만원은 유지하되 18~20주 첫 세 주만 168만원을
  지급하고, 실제 수신 기록은 선택 뒤에도 메시지함에 남는다. 도시시설
  지원은 같은 주 금요일 오후 6시까지 열어 시간 모순을 없앴다.
- 이삿짐은 선택한 주 나흘 연속, 전산반은 선택한 주 월~목 저녁 집중
  일정으로 바꿔 미래 주차를 미리 보상하지 않는다. 20주 원장은 무부업
  체납 105만원, 4·5개월 대타 3만원, 세 번 대타 39만원, 앞선 두 대타+
  한빛 취업 130만원과 리퍼폰 뒤 112만원을 잠갔다.
- 1,581사건 한영·서사·시각·오디오·장면 계약, A/B/C/D 저장 런타임,
  KO 960×600과 EN 1280×720 휴대폰 렌더, 전체 감사를 통과했다. 20주
  결산 뒤 21주 기존 편성기로 떨어지지 않고, `father_health_signal`은
  E 소유 21주 비슬롯으로 보존한다. `runtime_default=false`, 본편
  21~240주, 엔딩과 사용자 소유 `project.godot`은 유지했다.
- **사람 게이트는 OPEN이다.** 정상 속도 충돌 기억·재미·물리 패드와
  21~24주 첫 청구서·6개월 회고가 끝나기 전 기본 전환을 하지 않는다.

## 2026-07-30 (Codex — ORDER-57 C 1~16주 선제 추적·기기 세대 정합)

- 한빛유통의 제출→면접/불참, 다은의 선택별 직접 재방문, 연락처 없이
  버스정류장에서 재회하는 지연, 카페의 재개발 통화를 들은 뒤 방 시세를
  묻는 상철, SNS 압박 뒤 연락해 온 재혁을 실제 선행 상태에 연결했다.
  관계는 한 인물당 한 달 한 단계만 오르고 C에서 자동 데이트·채용·
  투자 권유·30억원 진실을 선취하지 않는다.
- 네 번째 달은 성긴 상태 5개·풍부한 상태 7개 제안, 고유 자격증·물류·
  검진·회복·주거복지 행동과 원자 저장을 가진다. 16주 무부업 체납
  68만원, 늦은 대타만 고른 체납 16만원, 두 합법 대타 뒤 20만원,
  리퍼폰 구매 뒤 2만원을 결정론 원장으로 잠갔다.
- 실제 수신·통화만 사람 연락으로 보이고 메모와 포기 영수증은 별도
  기록으로 분리했다. 시작폰은 최신형처럼 보이던 얇은 베젤을
  2013~2015년형 중고 보급폰의 두꺼운 플라스틱 셸·넓은 상하 베젤·수화부·
  작동하는 물리 홈/메뉴/뒤로 키로 교체했다. 리퍼폰·플래그십은 금속 셸·
  제스처 바를 유지한다.
- A/B/C 런타임, 1,575사건 정본·한영·시각·오디오·장면 계약,
  KO/EN 1280×720·960×600 실제 렌더와 전체 감사가 PASS다. 16주 결산 뒤
  17주 기존 편성기로 떨어지지 않는다. 정상 속도 기억·재미·물리 패드,
  17~24주와 기본 전환은 OPEN이며 사용자 소유 `project.godot`은
  수정·스테이징하지 않았다.

## 2026-07-29 (Codex — ORDER-57 휴대폰 허브·대화 기록 첫 완성)

- 현재 장소 위에서 세로→가로로 돌아오는 실물 휴대폰과 4열 앱 홈,
  메시지 목록→채팅 말풍선, 일정·연락처·은행·기기 앱, 하단 제스처 바를
  연결했다. 날짜·통신·배터리는 상태바, 실제 잔액은 홈·은행이 읽는다.
  시작폰·리퍼폰·최신형 플래그십은 독립 재질과 화면 인셋을 쓴다.
- 시작폰과 13주 리퍼폰의 저장 이관, 18만원 결제 전 가격·고정비·구매 후
  잔액 확인, 중복·다운그레이드·조기·잔액 부족 구매 차단을
  `PhoneSystem` 한 곳에서 소유한다. schema 2 리퍼폰은 구매 뒤 플레이어가
  직접 고른 앱 하나만 홈 첫 칸에 고정하며 기기 등급은 서사·수익률·승률·
  관계 보상을 바꾸지 않는다. 미구현 중급폰·플래그십은 전체판에서도
  상점과 결제를 막는다.
- 세 휴대폰 파손 사건은 기기 등급을 몰래 바꾸지 않는 수리로 고치고
  6만·10만·12만·18만·25만·30만원 산문과 실제 현금을 맞췄다. 고시원
  월세 사건은 65만원 월말 정산을 미리 중복 차감하지 않는 폴백 알림으로
  바꿔 중요한 인과 후속을 밀지 않게 하고 주거 조건을 붙였다.
  `통장을 열었다/닫았다/봤다` 같은 번역투도 실제
  계좌·잔액·은행 앱 행동으로 한영 함께 고쳤다.
- StoryMode에 `대화 기록`을 추가했다. 현재 대화에서 실제로 본 본문과
  확정한 선택·결과만 보이며 미선택 선택지와 숨은 수치는 보이지 않는다.
  기록을 읽는 동안 타이핑·AUTO·연출·선택 타이머를 멈추고 닫으면 같은
  선택지와 남은 시간으로 돌아온다. 중간 저장과 즉시 후속에서도 유지한다.
  실제 대괄호 공지문은 보존하고 BBCode만 제거하며, 글자 크기 변경 뒤에도
  원문 진행률로 복원한다. 기능 도입 전 저장에는 이전 기록 부재를 알린다.
- 휴대폰은 KO 1280×720·EN 960×600·KO 1920×1080 동작 줄이기에서
  실제 렌더와 독립 시각 검수 P0/P1 0 GO를 받았다. 대화 기록, 상태·입력·
  저장, 일본어 UI와 전체 감사도 PASS다. `runtime_default=false`, 25~240주와 사용자
  소유 `project.godot`은 유지했다.

## 2026-07-29 (Codex — ORDER-57 B 1~12주 관계 입구·원자 저장 완결)

- 세 번째 달의 행동·기한·포기 소비자와 typed 선행조건을 연결했다.
  생활 동선에 따라 다은/지연 중 최대 한 첫 만남만 열고, 아버지·현수
  후속은 실제 선택 기억과 플레이어 주도권을 다시 검사한다.
- 선택별 관계 기억, 미래산업기술·서린물산 지원 상태 전이, 한빛유통의
  13~16주 면접 계약과 대포통장 경찰 추적의 24주 소유권을 잠갔다.
  행동·결과·월말 저장은 원자 적용과 정확히 한 번 복구를 통과했다.
- 월간 증감은 월초부터 네 주 전체를 계산한다. 8만원 수리, 현금
  200만원·150만원 반환·보관함 현금 300만원, 체납 31만원·재고조사 뒤
  5만원을 장면과 장부에서 맞추고 번역투·발신자·고정 잔고 모순을 수리했다.
- 1,570사건 한영, A/B 런타임, 240주 인과·밸런스·CG·오디오·입력과
  전체 감사가 PASS다. 사람 기억·재미·물리 패드 GO와 기본 전환은
  OPEN이며 `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-29 (Codex — ORDER-57 A1 1~8주 공통 루프 완결)

- 주/보조 루틴을 이야기 주에도 8주 총 16단위씩 한 번만 계산하고,
  월중 취업은 본업+기존 주 루틴으로 전환한다. 14개 카드의 기한,
  미선택 결과 13종의 한영 소비자, 계획·결과 중복 차단을 잠갔다.
- 관계는 실제 선택으로만 전이한다. 민준이 먼저 메시지를 보내고 다음 날
  현수와 공용 주방에서 만나는 전용 장면은 원격 문단의 초상을 숨긴다.
  저장형 월말 수첩과 합법 두 경로의 6만원 생존 하한도 연결했다.
- 정적·밸런스·저장·KO/EN 720p·일본어 UI·A/V·전체 감사가 PASS다.
  사람 GO는 OPEN이고 기본 전환·`project.godot`은 유지했다.

## 2026-07-29 (Codex — ORDER-57 24주 계획·A0 세션 경계 수리)

### 실제 상품은 24주로 고정하되, 완성된 월만 차례로 연다

- 사용자 승인에 따라 1~8주 진단을 수용하고 최종 데모를 1~24주로
  고정했다. 제작은 `8 → 12 → 16 → 20 → 24주` 게이트로 진행하며,
  첫 만남만 있는 12주가 아니라 선제 연락까지 있는 16주에서 관계
  마이크로 슬라이스를 판정한다.
- 8주차 월말 경제 정산 뒤 완료 마커를 저장하고 9주차 기존 편성기로
  떨어지지 않게 했다. 다시 불러와도 실제 지킨 약속·닫힌 제안,
  대포통장 분기, 선제 연락, 현금·몸·마음·다음 경제 한 단을 보여 주는
  KO/EN 회고로 돌아오며 `finish_run`과 본편 엔딩은 호출하지 않는다.
- 휴대폰 메시지 6~7개를 720p 두 열로 수용했다. 기록 탭은 숫자 대신
  실제 약속과 미선택 제안의 이름을 보여 주고, 첫 확인은 확정 전 검토,
  East는 일정 보존 편집 복귀, 두 번째 확인만 실제 계획을 저장한다.
- 상태·저장·실제 `MainGame` 회고, 의미 입력, KO/EN 1280×720 경계와
  한글 누출을 표적 검사로 고정했고 전체 감사도 통과했다. 이번 묶음은 A0이며 배경 루틴,
  `forgone`의 실제 다음 달 소비자, 기한, 결과 기반 관계 단계와 합법
  경제 회수는 다음 A1이다. `runtime_default=false`와 사용자 소유
  `project.godot`은 유지했다.

## 2026-07-27 (Codex — 빌드 식별·V2 테스트 진입)

- `v0.1.0-dev / BUILD 2026.07.27.1`을 시작 화면·실행창에 표시하고,
  DEBUG V2 진입만 창 제목에 채널을 남긴다. 일반 시작은 기존판이며 전체 감사 PASS다.

## 2026-07-27 (Codex — 프롤로그 초상·화자 분리)

- 과거 아버지 초상은 유지하되 민준 시점 서술의 이름표를 숨겼다.

## 2026-07-27 (Codex — ORDER-57 Core Loop V2 1~8주 런타임)

- 명시적 개발 런에 휴대폰·월 네 약속·포기·회수와 지원·면접·부업을
  연결했다. 한영·저장·패드·컴파일은 PASS다.
- 기본 전환과 9~24주 확장은 8주 사람 GO까지 보류한다.

## 2026-07-27 (Codex — ORDER-57 Core Loop V2 설계·6개월 계약)

### 매주 배급받던 AP를 내가 고른 월간 네 약속으로 바꾼다

- 짧은 데모·자동 관계·반복 행동을 NO-GO로 기록하고 1~24주 정본을 만들었다.
- 월 5~7개 제안 중 네 약속, 휴대폰 네 탭, 관계 11단계, 놓친 길과 월말
  회수를 고정했다. 숨은 수치·미래 루트·선제 행동 없는 관계 진전은 금지한다.
- 6개월·28장면 번들을 기계 계약과 전체 감사에 연결했다. 기존 AP 전체판은
  사람 GO 전까지 폴백으로 보존한다.

## 2026-07-27 (Codex — ORDER-56 노출 상태 정합 전수 계약, 2026-07-29 정본 보정)

### 지금 플레이에 나올 수 있는 모든 사건이 자기 상태 가정을 증명한다

- 런타임 전경·다리 허용목록, 아크 스케줄러 226건, 직접·지연 후속을
  따라 현재 진입점 307개와 폐쇄 집합 443개를 계산했다. 개수와 사건 ID
  SHA-256을 함께 고정해 새 노출 사건이 계약 없이 섞이면 감사를
  실패시키도록 했다.
- 노출 사건을 상태 민감 434건과 검토 중립 9건으로 전수 분류했다.
  직업 99, 주거 167, 관계 214, 아버지 생사 111, 장소 409건의
  중첩 축을 실행 조건·원장·생산자·후속 경로에 대조한다.
- 고위험 26장면은 산문 원장·시각·오디오 3층 계약을 모두 요구하고,
  고시원 소유가 확정된 7장면은 현재 주거와 혼동하지 않도록 고정했다.
  원장은 160건, 원격 장면 52건, 동적 장소 83건, 전환 65건으로
  확장했다.
- 무직 상태의 회사 회고, 이사 뒤 고시원 회귀, 현수 방의 소유자 혼동,
  휴일의 아버지 생사 충돌, 마지막 겨울 중복, 다은 동거 장면의 고정
  주거, 직업·주거를 발명하는 장기 회고를 실제 한영 산문과 조건·
  배경·오디오·전환에서 함께 수리했다.
- Godot 54개 스크립트 컴파일과 데모 회귀, 대표 240주 아크·연출·
  오디오, 한영 1,565사건, 컨텍스트·영어 누출·공백 검사를 포함한
  전체 감사가 `✅ 감사 통과`로 끝났다. 휴면 1,123사건과 사람 눈이
  필요한 미술 연속성은 이번 보증 범위가 아니며, 사용자 소유
  `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-26 (Codex — ORDER-55 노출 사건 직업·주거·장소 정합 QA)

### 이사와 퇴사 뒤에도 장면이 과거 상태를 발명하지 않는다

- 실제 도달 가능한 사건을 직업·주거·장소 원장과 교차해 전화·문자·
  사적 회상·생활 콜백을 `current_housing`으로 정렬했다. 이사 뒤
  고시원으로 되돌아가던 장면을 제거하고 임상철 대면 차 장면은 카페로
  고정했다.
- 직업을 선택하지 않은 저장에서도 발화하는 아버지 약 장면은 사무실에서
  대중교통 귀갓길로 옮겼다. 장기 회고의 직장·출근 기정사실은 일·수입·
  일정의 보편 문장으로 바꾸고, 회사 협상 홀덤 사건과 커리어 전문화
  결과는 현재 재직 상태를 다시 확인하도록 잠갔다.
- 사건 JSON, 한영 산문, `story_rules`, 시각·오디오·장면 전환
  매니페스트를 같은 ID로 정렬했다. 정합 감사는 동적 장소와 사건 배경의
  불일치를 빌드 실패로 처리하며 현재 원장 **135/1,565(8.6%)**,
  논리 49, 원격 49, 동적 장소 58, 전환 64, 무단 데모 점프 0이다.
- Godot 데모 회귀와 영어 `chapter2-peaks` 18면 실제 렌더에서 아버지
  약 장면의 지하철, 지연 장면의 비 오는 거리, 무직 회사 사건 차단을
  확인했다. 전체 감사, 한영 1,565사건, 컴파일 54개를 통과했으며 사용자
  소유 `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-26 (Codex — ORDER-54 씨앗-수확 12체인 부활)

### 사소한 선택이 8~12주 뒤 실제 상태와 모순 없이 돌아온다

- 낯선 사람 씨앗 5건의 장면 밀도를 전경 계약까지 높이고 생산자 선택
  14행을 고유 수확 12건에 예약했다. 전경 수확 2건을 빼고 씨앗 5건을
  넣어 전경은 **61 - 2 + 5 = 64건**, 도달 체인은 **0 → 12건**,
  휴면 체인은 **12 → 0건**이 됐다.
- 예약 발화 시 현재 직업·주거 조건을 다시 검사한다. 편의점을 그만둔 뒤
  연예인이 계산대로 돌아오거나, 고시원을 떠난 뒤 옆방 이사가 재현되거나,
  다른 회사 재직 중 인테리어 입사 제안이 강제되는 경로를 막았다. 무효
  예약과 같은 주에 만기인 다음 유효 예약은 삼키지 않는다.
- 계열사 합격은 실제 `job_08`·월 455만원, 편의점에서 이어진 제안은
  `job_01 → job_03`·월 224만원 이직으로 연결했다. 50만원은 현장
  인수인계 수당으로 명시했고 다른 월수입은 이직 뒤에도 보존한다.
- 거절·보류 11분기에 서로 다른 기억 플래그를 남기고 마지막 겨울의
  한영 산문이 실제로 읽게 했다. 격언·회계 결구는 명함, 반찬통, 줄자,
  닫힌 공고문 같은 사물과 행동으로 교체했다.
- 추가 재검수에서 고시원 이웃 씨앗의 거주 조건, 이웃 문자 장면의 현재
  주거 배경, 편의점 이미지에 맞지 않던 마트 벨트·카트 소품을 한영
  동시에 바로잡았다. 새 이직 로그는 일본어 UI 키까지 맞췄다.
- 정적 부채 0, 체인 12/12, 대표 A/B 240주 아크 잼 0, 5전략 발산,
  한영 1,565사건, 일본어 UI 키, Godot 전체 컴파일과 전체 감사를
  통과했다. 사용자 소유 `project.godot`은 수정·스테이징하지 않았다.

2026-07-26 이전 항목은 [`history/WORK_LOG_2026-07-26.md`](history/WORK_LOG_2026-07-26.md)와
[`history/WORK_LOG_2026-07-25.md`](history/WORK_LOG_2026-07-25.md),
[`history/WORK_LOG_2026-05-16_to_2026-07-24.md`](history/WORK_LOG_2026-05-16_to_2026-07-24.md)에 있다.
