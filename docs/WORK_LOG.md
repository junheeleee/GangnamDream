# Gangnam Dream Work Log

> 최신 작업만 이 파일에 역순으로 기록한다. 2026-05-16부터 2026-07-24 USER-P0M까지의 원문은
> [`history/WORK_LOG_2026-05-16_to_2026-07-24.md`](history/WORK_LOG_2026-05-16_to_2026-07-24.md)에 손실 없이 보존했다.
> 과거 근거는 기본 컨텍스트에 넣지 말고 먼저 `rg -n "<키워드>" docs/history/`로 찾는다.

## 2026-07-27 (Codex — 프롤로그 초상·화자 분리)

- 두 과거 장면은 아버지 초상을 유지하되 민준 시점 서술의 이름표를 숨긴다.
  원장·런타임·언어 갱신 회귀 검사가 이를 잠근다.

## 2026-07-27 (Codex — ORDER-57 Core Loop V2 1~8주 런타임)

### 계획을 고르고, 놓치고, 다음 달에 돌려받는 첫 두 달을 실제 게임에 연결했다

- 명시적 개발 런에 휴대폰·월 네 약속·포기·지연 회수와 실제
  지원·면접·부업을 연결했다. 한영·저장·패드·컴파일은 PASS다.
- 기본 전환과 9~24주 확장은 8주 사람 GO까지 보류한다.

## 2026-07-27 (Codex — ORDER-57 Core Loop V2 설계·6개월 계약)

### 매주 배급받던 AP를 내가 고른 월간 네 약속으로 바꾼다

- 짧은 데모·자동 관계·반복 행동을 NO-GO로 기록하고 1~24주 정본을 만들었다.
- 월 5~7개 제안 중 네 약속, 휴대폰 네 탭, 관계 11단계, 놓친 길과 월말
  회수를 고정했다. 숨은 수치·미래 루트·선제 행동 없는 관계 진전은 금지한다.
- 6개월·28장면 번들을 기계 계약과 전체 감사에 연결했다. 기존 AP 전체판은
  사람 GO 전까지 폴백으로 보존한다.

## 2026-07-27 (Codex — ORDER-56 노출 442사건 상태 정합 전수 계약)

### 지금 플레이에 나올 수 있는 모든 사건이 자기 상태 가정을 증명한다

- 런타임 전경·다리 허용목록, 아크 스케줄러 226건, 직접·지연 후속을
  따라 진입점 306개와 폐쇄 집합 442개를 계산했다. 개수와 사건 ID
  SHA-256을 함께 고정해 새 노출 사건이 계약 없이 섞이면 감사를
  실패시키도록 했다.
- 노출 사건을 상태 민감 433건과 검토 중립 9건으로 전수 분류했다.
  직업 98, 주거 163, 관계 214, 아버지 생사 111, 장소 408건의
  중첩 축을 실행 조건·원장·생산자·후속 경로에 대조한다.
- 고위험 26장면은 산문 원장·시각·오디오 3층 계약을 모두 요구하고,
  고시원 소유가 확정된 7장면은 현재 주거와 혼동하지 않도록 고정했다.
  원장은 160건, 원격 장면 52건, 동적 장소 83건, 전환 65건으로
  확장했다.
- 무직 상태의 회사 회고, 이사 뒤 고시원 회귀, 현수 방의 소유자 혼동,
  휴일의 아버지 생사 충돌, 마지막 겨울 중복, 다은 동거 장면의 고정
  주거, 직업·주거를 발명하는 장기 회고를 실제 한영 산문과 조건·
  배경·오디오·전환에서 함께 수리했다.
- Godot 54개 스크립트 컴파일과 데모 회귀, 대표 240주 아크·연출·
  오디오, 한영 1,565사건, 컨텍스트·영어 누출·공백 검사를 포함한
  전체 감사가 `✅ 감사 통과`로 끝났다. 휴면 1,123사건과 사람 눈이
  필요한 미술 연속성은 이번 보증 범위가 아니며, 사용자 소유
  `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-26 (Codex — ORDER-55 노출 사건 직업·주거·장소 정합 QA)

### 이사와 퇴사 뒤에도 장면이 과거 상태를 발명하지 않는다

- 실제 도달 가능한 사건을 직업·주거·장소 원장과 교차해 전화·문자·
  사적 회상·생활 콜백을 `current_housing`으로 정렬했다. 이사 뒤
  고시원으로 되돌아가던 장면을 제거하고 임상철 대면 차 장면은 카페로
  고정했다.
- 직업을 선택하지 않은 저장에서도 발화하는 아버지 약 장면은 사무실에서
  대중교통 귀갓길로 옮겼다. 장기 회고의 직장·출근 기정사실은 일·수입·
  일정의 보편 문장으로 바꾸고, 회사 협상 홀덤 사건과 커리어 전문화
  결과는 현재 재직 상태를 다시 확인하도록 잠갔다.
- 사건 JSON, 한영 산문, `story_rules`, 시각·오디오·장면 전환
  매니페스트를 같은 ID로 정렬했다. 정합 감사는 동적 장소와 사건 배경의
  불일치를 빌드 실패로 처리하며 현재 원장 **135/1,565(8.6%)**,
  논리 49, 원격 49, 동적 장소 58, 전환 64, 무단 데모 점프 0이다.
- Godot 데모 회귀와 영어 `chapter2-peaks` 18면 실제 렌더에서 아버지
  약 장면의 지하철, 지연 장면의 비 오는 거리, 무직 회사 사건 차단을
  확인했다. 전체 감사, 한영 1,565사건, 컴파일 54개를 통과했으며 사용자
  소유 `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-26 (Codex — ORDER-54 씨앗-수확 12체인 부활)

### 사소한 선택이 8~12주 뒤 실제 상태와 모순 없이 돌아온다

- 낯선 사람 씨앗 5건의 장면 밀도를 전경 계약까지 높이고 생산자 선택
  14행을 고유 수확 12건에 예약했다. 전경 수확 2건을 빼고 씨앗 5건을
  넣어 전경은 **61 - 2 + 5 = 64건**, 도달 체인은 **0 → 12건**,
  휴면 체인은 **12 → 0건**이 됐다.
- 예약 발화 시 현재 직업·주거 조건을 다시 검사한다. 편의점을 그만둔 뒤
  연예인이 계산대로 돌아오거나, 고시원을 떠난 뒤 옆방 이사가 재현되거나,
  다른 회사 재직 중 인테리어 입사 제안이 강제되는 경로를 막았다. 무효
  예약과 같은 주에 만기인 다음 유효 예약은 삼키지 않는다.
- 계열사 합격은 실제 `job_08`·월 455만원, 편의점에서 이어진 제안은
  `job_01 → job_03`·월 224만원 이직으로 연결했다. 50만원은 현장
  인수인계 수당으로 명시했고 다른 월수입은 이직 뒤에도 보존한다.
- 거절·보류 11분기에 서로 다른 기억 플래그를 남기고 마지막 겨울의
  한영 산문이 실제로 읽게 했다. 격언·회계 결구는 명함, 반찬통, 줄자,
  닫힌 공고문 같은 사물과 행동으로 교체했다.
- 추가 재검수에서 고시원 이웃 씨앗의 거주 조건, 이웃 문자 장면의 현재
  주거 배경, 편의점 이미지에 맞지 않던 마트 벨트·카트 소품을 한영
  동시에 바로잡았다. 새 이직 로그는 일본어 UI 키까지 맞췄다.
- 정적 부채 0, 체인 12/12, 대표 A/B 240주 아크 잼 0, 5전략 발산,
  한영 1,565사건, 일본어 UI 키, Godot 전체 컴파일과 전체 감사를
  통과했다. 사용자 소유 `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-26 (Claude — ORDER-51·52·53 계보 판정: PASS)

### 보고를 믿지 않고 저장소에서 재현해 지연 회수 부활 전 계보를 판정했다

- **엔진 확장 검증**: `DataRegistry.deferred_follow_ups()`가 문자열·문자열배열·`{id, delay}` dict를 정규화하고 빈 id를 무시한다. 사양대로 `add_deferred_event()` 시그니처와 저장 포맷 `deferred_events`는 불변이라 세이브 마이그레이션이 없다. `deferred_follow_up_shape_is_valid()` 형태 검증기까지 추가된 것은 사양 초과 달성이다.
- **하위 호환 실측**: 기존 문자열 배선 112건이 그대로 남고 배열은 T2 3행뿐이다. 문자열 경로를 건드리지 않았다.
- **T2 배선 실측**: 세 선택지 모두 기존 예약(`arc_sangchul_year3`·`arc_jaehyuk_aftermath`, delay 1)을 배열 첫 원소로 보존하고 신규 콜백을 자체 delay(12/12/14) dict로 덧붙였다. 사양의 "대체하지 말 것"을 정확히 지켰다.
- **감사 확장 검증**: `tools/audit.py`가 인라인 검사를 공유 모듈 `event_schedule.deferred_follow_ups`로 승격하고 `DeferredFollowUpError` 형태 오류·존재성·체인 추적(1053/1062)을 배열 전수로 처리한다. 사문 재발 경로가 닫혔다.
- **게이트 실행**: `audit.py` ERROR 0/WARNING 0, write-only 플래그 0, inert 0. `event_director_audit.py`는 `callback_reachable=56`(24→53→56), 휴면 `callback=564`(596에서 감소), `chain=0/12`.
- **판정: ORDER-51·52·53 PASS.** 각본 리뷰 3차에서 발견한 "도달 불가 회수 코퍼스"가 유저 결정 (나) 선별 부활로 닫혔다.
- **남은 열린 항목**: `chain_events.json` 12체인은 여전히 `chain_reachable=0`이다. 부활 선별이 작성형 생산자를 가진 콜백만 대상으로 했고, 체인의 씨앗은 `rare_encounter_events.json`의 랜덤 조우라 예약 배선 대상이 아니었기 때문이다. 감사가 `dormant ... chain=12 pending design`으로 정직하게 경고 중이며, 되살리려면 씨앗 5~6건의 허용목록 편입(소규모 (다) 방식)이 필요하므로 별도 유저 판단 사안으로 남긴다.

## 2026-07-26 (Codex — ORDER-53 다중 지연 예약·T2 3행 부활)

### 한 선택이 가까운 후속과 늦은 대가를 모두 잃지 않고 기억한다

- `deferred_follow_up`을 기존 문자열 또는 문자열/`{id, delay}` 혼합
  배열로 읽는 공용 정규화기를 Godot와 Python 감사기에 추가했다. 현재
  콘텐츠의 **문자열 112행**은 그대로 호환되고 새 배열은 3행뿐이다.
  빈 id 무시, 정수 delay, 대상 존재, 모드 팩 경계와 모든 체인 소비자를
  같은 계약으로 검증한다.
- 재혁 갈취/공모와 상철 죄책감 이용 선택은 기존 1주 후속을 보존하면서
  12~14주 뒤 콜백도 예약한다. 런타임에서 두 예약이 순서대로 한 번씩
  발화하고 서로 삼키지 않으며, 같은 id 중복 예약은 더 이른 주만 남고
  저장 포맷 `deferred_events[{event_id, trigger_turn}]`은 바뀌지 않았다.
- 재혁 전화는 현재 거처의 원격 초상과 통화 배지, 공모 정산은 회의실
  대면, 상철 회수는 카페의 내면 장면으로 명시했다. 재혁의 -300만원,
  +1,500만원/+800만원 효과와 한영 결과 산문도 일치시켰다.
- 도달 콜백은 **53→56**, 휴면 콜백은 **567→564**, 디렉터 랜덤은
  **1,017→1,015**가 됐다. 한국어·영어 지연 회수 스위트 각 28장,
  장면·오디오·시각 계약, 대표 A/B 240주, 세이브 왕복과 전체 감사를
  통과했다. 사용자 소유 `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-26 (Codex — ORDER-52 T1 지연 회수 선별 부활)

### 과거 선택은 랜덤 카드가 아니라 8~16주 뒤의 작성형 결과로 돌아온다

- 생산자 선택 34행을 고유 콜백 29건에 직접 예약했다. 기존 랜덤
  허용목록은 넓히지 않았고 `GameState.apply_choice()`로 대상과 발화
  주차를 전부 검증했다. 도달 가능한 콜백은 24→53건, 휴면 콜백은
  596→567건이 됐다.
- 조건을 재검사하지 않는 예약 경로에 맞춰 25건의 산문·시점·장소·효과를
  한영으로 수리했다. 아버지 약 복용은 현재 집에서 보는 과거 사진,
  회식은 이직 뒤에도 성립하는 메시지, 지연 계약은 승패와 무관한
  부대비용으로 정렬했다. 카페 정보 사건은 과거 승패를 사후 선택하던
  구조를 없애고 출처를 기록하거나 숨기는 현재 선택으로 바꿨다.
- 대표 A/B 240주에서 회수 6건/9건, 중복 0, 상한 아크 생존, 아크 잼
  0을 확인했다. 작성형 회수가 주를 차지해 비정석 경로의 랜덤 전경 창
  하한은 24→22건으로 조정했지만 장별 최소 1건은 유지됐다.
- 1280×800 한국어·영어 대표 11건의 소개/결과 44장을 렌더했고 돈·
  정신력 효과와 결과 문단 진행도 함께 검사했다. 오디오·장면 카탈로그,
  한영 1,565개, 전체런 연출, 장기 밸런스와 Godot 54개 스크립트
  컴파일을 재검증했다.
- T2의 이미 예약 슬롯을 쓰는 3행과 `GameState.gd` 배열 확장은 사용자
  승인 대기라 수정하지 않았다. 사용자 소유 `project.godot`도 수정·
  스테이징하지 않았다.

## 2026-07-26 (Codex — ORDER-51 도달 콜백 수리·사문 회귀 게이트)

### 실제 보이는 결함만 먼저 고치고 잠든 코퍼스는 별도 작업으로 격리했다

- 도달 가능한 콜백 24건 중 `callback_jeonse_scam_narrow`,
  `callback_recycling_neighbor`, `callback_formal_complaint_filed_echo`를
  한영 동시에 수리했다. 전세 손실은 실제 500만원 차감으로 맞추고
  `BALANCE.md`에 근거를 남겼으며, 설교 결구는 행동으로, 신고 회수는
  생산자 이후 80주와 현재 주거 배경으로 정렬했다.
- 디렉터 감사가 전경·다리·명시/예약 후속·아크 스케줄러와 플래그
  생산자를 따라 실제 도달성을 계산한다. 콜백 24/620, 체인 0/12,
  휴면 596+12를 기준선으로 고정해 신규 사문이 늘면 실패한다.
- 세 사건의 본문·결과를 한영 1280x800로 각각 6면씩 렌더하고 실제
  돈·정신력 효과까지 검증했다. 영어 한글 누출, 밸런스 5개 밴드,
  전략 발산, 사건 디렉터·몰입 루프와 Godot 54개 스크립트 컴파일을
  통과했다.
- 유저가 선택한 32건 선별 부활은 `ORDER-52`로 분리했다. 사용자 소유
  `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-26 (Claude — 지연 회수 엔진 정독·ORDER-51 발행)

### 각본 리뷰의 마지막 미검토 표면에서 도달 불가 코퍼스를 찾았다

- gap감사가 지목한 2축(`chain_events.json` 12체인 + 비인물 콜백)을 4갈래 병렬 정독했다. 리뷰어 주장을 믿지 않고 **저장소에서 직접 재현**했다.
- 검증된 사실: 콜백 **620건 중 도달 가능 24건(3.9%)**, chain 체인 **0/12**. 유일 경로는 `EventManager.draw_situations()` → `is_foreground_random_event()` 허용목록(전경 61 + 다리 18)이고, **대체 경로 0건**(전 이벤트의 `follow_up_event`/`deferred_follow_up`/`next_event` 중 `callback_`·`chain_` 포인터 0, 아크 스케줄러 반환 0). chain은 허용목록 2건조차 씨앗이 목록 밖이라 연쇄 사망.
- 맥락: 이 상태는 `ORDER-37` 콘텐츠 다이어트의 결과이며 사고가 아니다. 다만 다이어트 의도는 무관한 단문의 전경 점유 차단이었지 회수 층 폐쇄가 아니었다. 주간 Echo는 콜백이 아니라 행동 원장(`_demo_director_recent_action_record`)을 읽으므로, 회수 약속의 일부는 Echo·dik가 이미 수행 중이다.
- **우선순위 분류:** 리뷰가 지적한 결함 15건 중 **플레이어가 실제로 보는 것은 3건뿐**(`callback_jeonse_scam_narrow` 손실 10배 불일치, `callback_recycling_neighbor` 잔존 설교, `callback_formal_complaint_filed_echo` min_turn 사문). 나머지 12건은 죽은 콘텐츠 안이라 처분 결정 전 착수 금지로 묶었다.
- ORDER-51 발행: A(도달분 3건 수리)·C(사문 회귀 게이트)는 즉시 실행, **B(596콜백+12체인 처분)는 ⚠유저 설계 판단 대기**. Claude 추천은 (나) 선별 부활 — 허용목록 확대가 아니라 생산자 이벤트에서 `deferred_follow_up`으로 직접 예약해 다이어트 정본을 깨지 않는 방식.

## 2026-07-26 (Codex — 활성 큐 자동 범위 전수 마감·clean RC)

### 기계가 판정할 수 있는 일은 모두 끝내고 사람 판정을 분리했다

- 활성 인덱스의 `ORDER-43`, `USER-P0N`, `ORDER-21`, `ORDER-23`,
  `ORDER-22`, `ORDER-28`, `ORDER-26`을 순서대로 다시 읽고 자동 게이트를
  전수 확인했다. 미착수 자동 작업은 0이며 각 항목은 사람 판정 때문에만
  `[~]`로 남는다.
- 전체 감사는 Godot 54개 스크립트 컴파일, 한영 1,565사건, 오디오·장면
  연속성, 240주·엔딩·입력 회귀까지 `✅ 감사 통과`로 끝났다. 영어 누출,
  컨텍스트 정본, diff 공백 검사도 통과했다.
- 사용자 소유 `project.godot`이 변경된 작업 폴더와 분리해 clean revision
  `d73afa6b2ea5da4d35511d5ed5e02e4ec57d7d7e`, tree
  `4acc598a9f9d64e6c638e1d45150cf7f4c397810`에서 fresh import와 데모 계약,
  Windows, macOS, Linux/Steam Deck export를 통과했다.
- 최신 로컬 RC의 매니페스트 SHA-256은
  `e0cb837ababe5baa14f398473a1bfa8b309e19054061cb89e380b9fd416c0752`이며
  세 산출물 체크섬을 복사 뒤 다시 검산했다. 임시 워크트리는 제거했다.
- 남은 게이트는 정상 속도 전체 플레이, 장별 헤드폰·노트북·TV 연속 청취,
  물리 Steam Deck/DualSense/Switch Pro, 외부 10인 독해·재미, 데모 GO 뒤
  일본어 본문 번역·15장 캡처·원어민 검수다. 자동 통과로 대체하지 않는다.

## 2026-07-26 (Codex — USER-P0N 240주 장면 연출 자동 확산)

### 화면과 소리가 같은 장면 경계를 읽는다

- 사건 1,565개, 노출 경계 166개, 배경 91종, 활동 7종, 엔딩 35종을
  `scene_direction_manifest.json`에 분류했다. 런타임의 산문·해시 추론과
  기본 와이프를 제거하고 미검토 대상은 `none`으로 실패 닫힘 처리했다.
- 같은 장소, 시간 경과, 명시 이동, 회상, 원격 통신, 활동 진입·복귀,
  피날레를 서로 다른 계약으로 연결했다. Moral은 전환 사실을 바꾸지 않고
  사람층·공간 깊이·잔상·재질의 지각 강도만 바꾼다.
- 정석/사람 중심과 Black-risk 두 구조를 한영으로 총 960주 정적 추적했고,
  실제 KO PlayStation/EN Xbox 런은 각각 240주·218사건·`with_daeun`
  엔딩을 패드만으로 완주했다. 미분류 전환·키보드·마우스 구조는 0이었다.
- 960x600, 720p, Deck 1280x800, 900p, 1080p, QHD, 논리 4K,
  3440x1440을 통과했다. 정상 속도 체감·물리 Deck 성능·장별 A/V 청취는
  사람 게이트로 남겼고 사용자 소유 `project.godot`은 건드리지 않았다.

## 2026-07-26 (Codex — ORDER-43 본편 오디오 자동 확산 P0)

### 소리는 번역 문구가 아니라 실제 보이는 장소와 사건 계약을 따른다

- 한영 사건 1,565개를 사건 계약 225, CG 상속 6, 렌더 배경 프로필 1,334로
  전수 분류하고 이미지 레지스트리 배경 91종의 장소 프로필을 잠갔다. 신규·
  삭제 사건이나 배경은 의도 검토 전 전체 감사를 통과할 수 없다.
- 런타임의 제목·본문·태그·카테고리 키워드 추론과 보편적 고시원 방 폴백을
  제거했다. 현재 주거와 현재 직장은 실제 상태로 해석하며 미검토 배경은
  엉뚱한 장소음을 내는 대신 경고와 무음으로 실패한다.
- 정석/사람 중심과 비정석/Black 두 대표 경로를 한영 동일 구조로 240주씩
  추적했다. 총 960주에서 5개 장의 작성형 장면·연결부·음악 진입, 활동 7종,
  엔딩 2종의 소유권과 패리티를 검증했다.
- 오디오 139개는 전부 실제 녹음·실악기 샘플이며 합성 원음은 0이다. 전체
  감사는 한영 1,565개, 장면·게임·Moral·BGM 연속성과 Godot 54개 스크립트
  컴파일까지 `✅ 감사 통과`로 끝났다.
- 자동 추적은 사람 청취가 아니다. clean 3플랫폼 RC와 헤드폰·노트북·
  거실 TV 장별 연속 청취는 열린 게이트로 유지한다. 사용자 소유
  `project.godot`은 수정·스테이징하지 않았다.
- clean revision `e849a6af2aed4aa1c7fc5a7785f59ac1b7ac952d`, tree
  `95a0674b05987efd62558f7aab09a64df0056042`에서 fresh import·데모 계약,
  Windows/macOS/Linux·Steam Deck export와 macOS 격리 HOME 부팅을 통과했다.
  매니페스트 SHA-256은 `26287c8124bb0838dbe2062f5d8072d819b164569162af773c091ad28b644cab`이며
  세 산출물은 로컬 복사 뒤 다시 검산했다.

## 2026-07-26 (Codex — ORDER-50 동기 회수·작성형 산문 P1)

### 첫 줄의 이유는 숫자에 덮이지 않고 마지막 해까지 같은 사실로 남는다

- 프롤로그의 가족·증명·생존 동기를 공용 `GameState` 독자로 올리고
  `{notebook_motive}` 사건 토큰을 추가했다. 2개월 결산과 1년 결산의
  기본·조건부 문안 모두 실제 선택한 문장을 첫 줄로 되짚는다.
- 면접·SNS·투자 안내·루틴의 추상 격언을 난간 페인트, 휴대폰 액정,
  닳은 명함, 지하철 문, 영수증 같은 감각 사물과 동작으로 바꿨다.
  영어 루틴 선택지의 뒤집힌 순서도 한국어 효과와 일치시켰다.
- 마지막 해 선언 뒤 12주 점검을 별도 창으로 분리하고, 6개월
  카운트다운과 겹치지 않게 했다. 다은 출발선 회고는 5장·20억 이정표
  이후에만, 30억 전야 저녁은 29억원 이상에서만 열린다. 연애 전 지연의
  `오빠/oppa`는 `민준 씨/Minjun`으로 정렬했다.
- 동기 3종 × 결산 2종을 한·영 1280×800에서 렌더했다. 조건부 결산
  문안 48면, 후반 트리거, 격언 재유입을 자동 검사로 잠갔다.
- 대표 240주 두 경로의 잼 0, 직접 결정 52회와 장별 13/9/10/10/10,
  고립 단편 기준선, 한영 1,565개, 정점 체인 32개, 밸런스·엔딩 수렴,
  Godot 54개 스크립트 컴파일을 검증했다.
- 사용자 소유 `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-26 (Codex — ORDER-49 선택의 고민 장부화 P2)

### 사랑과 유혹은 문장만이 아니라 실제 후속과 결산에 남는다

- 다은 최종 배신 선택의 허위 자산 이득 문구를 제거했다. 결혼 준비의 작은
  예식과 풀패키지는 어느 쪽도 도덕적 정답으로 서술하지 않고, 각각 줄어든
  관계의 상처와 가족 사진으로 남는 실제 기억을 함께 보여 준다.
- `daeun_married + used_daeun_as_means` 상태에서 30억에 먼저 도달해도
  다은 최종 대면을 건너뛰지 않는다. 기존 최종 선택이 끝난 뒤에만 엔딩
  캐스케이드가 다시 열리며 실제 `game_over` 신호 회귀 검사로 잠갔다.
- 다은 이별 변주 두 개를 3년차 후일담에 연결했다. 재혁의 300만원 판돈은
  “거의 전부”라 부르지 않으며, 보증 서명은 기존 협박 후폭풍과 재혁 엔딩
  기억이 읽는다. 아버지 소개인 단서는 상철의 t104 추리 전에 열릴 수 있다.
- 단일 선택 연결 장면에서 화면 QA가 다음 사건까지 과진행하던 결함을
  수리하고, 요청한 사건과 실제 열린 사건 ID가 다르면 즉시 실패하도록 했다.
- 한국어·영어 결별 56면과 약혼·결혼 76면, 정점 체인 32개, 대표 아크
  2경로, 엔딩 구분성 35종·실제 라우팅 12종, 밸런스·전략 발산,
  한영 1,565개와 Godot 54개 스크립트를 검증했다. 전체 감사는
  `✅ 감사 통과`였고 사용자 소유 `project.godot`은 제외했다.
- 결혼 비용·재혁 판돈 수치는 외부 표본 근거 없이 바꾸지 않았다. 이후
  플레이테스트가 해당 선택의 무망설임을 입증할 때만 별도 밸런스 오더로
  재개한다.

## 2026-07-26 (Codex — ORDER-48 정점 산문·엔딩 기억 P1)

### 큰 선택과 마지막 장면은 요약문이 아니라 실제 기억으로 남는다

- 다은의 조기 연애 확정은 계산기 불빛·멈춘 손·붉어진 눈가와 존댓말의
  박자로 늘렸고, 이혼은 계약서·도장·식은 차와 담판의 침묵으로 벌렸다.
  초기 선택지의 괄호형 관계 결과 예고는 한영 모두 제거했다.
- White 엔딩은 “아무도 밟지 않았다”는 검증 불가능한 선언 대신 이삿짐
  상자·오래된 수첩·아버지 약속·상철과의 거리·깨끗한 손을 실제 경로
  기억으로 회수한다. `empty_house`, `mental_break`, `career_burnout`도
  아버지·다은·현수·직업 선택에 따라 다른 후일담을 갖는다.
- 안정·정석·투자·비정석·균형·조기 은퇴 결산은 서로 다른 사물과 하루의
  동작으로 분리했다. 34개 `description_if_known`은 본문 복제가 아닌
  짧은 코다로 축약했고 핵심 엔딩 9종에 실제 선택 기억을 연결했다.
- 회귀 감사가 White 시각 앵커와 허위 무결 문장, 필수 기억 키, 코다 길이,
  한영 키 패리티를 잠근다. 다은 확정 3면·이혼 4면·엔딩 14면을 한국어와
  영어 1280×800에서 렌더해 문단 분할·잘림·버튼/CG 충돌을 확인했다.
- 사건 1,565개 정합·호칭, 정점 체인 32개, 엔딩 35종 구분성, 영문
  커버리지, Godot 54개 스크립트 컴파일을 포함한 전체 감사가
  `✅ 감사 통과`로 끝났다. 사용자 소유 `project.godot`은 수정 범위와
  커밋에서 제외한다.

## 2026-07-26 (Codex — USER-P0S 실플레이 가독성·인과·경마 P0)

### 한 사건의 글·조건·역할·장소·소리가 같은 사실을 말한다

- StoryMode의 긴 한영 본문과 결과를 실제 폰트·창 높이·사용자 글자
  크기로 계측해 여러 페이지로 안전 분할했다. 크기나 언어를 바꿔도 같은
  원문 위치를 유지하며, 추가 페이지는 배경·초상·CG·오디오 큐를 반복
  재생하지 않는다.
- 결과창의 정확한 호감도 수치와 관계 등급 해설을 제거했다. 선택의 의미는
  사람의 반응과 후속 장면으로 읽히며 숨은 시스템을 공략표로 만들지 않는다.
- `story_rules.json`의 사건 ID에 실행 가능한 `all/any` 선행조건, 현장
  참여자 역할, 기대 배경·초상·앰비언스를 결합했다. 일반 평가기는
  직업·숙련·주차·플래그 조건을 읽고 감사기는 한영 사건과 A/V 계약을
  교차 검증한다.
- 회사 팀장 사건은 실제 사무직만 통과한다. 지연 재회는 편의점에서 신촌역
  버스정류장으로 옮겨 민준=승객, 지연=운전자 역할을 고정했다. 청담
  한식당은 전용 식당 배경을 쓰고 커피를 받거나 솔직한 대화를 이어 간
  경로만 열린다.
- 경마 첫 체험의 가짜 고정 손실을 제거하고 실제 경마 미니게임으로
  넘겼다. 결과 화면은 좌우/Tab·확인·취소로 다음 경주와 나가기를 고르며,
  주간 결산은 카지노가 아니라 경마장 장소와 행동 원장을 유지한다.
- 추상적인 주간 문구를 구체적인 한국어·영어로 낮추고 영어 `파장` 표면은
  `WAVE`로 정렬했다. 일본어 UI 키도 같은 변경만 동기화했다.
- `StoryPlaybackCheck`, `CoreChoiceSliceCheck`, `InputMatrixCheck`,
  `ImmersionLoopCheck`, 사건 정합·시각·오디오 계약, 두 240주 대표
  경로와 전체 `audit.sh`를 통과했다. 54개 GDScript 강제 컴파일도
  깨끗했다.
- 사용자·Claude 소유 `project.godot`, 엔딩·다은 아크·자산 요청 문서와
  진행 중인 `ORDER-48` 변경은 이 커밋에서 제외한다.

## 2026-07-25 (Codex — USER-P0R 고시원 공용 주방 장소 P0)

### 현수와 라면을 끓이는 새벽은 민준의 개인실이 아니다

- `hyunsu_study_together`가 "고시원 공용 주방"을 명시하면서
  `goshiwon_room`을 고정해 침대와 개인 책상을 보여 주던 원인을
  재현했다.
- 같은 신촌 고시원의 낡은 벽·형광등·한밤 색온도를 잇는 좁은 공용
  주방을 전용 배경으로 제작했다. 싱크대·건조대·2구 조리기·선반·
  공용 냉장고를 물리적으로 연결하고 사람·침대·개인 책상·상표·판독
  문자는 배제했다.
- 사건 산문·선택·효과와 기존 고시원 룸톤·주전자·컵 폴리는 유지하고,
  사건 배경과 이미지 레지스트리·시각 계약만
  `goshiwon_shared_kitchen`으로 정렬했다.
- 1280×800 한국어 StoryMode에서 현수 초상·하단 대화창을 실제 합성해
  장소 동사와 원근이 읽히는 것을 확인했다. 사건별 회귀 계약은 이 장면이
  다시 개인실로 돌아가면 실패한다.
- 모드 매니페스트 244장, 시각 계약 236건, 해상도 기준선, 장면 오디오,
  한영 표면과 Godot 전체 감사를 검증했다. 사용자 소유
  `project.godot`과 진행 중인 `ORDER-48` 파일은 포함하지 않았다.

## 2026-07-25 (Codex — USER-P0Q 상철 사무실 오발 기침 P0)

### 두 사람뿐인 사설 사무실에 화면 밖 제3자의 기침이 들리지 않는다

- `office` 장소음이 공용 `public_interior` 사람층을 자동 호출했고, 그 녹음의
  14.7초 지점에 있던 실제 기침이 상철과 민준의 대화에 섞이는 원인을
  재현했다.
- 상철 첫 만남 4장면에만 `suppress_human_ambience` 계약을 적용했다.
  `amb_office_room.wav`는 장면 사이에서 이어지고 빗길 차량·종이컵·종이
  폴리와 무음악 연출도 그대로 유지한다.
- 이미 재생 중인 사람층이 페이드 중 기침을 흘리지 않도록 명시적 억제는
  해당 플레이어를 즉시 정지한다. 다른 사무실·학교·공공청사의 사람층은
  바꾸지 않았다.
- 장면 계약·BGM 연속성·오디오 자산 139개·녹음/샘플 출처·한영 표면과
  Godot 54개 스크립트 컴파일을 포함한 전체 감사가 `✅ 감사 통과`로 끝났다.
- 사용자 소유 `project.godot`과 진행 중인 `ORDER-48` 파일은 수정·
  스테이징하지 않았다.

## 2026-07-25 (Claude — 각본 리뷰 gapfill·ORDER-50 발행)

### 리뷰 미완 4조각을 채우고 잔존 신규 발견을 오더로 묶었다

- 세션 한도로 미뤘던 민준 목소리 스레드·2장·5장·gap감사를 소형 4에이전트 워크플로로 채웠다(44개 재시도 없이 표적 실행).
- 판정: 민준의 '몸'(33세·6년 빚·50만원)은 전 루트 일관하고 무릎 회상 회수는 육화의 모범. 2장은 확장의 장 정체성 뚜렷. **5장은 데드존이 아니라 의도된 하강**(뿌리는 줄지만 dik·회수로 두꺼워짐). gap감사는 그물 밖 8표면 확인 — 대부분 양질이나 chain_events·비인물 콜백 419건 미검토.
- **잔존 신규 발견 → ORDER-50 발행:** A(high) 프롤로그 동기 문장이 `arc_chapter1_close`·`arc_year1_close` 회고에서 '30억. 5년.' 숫자로 덮여 ORDER-23 동기 각인과 정면 모순(ORDER-47 이후에도 실파일 잔존 확인) B(medium) 작성형 루트 변주의 자기계발 설교 미끄러짐 C(medium) 5장 삼중 유혹 중복 D 다은/지연 잔존은 ORDER-48 범위에서 검증.
- Codex ORDER-48 WIP 커밋 후 트리가 정리된 창에서 인덱스 배선. `SCRIPT_REVIEW`는 ORDER-48 파일 범위라 건드리지 않고 발견을 ORDER-50 자기완결형으로 담았다. 커밋은 Codex WIP를 피해 큐/문서만.

## 2026-07-25 (Codex — USER-P0O/P0P 퍼블리셔·인물 연기 P0)

### 첫 화면은 승인된 브랜드로 시작하고, 피곤한 민준은 아픈 사람처럼 보이지 않는다

- 사용자 승인 1024×1024 RGBA `JUNPAC GAMES` PNG를 퍼블리셔 정본으로
  등록했다. 순백 전면 배경과 로고가 함께 사라진 뒤에만 강남드림의 어두운
  타이틀 필름이 시작되며, 과거 JPEG 보드와 코드 네이티브 초승달은 제거했다.
- 1280×800과 3840×2160 실제 프레임에서 흰 배경, 중앙 정렬, 로고 비율과
  안전 영역을 확인했다. 첫 30초 런타임 검사와 키아트 자산 감사도 새 PNG만
  허용하도록 잠갔다.
- 민준 피로 초상은 같은 33세 얼굴·검은 크루넥을 유지하면서 두 눈을 분명히
  뜬 하향 시선, 병색 없는 눈 밑 그림자, 척추는 세우고 어깨만 이완한 연기로
  교체했다. StoryMode 합성과 27개 크롭 회귀 화면에서 정체성과 하단 크롭을
  육안 확인했다.
- 인물마다 자세가 같아 보이지 않도록 민준·다은·지연·현수·재혁·상철·아버지의
  자세 실루엣을 정본화했다. 감정은 구부정한 상체 하나가 아니라 시선·턱·손·
  호흡·체중 이동으로 먼저 표현한다.
- 첫 30초·키아트·활성 이미지 243장·한영 표면·입력·저장·오디오·엔딩과
  Godot 54개 스크립트 컴파일을 포함한 전체 감사가 `✅ 감사 통과`로 끝났다.
- 사용자 소유 변경 `project.godot`과 진행 중인 `ORDER-48` 파일은 이 완료
  범위에 포함하지 않았다.

## 2026-07-25 (Codex — ORDER-47 편성·구조 P1)

### 240주의 장면이 같은 말을 연달아 하지 않고, 장 경계가 실제 달력에 선다

- 1장 환불선에 t35 직접 선택과 t33 Echo를 배치하고 2장 t87 직접 주를
  이동했다. 직접 결정 52회는 유지하며 장별 밀도를 13/9/10/10/10으로,
  Echo를 21회로 잠갔다.
- 1장 결산은 대포통장 선택을 상태별로 회수하고 현재 주거를 고시원으로
  오인하지 않는다. 보스 주를 실제 12월 1주차인 t45로 맞췄으며 2·4장
  결산의 보편 상태 변주와 결과문도 장 질문을 이어받게 보강했다.
- 3장 말미의 계산기·장부 장면을 4장으로 넘겨 22주·3주·6주·12주 간격으로
  분리했다. 2장 비교 장면 창, 4장 회고 창, 5장 고자산 공백과
  “1년 남았다” 연속 발화도 재배치했다.
- 대표 A/B 경로 아크 잼 0, 추정 223~246분, 장별 무작위 창 최소 1개,
  한영 1,565개, 비주얼 계약, 경제·종결 밴드, 실제 Godot 55개 스크립트
  컴파일을 포함한 전체 감사가 `✅ 감사 통과`로 끝났다.
- 사용자 소유 변경 `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-25 (Codex — ORDER-46 랜덤 풀 위생 P1)

### 짧은 사건이 보상 출처를 발명하거나 플레이어를 훈계하지 않는다

- 결과문에 수입 근거가 없던 직업·정치·코미디·정보·레버리지 사건의 단발
  금전 효과 5건을 제거했다. 프로필 확인에 투자감각이 오르던 모순, 앱 설치
  전 수익 확정, FOMO 금액 10배 오차와 국회의원 즉시 당선 스케일도 한영
  산문·효과·종결 경로에 맞춰 수리했다.
- 초기 에코 콜백 400개에서 마지막 격언을 걷어내 행동과 대화가 장면을
  닫게 했다. 후기 콜백 162개의 두 번째 분기는 자동 칭찬을 제거해 유보·
  비용이 남도록 했고, 대표 첫 분기 3건도 자기계발 결구를 낮췄다.
- 파손 문장과 사전형 선택 라벨을 교정하고,
  `random_pool_hygiene_audit.py`를 전체 감사에 연결했다. 이 게이트는
  한영 핵심 계약, 사건 수, 초기·후기 콜백 줄 예산을 함께 잠근다.
- 장기 밸런스 밴드와 5전략 종결 다양성, 이벤트 디렉터, 한영 커버리지,
  한글 누출, JSON, GDScript 전체 컴파일을 재검증했다.
- 사용자 소유 변경 `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-25 (Codex — ORDER-45 호칭·스피치 정본 P0)

### 관계의 시간이 말투에서 되돌아가지 않는다

- 한지연은 연애 확정 전 존댓말, 확정 후 “오빠”+반말로 거래·재회·데이트·결혼·
  엔딩을 정렬했다. 김다은→김민준 “민준씨”+존댓말, 민준→두 여성·임상철
  존댓말 기조도 콜백과 정점까지 전수 교정했다.
- 한국어 필드에 “오빠”가 있는 지연 대사는 같은 영어 필드에 `oppa`를 갖도록
  맞췄다. `arc_jiyeon_05_epilogue`의 의미 오역과 데이트 문장부호도 함께 수리했다.
- `speech_register_audit.py`를 전체 감사에 연결해 사건 29계약·엔딩 4계약과
  KR↔EN 동일 필드 호칭을 자동 검사한다. 한영 커버리지·한글 누출·정점 32개·
  GDScript 55개 컴파일을 포함한 전체 감사를 통과했다.
- `home-peaks`, `ending-p0/p1`, `namsan`, `first-snow`, `amusement` 영어 실렌더를
  확인했다. 한국어 `breakup` QA에서는 선택 전 이미 종결 상태가 서는 기존 진행
  부채를 별도 발견했으며, 이번 텍스트 변경의 회귀로 판정하지 않고 구조 수리 대상으로 남겼다.
- 사용자 소유 변경 `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-25 (Codex — ORDER-44 정합 위생 P0)

### 외부 테스트에서 즉시 신뢰를 깨는 정합 오류 18군을 닫았다

- 아버지 사망 후 `gangnam_dream`·`late_call`과 현재 시점의 생활·관계·전화
  사건에서 다시 등장하던 경로를 차단했다. 30억 달성 뒤 아버지가 사망한 런은
  `empty_house`로 귀결되며, 회복된 관계 플래그만으로 고인을 호출하지 않는다.
- 한지연·최재혁 성씨를 정본에 맞추고, 현수는 `STORY_BIBLE`의 회계 직무 궤적으로
  통일했다. 다은 후속 두 장면, 현수 잔존 경로, 상철 진실·NG+·첫 만남 경합을
  실제 생산 플래그와 발화 창에 맞췄다.
- 도달 불가였던 `startup_exit` 엔딩 변주를 실제 소유 엔딩으로 옮기고,
  `jiyeon_left` 이후 “한지연과 함께” 오발화를 막았다. 38세 마감, 6년 공백,
  보증 사기 연대, 강남 첫 기억과 서울 첫해 시제를 현재 정본으로 정렬했다.
- `EndingRouteIdentityCheck`를 11개 경로로 확장하고 정점 체인·엔딩 구분 감사
  계약을 새 라우팅에 맞췄다. 한영 JSON·커버리지·한글 누출·아크 흐름·스토리
  정합·문서 매니페스트와 전체 감사로 회귀를 확인했다.
- 사용자 소유 변경 `project.godot`은 수정·스테이징하지 않았다.

## 2026-07-24 (Claude — 전체 각본 리뷰·ORDER-44~49 발행)

### 유저 요청으로 전 게임 스토리·각본을 정독 판정하고 오더로 묶었다

- 유저 7기준(지루함·로맨스·민준 몰입·반전·추리·기승전결·선택 고민) + 챕터 4질문(구분·흐름·장별 기승전결·분량)을 워크플로로 스레드별 전담 정독했다. 실제 이벤트 산문 전문 ~680사건을 읽고 채점·본문 인용·적대 검증했다.
- 완료: 9스레드(상철·재혁·다은·지연·아버지·현수·엔딩·랜덤풀·편성) + 1·3·4장 심층 + 5장 경계. 미완(세션 한도 반복): 민준 목소리 스레드·2·5장 심층·gap감사 — 8:10 이후 워크플로 resume로 부록 예정.
- 판정 요약: 강점=아버지 스레드(4.0)·상철/재혁 반전(4)·정점 산문·장 경계 3→4. 최대 약점=선택의 고민(다은 2/랜덤풀 2, 사랑·정직이 수치 상위호환 — 데모 R1과 동일 축) + 플레이 도달 확정 버그 다수.
- 근거 원장 `docs/SCRIPT_REVIEW_2026-07-24.md` 생성. 오더 6종 발행:
  - **ORDER-44 [P0]** 정합 위생 — 죽은 아버지 재등장(gangnam_dream·late_call 가드 누락)·이지연/이재혁 오기·현수 요리↔회계·다은 money_gap/trace 도달불가·상철 진실 증발·시제 동요.
  - **ORDER-45 [P0]** 호칭 정본 붕괴 전수(지연·다은·민준→상철 반말 이탈).
  - **ORDER-46 [P1]** 랜덤 풀 위생(근거 없는 금전·산문/효과 오차·파손 문장·설교투 격언).
  - **ORDER-47 [P1]** 편성/구조(1장 보스 결산·환불선 t25~48 밀도·중반 계산장면 6연속·4장 함몰·4→5 '1년 남았다' 3연속).
  - **ORDER-48 [P1]** 정점 산문 밀도 역전(조기 고백·이혼 상실 압축·S+ 엔딩 무변주·결산 6종 템플릿).
  - **ORDER-49 [⚠P2·밸런스/유저 승인]** 선택의 고민 장부화 — 산문 정렬(무수치)만 선제, 수치 변경은 외부 표본 데이터+유저 승인 후.
- 전부 콘텐츠 동결 준수(신규 사건 0, 문장 수리·플래그 연결·창 재배치·dik 재배치). 커밋은 Codex WIP를 피해 리뷰/오더/인덱스 문서만.
