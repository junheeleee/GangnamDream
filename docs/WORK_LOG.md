# Gangnam Dream Work Log

> 최신 작업만 이 파일에 역순으로 기록한다. 2026-05-16부터 2026-07-24 USER-P0M까지의 원문은
> [`history/WORK_LOG_2026-05-16_to_2026-07-24.md`](history/WORK_LOG_2026-05-16_to_2026-07-24.md)에 손실 없이 보존했다.
> 2026-07-30의 오래된 V2 C/D 항목과 2026-07-29 이전 항목은 날짜별 보관본인
> [`history/WORK_LOG_2026-07-30.md`](history/WORK_LOG_2026-07-30.md),
> [`history/WORK_LOG_2026-07-29.md`](history/WORK_LOG_2026-07-29.md),
> [`history/WORK_LOG_2026-07-27.md`](history/WORK_LOG_2026-07-27.md)와
> [`history/WORK_LOG_2026-07-26.md`](history/WORK_LOG_2026-07-26.md),
> [`history/WORK_LOG_2026-07-25.md`](history/WORK_LOG_2026-07-25.md)로
> 차례로 옮긴다.
> 과거 근거는 기본 컨텍스트에 넣지 말고 먼저 `rg -n "<키워드>" docs/history/`로 찾는다.

## 2026-08-04 (Codex — V2 타이틀→24주 계측형 GUI raw-event 자동 완주)

- 언어를 미리 지정한 타이틀에서 오프닝·프롤로그·Chapter 1·실제 계획판과 상태를
  바꾸지 않는 3장 튜토리얼을 지나, 네 약속과 두 루틴을 정하는 계획을 여섯 번
  화면에서 확정해 1~24주를 완주했다. 첫 청구서 opening→decision→ledger와
  현수 시험 아침 뒤 turn 25 자동 저장은 정확히 한 번 생성됐고, 완료 CTA는
  저장 바이트를 바꾸지 않은 채 타이틀과 V2 진입점을 다시 열었다.
- KO 경로는 PlayStation 배열 South joypad event만, EN 경로는 key event만
  자동화에서 주입했다.
  일반 버튼의 South 확인 매핑을 제품 InputMap에 복구하고 계획판의 중복 수동
  발화를 제거했다. 하네스가 주입한 마우스·타 장치·의미 action·미상 입력은
  0이며 일정·선택·GameState 직접 조립이나 `pressed.emit()`을 완주 증거로
  쓰지 않았다.
- 로컬 실행기와 CI에 등록한 같은 실행기가 설정·화면·메타·자동 저장·열 슬롯
  14개 경로를 매번 새 절대 임시 루트에 격리한다. 자동 PASS는 제품 화면
  도달성·결과·원장·저장을
  증명하지만 D-pad·방향키 포커스 연결은 별도 자동 회귀, Steam Deck·실물 패드
  손맛과 정상 속도 몰입·재미·연속 A/V는 사람 게이트로 남긴다.

## 2026-08-04 (Codex — 24주 첫 청구서 연속 피날레)

- 금요일 17:52의 `첫 청구서 / The First Bill`을 압박→결정→장부의 한 연속
  장면으로 완성했다. 고른 행동의 실제 장소·시간을 거쳐 수첩 장부로 돌아오고,
  제목·로그·BGM은 재시작하지 않는다. 갤러리에는 진입 한 건만 남기며 기존
  여덟 결정·후속 영수증은 보존했다.
- 2~4개 의무만 행동과 몸의 비용까지 썼다. 표현 선택지
  세 가지는 상태를 바꾸지 않고 고유 반응 뒤 합류한다. 끝낸 일·미룬 약속·
  18:00에 마감된 일을 구분하고 체력 0 경로는 장부를 건너뛴다.
- 새 책상·민준 초상과 종이·펜 실녹음 큐는 KO 960×600·EN 1280×800 렌더를
  통과했다. 완료 순간의 후보·돈·몸·관계·의무를 동결해 읽기 전용 회상을
  현재 런 변이 없이 재현한다. 옛 중간 저장은
  원자적으로 보정하고 복원 불가능한 완료 저장에는 기억을 발명하지 않는다.
  저장 손상 롤백·오디오·네 24→48주 경로·전체 감사를 통과했다. 정상 속도
  재미와 V2 타이틀→24주 실제 입력 완주는 다음 게이트다.

## 2026-08-03 (Codex — 신규 데모 화자 지식·말투 정합)

- 24주 V2 사건 32건을 실제 대화·메시지 27건과 독백형 행동 다리 5건으로
  완전히 분류했다. 27건에는 화자마다 말투 근거와 자신·공개 사실·현장 관찰·
  선행 사건·정확한 선행 선택 중 하나의 지식 출처를 붙였다. 나머지 1,565건을
  추측으로 강제 백필하지 않고 신규 V2 파일의 미분류만 앞으로 차단한다.
- 현수가 알려 주는 시험을 6월 27일 토요일 오전 9시로, 도시시설 작업표 마감을
  6월 26일 금요일 오후 6시로 맞췄다. V2 산문의 숫자형 ‘N주차/Week N’은
  세계 안의 날짜·월 표현으로 바꾸되 스케줄러의 내부 주차 계약은 보존했다.
  상철의 등장은 ‘4월 첫째 주’에서 실제 허용 범위인 ‘4월 초’로, 영어판 다은
  대화의 한국식 `-ssi` 직역은 자연스러운 이름 호칭으로 고쳤다. 다은의 화요일
  재방문은 늦은 식사 약속만 있는 경로에서는 나타나지 않는다.
- 지연과 민준이 이름을 온전히 교환하지 않은 분기는 ‘제대로 인사하지 못했다’로
  바로잡았다. 한 장면에 섞여 있던 최씨 아저씨·아버지 문자와 첫 청구서의
  선택 한정 아버지 답변도 실제 화자로 등록했다. 지식 참조 51건, 선택 결과
  한정 화자 1건이며 생산자 없는 참조·공통 화자 참조·미래 선행지식은 모두 0이다.
  KO/EN 1280×800·960×600 실제 렌더에서 계획판·세로폰·기록과 위 다섯
  대화 장면의 잘림과
  한글 누출이 없음을 확인했고, 24→48주 인계·저장·밸런스·입력·전체 60개
  GDScript 컴파일을 포함한 전체 감사가 `✅ 감사 통과`로 끝났다. 자동 결과는
  대사의 재미나 인물 매력을 사람 판정으로 대신하지 않는다.

## 2026-08-03 (Codex — 소설·영화형 연속 장면과 5장 선택 스노우볼 설계)

- `3링크`를 장면 길이와 혼용하던 기준을 고쳤다. 구현 링크·실제 패널·극적
  비트를 분리하고, 5비트든 30비트든 필요한 장면 기능이 끝날 때까지 쓰되
  숫자를 분량 목표로 삼지 않게 했다. 과거 32-root 감사의 `2~4링크·대사 2·
  PASS`는 신규 품질 합격이 아니라 퇴행 방지 기준선으로 내렸다.
- 짧은 1링크 이벤트를 여러 카드로 연속 노출하지 않는다. 시간·장소·핵심 질문이
  이어지면 제목·로그·장소·음악을 유지한 한 장면에서 대화·선택·결과·여운까지
  겪고, 짧은 결과는 현재 반응·연락·메아리·시간 다리로 흡수한다.
- 선택을 표현·기억·결정으로 분리했다. 표현 선택은 수치·플래그 없이 상대의
  고유 대답과 지문 뒤 합류할 수 있고, 기억은 이름 있는 미래 독자를, 결정은
  닫힌 길과 다음 장 또는 마지막 엔딩의 변화를 가진다. fact·receipt·반복 pattern·Moral Tint의
  역할과 다섯 장 원장·보스 입력·엔딩 계층·이관 순서를 한 정본에 고정했다.
- 24주 첫 청구서는 기존 8방향 중 하나의 결정·효과·영수증을 보존한 채 한 번
  들어가 끝까지 읽는 깊은 피날레로 다시 설계했다. 비트·링크·패널·시간은
  목표치가 아니라 관측값이며 표현 선택은 자연스러운 질문에만 쓴다.
  25~240주 전체 이관은 데모 출시선을 막지 않는 부모 계획으로 분리했다.

## 2026-08-03 (Codex — 넓은 월간 계획판·세로 연락폰 분리)

- 기종 구매·변경, 등급별 앱 잠금, 즐겨찾기와 올인원 폰 런처를 제거했다.
  월간 제안·네 주·생계/성장/회복 루틴과 잔액·고정비·사람·기록은
  1280×800 전체 폭 계획판으로 복원하고, 확정본은 읽기 전용으로 다시 연다.
- 단일 최신형 셸의 우측 세로폰은 실제 `inbound_message|call_log` 대화와
  `phone|kakao|business_card` 연락처만 보인다. 완료 뒤 자격 조건이 바뀐
  한빛 면접 문자도 확정 계획·완료 영수증으로 같은 달 기록에 남으며,
  연락 행동은 보상 없이 작성된 일정 제안으로만 이동한다.
- 과거 schema-2의 검증된 13주 리퍼폰 구매만 18만원을 한 번 환불하고 schema 3에
  정산 완료를 남긴다. 새 게임·반복 로드·손상·위조 영수증은 0원이며 24주
  경제 원장에서 선택형 기기 비용을 제거했다.
- 첫 진입을 `프롤로그 → Chapter 1 → 계획판 → 3장 튜토리얼`로 고정하고,
  실루엣 얼굴 플래시, 대화 기록 휠 닫힘, 스레드 포커스, 계획판 P/North,
  튜토리얼 뒤 숨은 폰 입력을 회귀로 막았다. KO/EN 1280×800·960×600
  44장을 실제 렌더해 안전 여백·텍스트·포커스를 확인했으며 독립 통합 검토의
  남은 P0/P1은 0건이고 최종 전체 감사도 `✅ 감사 통과`로 끝났다. 실제
  Steam Deck·실물 패드와 정상 속도 24주 재미·A/V는 동일 RC가 나온 뒤
  사람 게이트로 남는다.

## 2026-08-03 (Codex — 산출물 식별·저장 호환·제3자 고지)

- 시작 화면·새 세이브·빌드 매니페스트가 같은 네 identity 필드를 쓰게 하고,
  빌드 ID 차이는 경고로, 미래 schema·잘못된 flavor/namespace·데모 24주 초과는
  플레이어 상태 적용 전 거부하도록 고정했다. 비호환 슬롯은 사라지지 않고 출처와
  이유를 보여 주며 삭제할 수 있다.
- Godot 4.6.2 MIT와 정확한 bundled-component COPYRIGHT, 세 OFL, 오디오 21원천의
  고지를 원장에서 생성해 설정의 세 탭에 연결했다. 말발굽은 실제 출하
  `ground.mp3`의 D4XX·CC0 기록을 사용하며 필수 저작자표시는 피아노 1원천이다.
- clean Full/V2 pack은 각각 1,416 entries와 동일 entry-set hash를 가졌고 필수
  고지 10파일이 바이트 단위로 일치했다. build identity self-test 51개,
  notice self-test 15개, KO/EN 960×600·1280×800 실제 렌더, 전체 감사를 통과했다.
  UI 표면 래칫과 비출시 일본어 UI 2,585키 패리티도 기존 기준선을 유지했다.

## 2026-08-03 (Codex — ORDER-72 실제 출시 콘텐츠 인벤토리)

- 10개 `all_resources` preset의 package 포함, 런타임 로드, 24주 V2·240주
  full fresh-start 도달을 분리했다. KO/EN 사건은 각 127파일·1,597건, 엔딩
  35건이며 V2 공식 표면은 프롤로그 12+W1–24 55=67건이다. 도박·선정성·
  폭력성·공포·언어·범죄·음주/흡연/약물·AI·온라인 9축의 소유·강도·근거를
  원장과 생성 보고서로 고정했다.
- source raster 292장, 활성 246장, game pack 대상 284장, Registry 외부
  source 46장 중 pack 38장과 `.gdignore` 8장을 원본·import target까지
  분리했다. 기존 AI 정본의 코드·오디오 제작 보조 누락과 전수 인게임 검수
  과장을 현 증거에 맞게 대체했다.
- 구현 `1408609`에서 뽑은 Full/V2 pack은 각 1,412 entries이며 SHA-256은
  `1f742a5c…b297` / `ba14d823…50d2`, entry-set hash는 동일한
  `3efaa611…305e`다. V2 무인자 mounted runtime도 전용 진입 1·retail 0·
  저장 충돌 0·24주 cutoff로 통과했고 전체 감사가 `✅ 감사 통과`로 끝났다.
  최종 등급·삭제·filter와 수량 상한 없는 13개 사람 게이트는 완성된 동일
  RC에서 사용자가 판정한다.
- `gambling_rock_bottom`은 중독 80 조건만으로 빈 통장·빌린 돈을 확정해 실제
  현금/부채와 어긋날 수 있다. 이번 범위에서 서사·밸런스를 바꾸거나 여섯 번째
  제안으로 우회하지 않고 다음 승인된 정합 수리 대상으로 명시했다.

## 2026-08-03 (Codex — ORDER-71 영어 유혹 선택지 중립화)

- 4주차 영어 거절 선택지의 행동 오류와 도덕 자기선언을 한국어와 같은
  번호 차단·휴대폰 뒤집기로 고쳤다. 콘텐츠 변경은 한 필드뿐이며 선택
  순서·결과 행동·게임플레이·8주차 분기는 그대로다. 한영·정적·전체
  감사를 통과했고 영어 전체의 자연스러움이나 사람 GO는 주장하지 않는다.

## 2026-08-03 (Codex — ORDER-70 V2 release 테스트 flavor)

- retail 기본값을 닫은 채 세 플랫폼 전용 flavor·clean export·manifest와
  교집합 0인 게임 쓰기 14경로를 만들었다. macOS 무인자 KO/EN에서 전역
  표식과 24주 진입을 확인했다. 상세 해시는 `queue_archive/ORDER-70.md`가
  소유하며 외부 RC·정상 속도 재미 GO는 미완료다.

## 2026-08-03 (Codex — ORDER-69 24주 생존성·첫해 파급 정합)

- 사용자 승인대로 V2 무직/재직 생계와 성장 루틴의 주간 정신력을 `-1→+1`로
  바꿨다. 회복 `+3`, 여섯 번의 현실·주거·고용·현금 압박, 5년 공통 경제,
  `finish_run`은 건드리지 않았다. 필수 8주 클린 보상 `+10`까지 포함한
  18개 branch-only kernel은 모두 생존했고 범위는 H20~79/M1~89다. M1은
  출시 지원 하한이 아니라 경계 회귀로만 남긴다.
- 1주부터 계획·사건·포기·월말을 실행한 네 컴포넌트 경로의 W24는
  `-906,500/H28/M68`, `3,443,500/H42/M93`,
  `-2,086,500/H69/M73`, `4,373,500/H23/M52`로 잠갔다. 임의의 21주
  H/M 재설정과 과거 receipt 조립은 제거했다. H5 과로 선택은 `burnout`,
  같은 상태의 작성된 휴식 선택은 생존하며, decline이 수치를 0으로 내리면
  회고·CTA 전에 즉시 `mental_break`로 끝난다.
- 이 네 turn-25 snapshot을 실제 주간 행동·월말 시스템·MainGame scheduler로
  48주까지 이었다. W48은 각각 `1,763,500/H25/M41`,
  `12,006,537.5/H30/M56`, `583,500/H65/M47`,
  `5,840,787.5/H26/M61`; W25~48 저점은 `24/34`, `30/47`, `61/22`,
  `23/41`이다. 24주 이후 V2 효과·receipt·수치 정규화는 0건이고,
  27·31·36·42주 현수 축, 선택 경로의 28주 도시시설 결과와 48주 첫해
  결산을 실제 한 주 한 root 편성으로 보존했다. 정확 월별 원장은
  `BALANCE.md`가 소유한다.
- 임시 suppression이 지워진 뒤에도 소비된 `opening_interview_math`와 완료된
  `hyunsu_first_meet` receipt가 옛 첫 달 계산 장면과 `서울 첫 두 달` 장면의
  7개월차 재등장을 막는다. 후반 카페 투자가 대포통장 거절 전용
  `kept_clean_hands`를 만들던 생산자 오류도 제거했다. 구 저장에
  `lent_account`와 clean 플래그가 함께 있으면 구체적인 dirty 이력을 보존하고
  오염된 clean 파생 플래그만 지운다. 이미 지급된 정신력 +10과 전역 Meta
  칭호는 출처를 안전하게 역산할 수 없어 회수하지 않는다.
- 엄격 숫자 비교를 포함한 Handoff 반복 결과가 동일했고, E verbose의 타이머·
  오디오·ObjectDB 누수 0, Python 밸런스·V2 정적 감사·아크 흐름·Godot 컴파일과
  전체 감사를 통과했다. 자동 PASS는 실제 프롤로그→24주 CTA 저장을 정식판
  MainGame에서 여는 제품 이월이나 정상 속도 재미·가시적 위험 경고를 증명하지
  않는다. 데모 저장 브리지 P-4, 치명 비용 미리보기 P-5, 1원·0원 투자 정책 P-6,
  125년 동기 재편 P-7은 사용자 결정으로 남겼다. 원래 `main`과 사용자 소유
  `project.godot`은 수정·스테이징·복구하지 않았다.

## 2026-08-03 (Codex — ORDER-68 실행 큐·사람 게이트 수렴)

- 충돌하던 `CLAUDE`·큐·HANDOFF 순서를 `CODEX_QUEUE` 한 곳으로 모았다. 57~67
  대형 원문은 손실 없이 `queue_backlog/`로 옮기고, 승인된 P-1~P-3과 즉시 출고
  블로커를 ORDER-69~75의 1~2배치 사양으로 분리했다. 상태/배치 수 불일치는 새
  `queue_consistency_check.py`가 전체 감사에서 막는다.
- 사람 게이트를 schema v2로 올려 demo/full scope, 정확한 RC, 표본·환경, 합격
  기준, 완료 증거의 commit/tree/manifest 결속을 강제했다. V2 이전 `e849a6a`는
  역사 증거로만 RETIRED했고, 현 후보는 거짓 해시 없이 `waiting_rebuild`다. 동일한
  구 RC 표본 10건도 `playtest_report.py`가 현 후보와 다르면 거부한다.
- P-1의 종전 정신력 수열이 필수 사건을 누락한 것도 정정했다. 실제 현 clean
  생계+성장은 12주 말 종료이며, ORDER-69는 18 kernel·Godot 4 full-route·24→48주
  이월로 `-1→+1` 승인값을 검증한다.
- context/queue/dashboard/human-gate/playtest self-test/정적 감사와 diff 검사가
  통과했다. 자동 검사는 사람의 정상 속도·연속 A/V·재미 GO를 대신하지 않는다.

## 2026-08-03 (Codex — 최신 메인 재통합·ORDER-57 E 후보 정합 마감)

- `origin/main@ed661ec`를 별도 `codex/order57-e-integration` 작업트리에 두고
  보존 체크포인트를 재통합했다. 원래 `main`과 사용자 소유 `project.godot`은
  수정·스테이징·복구하지 않았다.
- 기존 atomic 효과를 먼저 한 번 정산한 뒤 결과 확인→작성형 장면→주 완료로
  이어지는 다섯 action→story 경로를 붙였다. 결과 전·스토리 중 저장/불러오기와
  중복 신호에서도 효과를 다시 쓰지 않는다. 실제 배타·주차·인물 상한을 전수
  열거한 authored 최대는 `3/3/4/4/4/3`(합 21), 생계 중심 실용 경로는
  `1/2/1/2/2/1`이며 60번들 모두 양의 `estimated_minutes`를 가진다.
- 낡은 시작폰→리퍼폰→플래그십의 재질·베젤·물리 키·제스처 차이를 유지하고,
  구매·즐겨찾기를 소유 화면에서 즉시 자동저장한다. 실패하면 기기 앱에 경고와
  재시도 버튼을 남기고 실제 실패→재시도→저장 스냅샷 왕복을 검사한다.
- 음수 `money`를 마이너스 은행 잔액처럼 보여 주지 않고 `가용 잔액 0원 + 체납`으로
  분리했다. 24주 첫 청구서·폰 은행·MainGame·StoryMode가 같은 뜻을 쓰며,
  48주 결산은 현금 토큰이 아니라 투자·대출을 포함한 `{assets}`를 읽는다.
- 다은의 20주 약속→21주 회수, 24주 금요일 17:52→18:00·긴급 집결 18:30,
  현수 토요일 시험, 상자 여섯 개와 수당·근무일을 한영에서 맞췄다. 번역투와
  목적어 없는 문장도 실제 행동·소리·원장 문장으로 다시 썼다.
- 재고조사 창고와 5월 이삿짐 트럭 배경 2종, V2 장면 direction 55/55와
  시각·오디오 계약을 연결했다. KO 960×720·EN 1280×720 실제 렌더는 각각
  34면을 거쳐 24주 결산과 구형 저장 재개까지 통과했고, 시작폰·리퍼폰·
  플래그십·은행·월간 달력·결산을 직접 확인했다.
- 자동 검사는 실제 75~95분 재미·기억성·패드 감각을 증명하지 않는다.
  분량 가중치는 `[첫 실행 재조정]`이며 P-1 정신력, P-2 전용 flavor,
  P-3 24주 T1 정점은 사용자 결정 전까지 구현하지 않고 `runtime_default=false`를
  유지한다.
- 전체 감사는 정적 오류/경고 0, V2 A~E·24→48주 인계·폰 저장, 한영·일본어
  키, 모드 exact-path, 58개 GDScript 컴파일을 모두 통과했다. 자동 감사가
  대신할 수 없는 사람 게이트 12건은 열린 상태로 정확히 남겼다.

## 2026-07-31 (Claude — 사람 게이트 원장 `human_gates.json`)

- 사용자 질문 *"감사쪽은 이제 체계가 잘 잡혔어?"* 에 답하려고 실측했다. 검사 83개,
  추적 파일 1,816개 중 규칙 없는 것 19개(1%), 표적 선택 0.9초 대 전체 10분.
  **체계는 잡혔고, 약한 곳 넷을 정직하게 적었다** — ①감사는 정합을 재지 재미를
  재지 않는다 ②래칫에 얼린 부채(테마 우회 2,116·팔레트 밖 678)는 통과한다
  ③**사람 게이트를 출력하는 검사가 `identity_signature` 하나뿐이다**
  ④`STATUS.md`가 보고하는 수치(연출 6%·정답 선택 414)에 래칫이 없다.
- 사용자 지시로 ③을 고쳤다. ④는 `ORDER-63`·`ORDER-67`이 그 수치를 움직일 때
  같이 걸어야 재작업이 없다.
- **게이트를 발명하지 않았다.** `CLAUDE.md`의 `열려 있는 사람 게이트`, 큐 표의
  `사람 판정` 열, `identity_signature_audit`의 `HUMAN_GATES` 세 곳에 흩어져 있던
  것을 `docs/human_gates.json` 한 곳으로 모았다(12건).
- `tools/human_gates.py`는 **아무것도 통과시키지 않는다.** 남은 사람 판정을
  화면에 올려 **초록불의 뜻을 좁히는 것**이 전부다. 다만 원장 자체는 검사한다 —
  `owner`가 큐에 없으면(오더가 끝났는데 게이트가 남음), `done`인데 `evidence`가
  없으면 실패한다. 둘 다 실물로 시험해 확인했다.
- 도메인별로 자기 몫을 찍게 배선했다 — `surface_coherence`(surface),
  `narrative_continuity`(narrative), `first_session_pacing`(pacing),
  `identity_signature`(identity). `audit.sh`는 끝에 전체를 출력한다.
- **`demo_experience_audit`에 걸었다가 물렸다.** 인자 없이는 그 자리에 도달하지
  않아 게이트가 영영 안 보인다. 단독 실행되는 `first_session_pacing_audit`으로
  옮겼다. **출력을 실제로 확인하지 않았으면 배선한 줄 알았을 자리다.**
- 열린 게이트는 `STATUS.md`에도 실린다. 제안과 같은 이유다 — 보이지 않는 것은
  처리되지 않는다.

## 2026-07-31 (Claude — 제안 채널 `PROPOSALS.md`와 자가발전 규칙)

- 사용자 질문: *"코덱스가 작업하다 보면 더 좋은 아이디어나 스킬, 스토리 변경점,
  컨텐츠 수정 등이 필요할 때 스스로 생각해서 먼저 제안하고, 자가발전할 수 있게
  어떻게 시킬까."*
- **권한은 이미 있었다.** `CLAUDE.md`가 "지시를 무조건 긍정하지 않는다"고 이미
  말한다. 없던 것은 **자리·형식·정리 규칙**이다. 그래서 권한을 더 주지 않고
  **제안이 썩지 않는 구조**를 만들었다.
- **가장 큰 위험은 제안 자체가 아니라 안 읽히는 목록이다.** 이 저장소의 반복된
  실패가 `선언만 있고 소비자가 없는 정본`(`ORDER-65`)이다. 제안 파일을 하나 더
  만들면 석 달 뒤 아무도 안 읽는 200줄이 된다. 그래서 셋을 걸었다.
  - **열린 제안 5건 상한.** 여섯 번째를 올리려면 하나를 먼저 닫아야 한다.
    **무엇을 제안하지 않을지 고르는 것이 제안의 절반이다.**
  - **21일 만료.** 넘으면 감사가 실패하고 `POST_LAUNCH_NOTES.md`로 내려간다.
    버리지 않되 살아 있는 목록과 언젠가의 목록을 섞지 않는다.
  - **열린 제안이 `STATUS.md` 맨 위에 자동으로 실린다.** 사용자가 이미 보는
    화면에 나타나야 결정이 일어난다.
- **제안 자격을 "일하다 실제로 부딪혔는가"로 못박았다.** 브레인스토밍에서 나온
  것은 `POST_LAUNCH_NOTES.md`다. 이 한 줄이 노이즈의 대부분을 막는다.
- 형식에 둘을 필수로 넣었다. **`권고`** — 선택지만 늘어놓는 것은 판단을
  떠넘기는 것이다. **`안 하면 계속 무엇을 내는가`** — 이걸 못 쓰면 제안이 아니라
  취향이므로 올리지 않는다.
- 작업 중 발견은 **셋으로 분류**했다(그냥 한다 / 하면서 적는다 / 멈추고 제안한다).
  1번 목록이 넓다 — 오탐 수정, 실패 메시지 개선, 등록 누락, **방금 자기가 헤맨
  것을 스킬에 한 줄**. 마지막이 진짜 자가발전 고리이고 비용이 0이다. 다만
  **매 세션 자라는 스킬은 읽히지 않으므로** 자기를 구했을 자리에만 적게 했다.
- 검사는 `context_manifest_check.py`가 소유한다(새 도구를 만들지 않았다).
  상한 초과·만료·결정된 건 제외를 실물로 시험해 셋 다 확인했다.
- **방금 만든 1번 분류를 바로 적용했다.** 스킬을 고치고 `audit_select`를 돌리니
  `.codex/skills/**`에 규칙이 없어 **한 줄 수정이 전체 감사(10분)를 불렀다.**
  규칙을 넣었다 — 스킬은 런타임 표면이 없어 문서 정합만 해당하고, 그 검사는
  `always`라 실제 추가 비용이 0이다. 그러다 둘이 더 나왔다.
  - `audit_scope.json`에 `tools/project_dashboard.py`를 인자 없이 등록해 둬서
    **표적 감사가 신선도 검사 대신 HTML을 빌드하고 있었다.** `--md ... --check`를
    붙였다. 등록만 하고 실제로 무엇이 실행되는지 안 본 내 잘못이다.
  - 붙이자 `--verify`가 MISSING으로 잘못 잡았다. `run_check`는 인자를 지원하는데
    `registered_tools`가 전체 문자열로 대조해서다. 첫 토큰만 보게 고쳤다.
    부작용으로 **인자 달린 등록을 경로로 취급해 조용히 삼키던 EXTRA 보고**도
    드러났다 — 둘 다 의도된 것이라 보고만 정확해졌다.

## 2026-07-31 (Claude — `ORDER-67` 발행: `.gd` 분해)

- 사용자 지시: *".gd 정리하는 것도 오더에 넣어."*
- 재기 아니라 측정으로 썼다. **`MainGame.gd` 19,984줄 = GDScript 전체 95,805줄의
  21%**, 함수 688·멤버 244·`signal` **0**·구획 주석 2. 가장 큰 함수
  `_next_arc_id`가 **1,215줄**이다.
- **왜 지금인가를 근거로 묶었다.** `add_theme_*_override`가 전 저장소 2,362건인데
  **645건(27%)이 이 파일 하나에 있다** — `ORDER-63` 배치 3(테마 단일 출처)이
  19,984줄을 훑어야 하므로 리뷰도 되돌리기도 불가능하다. 그래서 `ORDER-57` E 뒤,
  `ORDER-63` 배치 3 앞으로 자리를 못박았다. **데모보다 앞서지 않는다.**
- **가장 싼 첫 칼을 기계로 찾았다** — 멤버 변수도 노드도 건드리지 않는 함수가
  **316개, 7,267줄**(함수의 46%, 줄의 36%). 입력만 받아 값을 내므로 옮기면 그대로
  돈다. `systems/`에 이미 `static func` 관례가 있어 새 관례를 만들지 않는다.
- 배치 1이 계측기다. **뜯어낸 자리로 다시 흘러 들어가는 것을 래칫이 막는다** —
  이 저장소에서 이미 여러 번 진 싸움이다. 상한 수치는 `[첫 실행 재조정]`이다.
- 경계를 명시했다. 고아 스크립트(`TradingFloor.gd`·`StatRow.gd`) 제거는
  `ORDER-62` 배치 3이 소유하므로 중복 소유하지 않는다. 테마·색·폰트는
  `ORDER-63`이 소유한다. **이 오더는 행동을 바꾸지 않는 이동만 한다.**
- **`CODEX_QUEUE.md` 예산을 12KB → 13KB로 올렸다.** 행 하나를 넣으려고 열두 행을
  압축했는데, 그중 다섯이 `사람 판정`에서 "사람"을 지워 **자동 게이트와 사람
  게이트의 구분을 없앴다.** 되돌리고 예산을 올렸다 — **의미를 깎아 예산을 맞추면
  예산이 문서를 이긴 것이다.** `HANDOFF` 상한 때와 같은 판단이며 이유를
  `context_manifest.json`에 남겼다. 다음에 모자라면 압축이 아니라 완료 오더를
  `queue_archive/`로 내린다.

## 2026-07-31 (Claude — 현황 대시보드 `tools/project_dashboard.py`)

- 사용자 질문: *"게임개발 진행상황을 파악하거나 각 이벤트 선택지에 따른 결과
  마인드맵이나 각종 컨텐츠, 일정, 인물정보를 쉽게 판단할 수 없을까?"*
- **손으로 그린 현황판을 만들지 않았다.** 그런 문서는 만든 날에만 맞다. 대신
  저장소를 읽어 한 장으로 생성하는 도구를 만들었다 — `CODEX_QUEUE.md` 표(오더),
  래칫 기준선 셋, `identity_signature.json`(주연 여섯 서명), `narrative_spine.json`
  (5장이 여닫는 동사), `demo_core_loop_v2.json`(24주 번들 56개), 그리고 모든
  KR 이벤트의 `choices`·`follow_up_event`로 만든 **선택 체인 그래프**.
- 체인 탐색기는 `ORDER-60` 배치 2의 **지배 선택 판정을 브라우저에서 다시 돌린다.**
  판정 로직이 감사 도구와 화면 양쪽에 있으므로 정의가 바뀌면 둘 다 고쳐야 한다.
  현재 그래프(2링크 이상 체인 65개, 211링크)에서 11건이 정답 선택으로 걸린다.
- **개발용 화면임을 페이지 상단에 명시했다.** `tint`·`route_*`·정확한 수치를 그대로
  보여 주는데 이는 플레이어에게 절대 노출하지 않는 값이다. 도구가 정본 규칙을
  우회하지 않도록 경고를 화면이 소유한다.
- 출력은 `build/`(gitignore)이므로 저장소에는 생성기만 들어간다. `audit.sh`에
  넣지 않았다 — 판정을 내리지 않는 생성기이며 게이트에 넣으면 시간만 든다.
- **사용자 지적 둘로 배선을 두 번 고쳤다.**
  (1) *"저 세션 닫으면 못 보는 대시보드 아니야?"* 맞다. 아티팩트 링크는 남지만
  **내용은 올린 시각의 스냅샷이고 Codex는 아티팩트를 올릴 수 없다.**
  (2) *"마크다운 형태로 사람이 보기 좋게 관리하는 문서여도 되잖아."* 이쪽이 맞다.
  Pages 워크플로를 만들었다가 **지우고** `docs/STATUS.md` 생성으로 갈아탔다.
  GitHub이 그대로 렌더하고, **PR diff에 상태 변화가 드러나고**, Pages 토글도
  공개 스포일러 판단도 사라진다. 인프라를 세우기 전에 이미 있는 표면(저장소가
  마크다운을 렌더한다)을 쓰는 편이 낫다는 것을 사용자가 먼저 봤다.
- **낡음을 검사가 막는다.** `--md ... --check`가 지금 내용과 다르면 1로 끝나고
  `audit.sh`가 `STATUS_DOC_EXIT`로 받는다(`audit_scope.json` 등록 완료).
  생성 시각 줄은 비교에서 뺐다 — 그것까지 비교하면 매번 실패한다.
  **갱신을 사람 기억에 맡기면 잊는 순간 거짓말이 되므로 검사가 소유한다.**
- 데모 체인 28개를 mermaid로 그린다. 번들 루트는 대개 다른 사건의 후속이라
  전역 체인 루트가 아니어서, 루트 집합으로 거르면 5개만 잡혔다 — 번들 루트에서
  직접 걷도록 고쳐 28개가 됐다. `⚠︎연출없음`이 미완 비트를 그림 위에 표시한다.
- **`정답 선택 413`을 하드코딩했던 것을 걷어내고 매번 다시 세게 했다.** 다시
  구현하니 **414**다. 축 아홉·엄격 우월·후속/플래그 동일이라는 정의는 그대로이고,
  호감 합산/인물별 분리/동률 허용을 바꿔 봐도 414에서 움직이지 않는다. 413을 낸
  스크립트가 남아 있지 않아 한 건의 차이는 특정하지 못했다. **재현되지 않는 수치를
  정본으로 두지 않으므로** `DEMO_TIER_AUDIT.md`에 정정을 적고 소유권을
  `dominant_index()`로 옮겼다. 처음엔 효과 키 전부를 축으로 봐서 345가 나왔는데,
  **정본이 고정한 축 아홉과 다른 정의를 쓴 내 잘못이었다** — 판정용(`ax`)과
  표시용(`eff`)을 분리해 맞췄다.
- 검증: 실제 렌더를 Chromium으로 확인했다(라이트·다크 양쪽, `data-theme` 포함).
  한국어 어절 중간 줄바꿈(`word-break:keep-all`)과 `ORDER-57`이 `ORDE/R-57`로
  쪼개지던 것을 고쳤다. 검색·선택 배선은 DOM 덤프로 확인했다(`daeun` → 12체인).

## 2026-07-30 (Claude — ORDER-60 배치 2 부분: 도덕 축 전 구간 측정)

- 배치 1의 `착한 쪽이 비용 없이 tint를 얻는다`가 계통인지 확인하려고 전 구간을
  기계로 쟀다. `tint 양수·음수 효과 없음`은 부정확해서(프롤로그가 반례였다)
  **지배 선택**으로 정의했다 — 모든 축에서 우월하고 후속·플래그도 갈리지 않는 선택.
- **전 구간 413/1,484(28%)가 순수 정답 선택이다.** 지배 선택은 639건이고 그중
  226건은 후속·플래그로 길이 갈려 대가가 있다. 프롤로그의 3/4는 우연이 아니었다.
- **데모 V2는 2/33(6%)로 예외적으로 건강하다.** `ORDER-57` 재구축이 이 축에서
  실제로 효과가 있었다는 첫 측정 증거다. 걸린 2건도 `tint`가 0이라 도덕 축이 아니다.
- 전형은 대가 부재가 아니라 **이중 보상**이다 — `arc_father_04_visit`는 `tint +9`에
  `mental +23`, `arc_daeun_proposal_answer`는 `tint +8`에 `mental +25`. 다만 감정의
  정점이라 `mental` 상승 자체는 옳고, 고칠 것은 밀어낸 것의 부재다.
- **배치 2를 완결했다.** 사용자가 범위 A(V2만 판정)를 택했다. 남은 기준을 재니 데모 V2는 **정합은 통과하고 밀도는 미달**이다 — 자산 미등록 0, EN 43/43 한글 누출 0, 루트 무결성 0건인데 **`existing_roots` 장면 27개 중 21개가 1링크**이고 **도달 이벤트 43개 중 39개에 `direction`이 없다(91%)**. `core_loop_v2_events.json` 신규 16개는 전부 0이다. 번들 56개 중 27개는 `action_id` 행동이라 애초에 장면이 아니다. `ORDER-57`은 구조를 옳게 짓고 아직 채우지 않았으며, 이는 E의 `장면 밀도 REWORK`가 이미 소유한다.
- 원래 멈췄던 이유: `runtime_default=false`라 판정한 V2가 지금 출고되는
  것이 아닌데, 폴백 구간은 주차가 아니라 플래그·아크로 걸려 정확히 열거되지 않는다.
  `min_turn<=24` 같은 필터는 후반 사건을 대량으로 끌어와 **방어할 수 없는 숫자라
  쓰지 않았다.** 범위 선택지 A/B/C를 사용자에게 올렸고 권고는 A(V2만 판정)다.
- P0은 없다. 판정표는 [`DEMO_TIER_AUDIT.md`](DEMO_TIER_AUDIT.md).

## 2026-07-30 (Claude — ORDER-60 배치 1 프롤로그 재판정)

- 프롤로그 11장면(`story_flashforward`~`story_prologue_meal`)을 2026-07-30에 선
  정본 여덟으로 재판정하고 [`PROLOGUE_TIER_AUDIT.md`](PROLOGUE_TIER_AUDIT.md)에
  판정표를 남겼다. **판정 오더라 콘텐츠·코드는 고치지 않았다. P0은 0건이다.**
- **계통 결함 하나**: 도덕 축이 대가를 읽지 않는다. 선택이 있는 4장면 중 3장면에서
  착한 쪽이 비용 없이 `tint`를 얻는다. `story_prologue_dad`는 한쪽이 순수 이득
  (`mental +5`·`tint +2`·아버지 호감 +12), 다른 쪽이 순수 손실이다. **산문은
  이미 대가를 안다** — 결과문이 스스로 `거짓말이었다`라고 쓴다. 수치만 모른다.
  `story_prologue_meal`은 실제 교환이 있어 대조군이 된다.
- `direction` 누락 4건(`story_arrival`·`story_prologue_dad`·`story_prologue_goal`·
  `story_prologue_meal`). 무릎·상환 체인 7장면은 전부 보유하는데, 하필 상품 정의를
  지는 뒤쪽 네 장면만 비었다. 렌더러는 이미 있어 수리 비용이 가장 낮다.
- 정본 쪽 공백 하나를 발견하고 **같은 날 사용자 승인으로 해소했다**:
  `SCENE_TIER`가 체인을 한 장면으로 세는지 규정하지 않아, T1의 `체인 2~4개`를
  문자대로 읽으면 11이벤트 프롤로그가 너무 길어서 미달이 됐다. §0을 세워
  **체인 하나가 장면 하나, 계약은 체인 전체에, 길이 상한 폐지**로 정했다.
  다시 판정하니 **프롤로그는 이미 T1이고 밀도 계약을 전부 충족한다** — 남은
  것은 연출 커버리지와 도덕 축 둘뿐이다. `[첫 실행 재조정]` 표시가 실제로
  작동한 첫 사례이며, 추정 수치가 방어되지 않고 고쳐졌다.
- 통과: 자산 10종 전부 등록, 한영 11/11에 한글 누출 0, 아버지 병 언급은
  `STORY_BIBLE.md:93`과 일치, 플래그 9개 전부 독자 보유, 번역체·설명조 없음.
- 조사 함정: **플래그는 `conditions`가 아니라 dik 산문 키로 소비된다.**
  `conditions`만 훑으면 살아 있는 플래그를 휴면으로 오판한다.
## 2026-07-30 (Codex — ORDER-57 E 1~24주 출시 후보·장편 인계 완결)

- 21주 계획 전에 도착하는 아버지 약국 봉투 신호부터 24주 토요일 현수
  시험 시작까지 실제 주차를 연결했다. 24주는 수요일 대포통장 후속,
  금요일 오후 5시 52분 한 가지 의무, 토요일 오전 9시 시험 순서이며
  선택한 일·미룬 일·기한 상실을 서로 다른 영수증으로 보존한다.
- 현수의 정식 실패는 27주, 도시시설 작업표 결과는 28주에 열리고,
  연말 결산은 48·96·144·192주에 정확히 닫힌다. 결산 직후 올해의
  장면은 같은 StoryMode 큐에서 고르며 4장 결산의 5주 뒤 정산은
  197주다. 대표 두 240주 경로는 잼과 후속 유실이 없다.
- 17주 취업 뒤 첫 급여는 3주치 168만원, 다음 온전한 달은 224만원으로
  유지했다. 24주 월말 문구를 두 번째 달의 첫 온전한 마감으로 고쳤고,
  현수의 23주 일요일 대화·21주 다은 약속·아버지 약 문자와 지연 후속도
  실제 시간·선행 만남만 읽게 했다.
- 21주 신호는 앞선 아버지 전화를 건너뛴 경로에서 그 첫 연락을 정식으로
  대체해 25주에 과거 통화가 되살아나지 않는다. 이미 약과 복용 정보를
  안 뒤 같은 혈압약 소식을 다시 새 정보로 말하는 랜덤 통화도 막았다.
  완료 회고는 합법적인 미해결 항목을 잘라내지 않는다.
- 24주 완료 전이만 단 한 번 자동 저장한다. 완료 저장 재개는 기존
  저장을 신뢰해 회고만 열며, 불필요한 재저장 실패로 제목 복귀를 막지
  않는다. `finish_run`과 5년 엔딩은 호출하지 않는다.
- 시작폰은 2013~2015년형 낡은 폴리카보네이트 셸, 넓은 상하 베젤,
  수화부·센서, 물리 메뉴/홈/뒤로 키를 사용한다. 리퍼폰과 플래그십의
  얇은 금속 셸·제스처 바와 실제 KO/EN 720p 화면에서 구분된다.
- 정적 감사 ERROR 0/WARNING 0, 1,592사건 한영·서사·시각·오디오·연출
  계약, A~E 저장 런타임, 24→48주 인계, 대표 240주 흐름과 58스크립트
  컴파일을 통과했다. `runtime_default=false`와 사용자 소유
  `project.godot`은 유지하며 F 정상 속도·물리 패드·A/V·외부 사람 GO는
  열어 둔다.

2026-07-30의 오래된 V2 C/D 항목과 2026-07-29 이전 항목은
[`history/WORK_LOG_2026-07-30.md`](history/WORK_LOG_2026-07-30.md),
[`history/WORK_LOG_2026-07-29.md`](history/WORK_LOG_2026-07-29.md),
[`history/WORK_LOG_2026-07-27.md`](history/WORK_LOG_2026-07-27.md),
[`history/WORK_LOG_2026-07-26.md`](history/WORK_LOG_2026-07-26.md)와
[`history/WORK_LOG_2026-07-25.md`](history/WORK_LOG_2026-07-25.md),
[`history/WORK_LOG_2026-05-16_to_2026-07-24.md`](history/WORK_LOG_2026-05-16_to_2026-07-24.md)에 있다.
