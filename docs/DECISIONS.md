# Gangnam Dream Decisions

## 2026-07-23 (출시 오디오는 실제 녹음·실악기 샘플만 사용한다)

- **영구 규칙**: BGM, 앰비언스, 폴리, UI, 카지노·경마 효과음의 원음은 실제 현장/사물 녹음 또는 녹음된 실악기 샘플이어야 한다. 오실레이터, 생성 노이즈, 코드로 만든 파형과 런타임 합성 대체음은 출시 키에서 금지한다.
- **허용 편집**: 컷 편집, 레이어링, 루프, 리샘플링, 제한된 피치 이동, EQ, 컴프레션, 리버브, 러드니스 정규화는 허용한다. 이 편집은 원음을 새 파형으로 발명하는 수단이 아니라 녹음물을 장면과 재생 계약에 맞추는 후반 작업이다.
- **출처 계약**: 모든 출시 파일은 `AUDIO_SOURCE_MANIFEST.json`에 공급자/저작자, URL, 라이선스, 원본 파일명, 원본·출력 SHA-256, 편집 이력을 가진다. 원본 라이브러리는 저장소 밖에 두고 필요한 고지문만 배포한다.
- **실패 방식**: 파일이 없거나 로드되지 않으면 오류와 무음으로 실패한다. 전자음 폴백으로 감추지 않으며 자동 감사에서 차단한다. 자동 감사 통과는 출처·기술 계약만 증명하고, 장면 적합성·감정선·피로도는 사람의 청취 승인이 필요하다.
- **폐기 결정**: 아래 2026-06-13의 deterministic local synthesis 결정은 이 결정으로 완전히 대체한다. 파일명과 런타임 semantic key만 저장 호환을 위해 유지한다.

## 2026-07-20 (랜덤 사건은 수량이 아니라 전경 자격과 인과로 편성한다)

- **전경 자격**: 실제 디렉터 후보 1,032개를 모두 StoryMode로 올리지 않는다. 충분한 도입·결과 산문과 실질 상태 변화가 있는 61개만 독립 전경을 소유한다. 코미디, 한국 해설형 단문, 비손실 콜백 복습은 전경 풀을 소유하지 않는다.
- **후속 자격**: 한 선택만 있고 선행 상태를 실제로 읽는 18개 사건은 Quiet/Echo의 비차단 후속이다. 이를 만들 수 있는 원인 선택 뿌리는 7개이며, 조건이 성립한 본편 주 25 이후 확률 없이 정확히 한 번 회수한다.
- **안전 경계**: 다중 선택은 자동 확정하지 않고, 직접 Decision/Boss 중에는 후속을 끼우지 않는다. 데모 빌드는 주차와 무관하게 랜덤 후속 0건이다.
- **불변**: 이벤트 ID, KO/EN 본문, 선택지, 효과, 이력, 저장 호환은 바꾸지 않는다. 강등은 삭제가 아니라 노출 위계 변경이다.
- **판정 한계**: 61/18/7 계약, 장별 창, 중복 0과 한영 240주 도달성은 구조 증거다. 사건이 기억에 남는지와 선택이 아쉬운지는 사람의 정상 독해 재플레이로만 합격시킨다.

## 2026-07-20 (30억은 지평선으로 남기고, 이번 주에는 다음 단을 오른다)

- **두 겹 목표**: 30억원은 게임의 종극 목표와 엔딩 임계로 보존한다. 상단 HUD·주간 압박·월말 결산은 현재 상태에서 실제로 닿을 수 있는 다음 한 단만 보여 준다. `30억 0.00%`를 5년 내내 응시하게 하지 않는다.
- **단계 정본**: 현재 월 고정비와 대출이자를 먼저 충당하고, 그다음 같은 비용의 3개월치를 현금 비상금으로 확보한다. 이후 `300만원 → 원룸 800만원 → 종잣돈 2천만원 → 빌라 4천만원 → 1억 → 아파트 1.3억 → 5억 → 10억 → 20억 → 30억`의 실제 자산 단계를 따른다.
- **안전 판정**: 직업 보유는 안전 플래그가 아니다. 월말 이후 현금이 음수면 정신력 -4, 한 달치 미만이면 -2, 세 달치 미만이면 -1이며, 실제 3개월 완충을 쌓아야 현금 압박이 사라진다. 주거가 오르거나 대출이자가 늘면 필요한 안전선도 함께 오른다.
- **표면 위계**: HUD의 다음 단계가 프롤로그에서 고른 수첩 문장을 대체하지 않는다. 단계는 경제의 현재 위치이고, 수첩 문장은 플레이어가 선택한 삶의 동기다. 둘은 툴팁과 결산에서 함께 읽히되 역할을 섞지 않는다.
- **불변**: 월급, 주거비, 투자 수익률, 파산선, 30억원 승리 조건, 엔딩 라우팅은 바꾸지 않는다. 이 결정은 숨은 추격 보정이나 새 소비 항목이 아니라 진행 가독성과 결핍 판정의 정렬이다.

## 2026-07-20 (산문은 자동으로 흐르고, 플레이어 입력은 결정에 쓴다)

- **본편 기본값**: 정상 StoryMode는 AUTO ON으로 시작한다. 타이핑·문단·결과·선택지가 하나뿐인 불가피한 행동은 언어별 독해 시간을 가진 뒤 이어진다. 수동 진행과 AUTO 토글은 언제나 허용한다.
- **결정 경계**: 다중 선택, 타이머, 챕터 카드는 자동 확정하지 않는다. 플레이어가 건 길과 그 대가만 입력으로 남기며, 자동 재생은 의미 선택을 대신하지 않는다.
- **회상 예외**: 읽기 전용 회상/갤러리는 수동으로 시작한다. 이 일시적 OFF는 본편 세션의 AUTO 취향을 변경하지 않는다.
- **독해 속도**: KO/JA는 글자 수, EN은 단어 수를 기준으로 정상 독해 시간을 계산하고 이미 보이는 타이핑 시간을 뺀 뒤 짧은 호흡을 더한다. 짧은 문장과 긴 문단을 같은 고정 초로 넘기지 않는다.
- **측정 정직성**: 빠른 입력 블랙박스 수치는 도달성 증거다. 실제 시네마틱 계약의 필수 Story 정지는 프롤로그 5회, 현재 데모 36회이며, 이 감소는 인간 재미 GO를 뜻하지 않는다.

## 2026-07-20 (한 주의 전경 결정은 작성형 장면과 범용 AP 중 하나만 소유한다)

- **소유권 정본**: 작성형 다중 선택이 해당 주의 핵심 갈등이면 그 선택이 주간 약속이다. 같은 주에 범용 AP 세 장을 다시 열지 않는다. 데모 소유 주차는 `4/10/13/16/20/23/24`, 범용 주차는 `1/8`이다.
- **데이터 계약**: 소유권은 이벤트 ID, 돈/사람 축, 선택 인물을 `event_director.json`에 선언한다. StoryMode는 언어 독립 선택 인덱스와 공개 상태 변화, 놓친 선택을 기록하며 같은 주의 두 번째 소유 후보는 새 원장을 만들 수 없다.
- **시간 정직성**: 4주 대포통장처럼 나중에 결과가 오는 계약만 미도착으로 표현한다. 즉시 끝난 선택은 이미 그 주의 방향을 바꿨다고 말한다. 구 저장의 `story_boss` 레코드는 계속 읽어 세이브를 깨지 않는다.
- **검사 정본**: AP 카드 클릭은 취소 가능한 하위 모달 탐색일 수 있으므로 완료 약속과 동일시하지 않는다. 한 주 한 결정은 실제 `weekly_commitments` 원장으로 판정한다.
- **한계**: 자동 런은 이중 결정 0과 240주 도달성을 증명한다. 포기한 길의 장기 비용, 산문의 감흥, 실제 망설임은 인간 재플레이 없이는 합격시키지 않는다.

## 2026-07-20 (플레이어는 민준을 지켜보는 사람이 아니라, 건 것의 대가를 사는 민준이다)

- **한 문장 판타지**: `나는 민준이다 — 매주 무언가를 걸고, 건 것의 대가를 살아낸다.` 이 문장이 상품·루프·대사·연출의 1차 판정 기준이다.
- **주도권 정본**: 편성기는 압박을 만들 수 있지만 궤적을 대신 정하지 않는다. 첫 주에 플레이어가 첫 두 달의 우선순위를 정하고, 직접 주간에는 하나를 실제로 실행하며, 포기와 결과가 이후 장면의 재료가 된다.
- **MORAL_TINT의 위계**: 색과 재독은 도덕 점수판이 아니라 행동의 후유증을 늦게 체감시키는 2차 여운이다. `moral`, `route`, 선악 등급과 정확한 숨은 확률은 계속 화면에 노출하지 않는다.
- **서사 보스 소유권**: 작성형 아크가 그 주의 핵심 딜레마를 이미 제공하면 그것이 주간 결정이다. 같은 주에 일반 AP 3택을 다시 요구하거나, 선택 시점에 미래 결과를 미리 설명하지 않는다.
- **증거와 한계**: ORDER-36 자동 경로는 1주 계획, 4주 대포통장 보스, 8주 지연 결과와 한영·패드·저장 계약을 통과했다. 이 결정은 구조 정본이며, 망설임·후회·계속하고 싶은 이유는 외부 정상 독해 플레이에서만 합격시킨다.

## 2026-07-18 (후반 자산 이정표는 시간의 청구서이며, 개인 장면은 실제 현재 주거를 따른다)

- **시간축 정본**: 2장은 `1년 반→두 번째 해의 끝`, 4장은 `10억→고독`, `몸의 경고→6주 뒤 3년 반→8주 뒤 새벽`, `36세 연말→5주 뒤 정산→마지막 해`로 읽힌다. 25억 부동산 판단은 20억 최종 질주를 본 뒤에만 허용한다. 수치는 별도 축하 카드가 아니라 이후 몸·사람·주거가 읽는 원인이 된다.
- **공간 정본**: 민준이 혼자 머무는 후반 개인 장면은 `current_housing`으로 저장된 고시원·원룸·빌라·아파트와 해당 룸톤을 해석한다. 정적 `goshiwon_room`, `goshiwon_hallway`, `late_night`는 본문이 실제 고시원 체류를 명시할 때만 쓴다. 한강 장면은 실제 강변/난간 구도와 산문을 일치시킨다.
- **생사 안전성**: 10억 고독 장면의 아버지 선택은 살아 있는 아버지에게 거는 실제 전화가 아니라, 생사 어느 경로에서도 성립하는 기억 속 대화다. 원격 통화가 실제인 장면만 전화 배지와 작은 원격 초상을 가진다.
- **증거와 한계**: 대표 A/B의 1~5장 고립 단편 0, 장별 정지 `27/25/26/16/13`, `29/24/32/23/15`, KO/EN 28컷은 순서·공간·오디오 회귀 증거다. 자동 통과는 감흥이나 엔딩 만족도를 판정하지 않는다.

## 2026-07-18 (1장의 약속은 모든 선택 경로에서 예약되고, 직업·공간을 발명하지 않는다)

- **예약 소유권**: `deferred_follow_up`은 EventManager가 아니라 모든 VN 선택이 공통으로 통과하는 `GameState.apply_choice()`가 예약한다. StoryMode 직접 재생에서도 약속을 잃지 않으며 EventManager는 중복 예약하지 않는다.
- **현수 시간축**: 실패선은 `시험→정식 결과→4주 뒤 후유증→5주 뒤 방황→6주 뒤 새 길`이다. 시험 당일과 결과 발표는 현수가 이미 방에 돌아온 것처럼 보이지 않게 복도·민준 단독으로, 이후는 실제 현재 주거의 메시지/통화로 표현한다. 구세이브의 `hyunsu_pivot` 폴백은 남기되 신규 경로에서는 중복 재생하지 않는다.
- **이사와 직업**: 고시원 퇴실의 모든 선택은 실제 현재 주거의 첫날밤으로 즉시 이어진다. 생존직은 사무실 루틴을 볼 수 없고, 사무직만 사무실 다리의 후보가 된다.
- **효과 불변**: 첫 월급·직장·심야 루틴은 비차단 다리로 내려도 원래 효과·플래그·쿨다운을 적용한다. 유일한 경제 정정은 캔맥주 한 개 30,000원→3,800원이며 장기 경제는 바꾸지 않는다.
- **증거의 한계**: 대표 A/B의 1장 27/29 정지, 7체인, 고립 단편 0, 한영 8컷은 시간·공간·표면 회귀 증거다. 사용자 Demo Round 2의 감흥 NO-GO를 자동으로 뒤집지 않는다.

## 2026-07-18 (몇 주 뒤의 인과는 즉시 체인으로 위장하지 않고, 예약 큐는 사건을 잃지 않는다)

- **3장 네 시간축**: `지연의 재연락과 이탈`, `재혁의 제안과 거울`, `아버지 고백과 상철의 진실`, `강남 목표의 반환점과 영수증`이 97~144주의 정본 시간축이다. `narrative_spine.json`은 정확히 네 축과 핵심 앵커를 소유한다.
- **시간 연결과 장면 연결 분리**: 같은 자리에서 계속되는 `follow_up_event`만 한 장면의 링크로 센다. 몇 주 뒤 돌아오는 `deferred_follow_up`은 시간 인과로 별도 계수하며, 정점 길이를 부풀리는 근거로 쓰지 않는다.
- **예약 큐 계약**: 같은 사건을 여러 경로가 예약하면 가장 이른 도착 주 하나만 남긴다. 같은 주에 여러 사건이 도착하면 호출당 가장 이른 한 건만 제거하고 나머지는 다음 호출까지 보존한다. MainGame의 구세이브 폴백은 같은 대상이 이미 예약돼 있으면 선점하지 않는다.
- **조건 안전성**: 원천 사건이 대상의 선행조건을 보장하지 않으면 정적 후속을 만들지 않는다. 생일은 지연을 만났음을 보장하지 않고, 상철의 알려진 제안은 재혁의 사후처리를 보장하지 않으므로 기존 조건 스케줄러가 연결을 소유한다.
- **증거의 한계**: A/B 17·23 앵커, 시간 연결 15·20개, 3장 고립 단편 0, KO/EN 22컷과 잼 0은 구조·표면 회귀 증거다. 사용자의 감흥 NO-GO를 자동으로 뒤집지 않는다.

## 2026-07-18 (데모 판정은 전체 대서사 편집을 막지 않으며, 결과 장소는 소리까지 함께 이동한다)

- **운영 우선순위**: 사용자 지시에 따라 Demo Round 2의 인간 재미 판정은 계속 NO-GO로 남기되, 그 판정을 기다리느라 이미 승인된 전 범위 인과 편집과 출시 차단 결함 수리를 멈추지 않는다. 자동 통과를 재미 GO로 바꾸지는 않는다.
- **2장 정본**: 2장은 `1년 장부→돈의 구조→상철의 인맥`, `다은의 질문→아버지 약→지연의 제안`, `사람이 여는 문→부모의 방문→아버지 응급실`, `상철의 거울→월급의 천장→병실 문` 네 개의 3링크 연쇄로 읽힌다. 신규 단편이나 경제 보상을 더하지 않고 기존 ID·효과·플래그를 마지막 결정에 보존한다.
- **결과 공간 계약**: 선택으로 배경이 바뀌면 선택지 원본의 `result_ambience`가 같은 프레임에 환경음만 전환한다. 기존 BGM은 재시작하지 않으며, `current_housing`은 실제 저장된 주거의 룸톤으로 해석한다. EN 오버레이는 계속 text-only다.
- **증거와 한계**: 대표 두 경로의 2장은 24회 정지·4체인·2정점·고립 단편 2개이며 KO/EN 18컷씩 통과했다. 이는 인과·화면·오디오 회귀 증거이지 감흥이나 장편 독해 만족도의 증거가 아니다.

## 2026-07-18 (240주 편성은 52개의 직접 결정으로 고정하고, 플레이타임 추정은 재미 판정과 분리한다)

- **편성 정본**: 직접 Decision/Boss는 전체 52회, 장별 `12 / 10 / 10 / 10 / 10`이다. 데모 보스 2회와 정식 보스 5회, Echo 20회를 사용한다. 결산 주차는 21개지만 주 240의 마지막 결산은 엔딩 기록에 흡수되므로 차단 모달은 20개다. 나머지 주는 기존 루틴·경제·콜백을 자동 실행한다.
- **위기와 잠금**: 건강·정신·월세 조달 위기는 주 시작 시 Quiet/Echo를 Decision으로 승격할 수 있다. 한 번 정한 주차 유형은 같은 주의 자동 정산이나 StoryMode 왕복 뒤에 재판정하지 않는다.
- **콘텐츠 노출**: 한 런이 1,177개 랜덤 풀을 소모하도록 만들지 않는다. 1~5장 첫 인과 재편 뒤 대표 경로는 데모 이후 27~29회의 랜덤 전경 기회를 가진다. 감사 계약은 24~36회이면서 각 장 최소 1회를 요구해 회차 차이는 남기되, 작성형 정점을 밀어내려고 뜬금없는 랜덤 사건을 끼워 넣지 않는다.
- **측정의 한계**: 현재 고정 독해 모델의 2시간선 주 107~110과 전체 210.5~235.6분은 회귀 비교용 구조 추정이다. 실제 환불선·몰입·재미는 사람의 정상 독해 플레이로만 판정한다.
- **저장 호환**: 구리듬 저장은 현재 주의 낡은 유형 잠금만 제거하고 모든 경제·관계·서사 상태를 유지한다. 현 버전 데모 저장은 정식판으로 이월한다.

## 2026-07-16 (5년의 결말은 최종 직함보다 실제로 걸은 길을 먼저 말한다)

- **종결 정체성 우선**: 38세 결산에서 평판 뒤 정석·비정석·은퇴·투자·안정·균형 경로를 일반 `career_climber`보다 먼저 판정한다. 직장을 유지했다는 사실이 5년간 반복한 투자나 경로 선택을 덮지 않게 한다.
- **투자 달인 이중 문**: 기존 순자산 5억+·투자감각 55+ 문은 보존한다. 추가 문은 순자산 1억+·투자감각 85+·`tendency_realized=invest`·비정석 우세 15+를 모두 요구한다. 수익률 상향이 아니라 누적 행동의 결말 소유권이다.
- **창업 인수는 별도 목적지**: `startup_exit` 플래그는 NG+ 특수 결말 뒤, 일반 30억 분기 전에 전용 엔딩으로 닫힌다. 32억은 아파트 매매 수익이 아니라 팀과 회사를 넘긴 대가이므로 같은 잔고라도 마지막 장면이 달라야 한다.
- **이전 동결 해제**: 아래 2026-07-13의 `startup_exit` 라우팅 동결은 당시 판정 대기 기록으로 보존하되, 사용자 전권 위임과 ORDER-26 15,000런 진단을 근거로 이 결정이 명시적으로 대체한다. 엔딩 ID·보상·월급·수익률은 바꾸지 않는다.

## 2026-07-16 (240주는 달력으로 남기고, 플레이어에게는 40~60개의 결정을 준다)

- **전면 폐기하지 않는다**: 서울·강남이라는 소재, 기존 인물, 콜백, 1,505개 이벤트, MORAL_TINT는 보존한다. 문제는 콘텐츠 부족이 아니라 240개의 주가 같은 무게로 멈추는 편성이다.
- **시간과 입력 분리**: 내부 달력은 240주, 기본 AP는 2를 유지한다. 플레이어가 직접 멈추는 것은 전체 40~60개의 Decision/Boss 주간이며 나머지는 Quiet/Echo로 흐른다. 데모 24주는 8~10개 결정, 2개 정점, 3~5개 짧은 에코를 목표로 먼저 검증한다.
- **편성기는 비노출**: 새 자원·탭·미니게임을 추가하지 않는다. 기존 위기·아크·후속·최근 행동·관계·MORAL_TINT를 읽는 `Narrative Pressure Director` 하나가 이번 주의 압박과 3개 맥락 행동을 고른다. 정확한 확률과 moral 수치는 숨긴다.
- **월급 현실성**: 성실한 직장생활만으로 5년 안에 30억원을 벌게 만들지 않는다. 성실함은 안정, 관계, 자존, 전문성, 의미 있는 비강남 결말을 만들며, 드문 지분·공동창업·파트너십 기회의 자격이 될 수 있다. 큰 기회는 여전히 위험하다.
- **챕터와 엔딩**: 다섯 해는 각각 예고·준비·대면·후폭풍을 갖는 삶의 보스전으로 닫는다. 엔딩은 `최종 장면 → 여운 → 엔딩명 → 크레딧 → 기록/통계` 순서다. 35개 ID는 세이브·도감 호환을 위해 남기되 8~10개 감정적 피날레 패밀리로 읽히게 한다.
- **실행 게이트**: 전체 이식 전에 데모 수직 슬라이스를 KO/EN·패드·실제 입력으로 끝까지 검증한다. 재미가 증명되지 않으면 전범위 코드를 넓히지 않는다. 이 결정은 ORDER-28에 한해 기존 콘텐츠 동결을 유저가 직접 해제한 예외다.

## 2026-07-13 (AI 아트는 출처가 아니라 화면에 남는 증상으로 판정한다)

- **출시 인벤토리**: 아트 품질 감사의 모집단은 `ImageRegistry`가 실제로 노출하는 활성 CG·초상·배경의 고유 경로다. 삭제·미등록 원화는 출시 차단 대상이 아니지만 다시 등록하는 순간 판정표와 자동 게이트에 함께 들어와야 한다.
- **사람 판정 우선**: 자동 검사는 누락·중복·해상도·초상 알파·미판정 회귀만 잠근다. 손, 시선, 반복 인물의 얼굴과 의상, 한국 공간 구조, 반사, 뭉개진 글자, 그레이딩과 크롭은 콘택트시트 비교 뒤 원본 해상도로 사람이 판정한다.
- **불합격 수리**: 정본 고시원에 6면 모니터 벽을 만든 `ending_crypto_ghost`는 휴대폰 1대와 낡은 노트북 1대만 남긴 장면으로 교체한다. 본편 33~38세 민준 슬롯에는 방이 구워진 50대 원화를 쓰지 않고 투명 피로 초상을 사용한다.
- **정직한 공시**: 일부 2D 아트·서사 초안·영문 현지화에 사전 생성 AI 보조가 있었고, 출시물은 개발자가 선별·편집·수정·인게임 검수했다는 실제 범위만 KR/EN으로 밝힌다. 실행 중 AI 생성이나 외부 AI 서비스 연결은 없으며 그렇게 약속한다.
- **출시 게이트**: `docs/ART_AI_AUDIT.md`의 활성 경로가 정확히 일치하고 `FAIL/PENDING=0`이어야 전체 audit가 통과한다. 플레이테스트에서는 “AI 같았나”라고 유도하지 않고 분위기 적합도와 튄 장면의 제목/장소를 회수한다.

## 2026-07-13 (30억은 베팅 전용 문이 아니며, 첫 투자는 예측이 아니라 손실 문법부터 가르친다)

- **세 경제 경로**: 30억원 도달은 베팅, 공동창업 M&A, 재개발 사다리의 세 정책이 각각 3% 이상을 실증해야 한다. 자동 회귀는 기존 생존·직장·베팅 밴드와 함께 세 경로를 잠근다.
- **큰 보상의 조건**: 창업 32억원은 300만원 공동창업 지분 20%를 수년 뒤 160억원 기업가치로 매각하는 단일 엑싯이다. 재개발 26억원은 급매 진입과 사업 승인 성공 뒤 160주 이후 관리처분인가를 거쳐야 한다. 반복 가능한 월급·AP 보상으로 이 금액을 주지 않는다.
- **라우팅 동결(당시 결정, 2026-07-16에 대체됨)**: `startup_exit` 플래그가 있어도 32억원이면 현행 30억원 분기가 먼저 잡는다. 전용 창업 엔딩을 강남 엔딩보다 먼저 둘지, `jiyeon_man`·`second_love` 등 사실 충돌을 어떻게 고칠지는 `docs/ENDING_AUDIT.md`에 올리고 Claude 승인 전 `finish_run` 순서를 바꾸지 않는다.
- **상점 표면**: 영구 비활성 일반 소모품 상점과 그 죽은 코드를 제거한다. 생활 카테고리의 관계 선물 진열대와 소지품 사용은 별개의 활성 서사 표면이므로 유지한다. 첫 월급 토스트는 더 이상 존재하지 않는 상점 해금을 약속하지 않는다.
- **첫 투자 안내**: 첫 진입에서 현재가, 위험 등급, 수수료, 레버리지·강제청산만 한 화면에 설명한다. 추천 종목·추천 금액·미래 예측은 주지 않으며, 조회는 무료이고 주문 확정만 AP 1이라는 실제 조작 규칙을 먼저 밝힌다. 이후 용어 버튼으로 다시 확인할 수 있다.

## 2026-07-13 (준비 언어는 영어 안전망 위에 놓고, 언어 변경으로 원화 가치를 바꾸지 않는다)

- **출시/준비 분리**: KO·EN만 출시 언어다. ja·zh-CN·zh-TW는 로더와 빈 스켈레톤만 준비하며 콘텐츠 동결, 네이티브 검수, 결정론적 폰트 게이트 전에는 시작 화면이나 Steam 지원 언어로 노출하지 않는다.
- **UI 키 절충**: 기존 1,835개 `_tr(ko,en)` 호출부를 재작성하지 않는다. 대상 언어는 한국어 원문을 `ui_<lang>.json`의 키로 쓰고 누락 시 EN 폴백+미스 기록한다. 한국어 문구를 고치면 동결 이후에는 사전 키도 같은 커밋에서 옮긴다.
- **콘텐츠 폴백 순서**: KR gameplay base → 완성 EN text overlay → 대상 언어 text overlay 순이다. 준비 언어가 비어 있거나 부분 번역이어도 한국어가 외국어 화면에 새지 않는다. gameplay key는 번역 오버레이가 소유하지 않는다.
- **통화 정합**: 언어 선택은 환전이 아니다. 1억원을 `¥100,000,000`으로 바꾸는 방식은 금지하고, 원화 정체성을 유지한 채 ja=`億/万ウォン`, zh-CN=`亿/万韩元`, zh-TW=`億/萬韓元`, en=`billion/million won`으로 큰수 읽기만 현지화한다. 실제 타 도시 토탈 컨버전은 별도 가치/환율 프로필이 필요하다.
- **폰트 게이트**: Pretendard는 중국어 전체를 보증하지 않으며 macOS 시스템 폴백은 Windows/Steam Deck 증거가 아니다. Noto Sans CJK 언어별 TTF와 OFL 고지를 번들하고 플랫폼 캡처를 통과하기 전까지 중국어 출시는 금지한다.
- **번역 시작선**: 콘텐츠 동결 선언 후 ja → zh-CN → zh-TW 순서다. 언어별 용어집·호칭표, 엄격 패리티, 한글 0, 1280x800 캡처, 네이티브 스팟체크가 한 묶음이다. 상세 계약은 `docs/I18N_INFRASTRUCTURE.md`다.

## 2026-07-13 (PC 기준 빌드는 콘솔을 막지 않으며, 콘솔 약속은 승인과 포팅을 통과한 뒤 한다)

- **플랫폼 순서**: Steam/Steam Deck 데모와 PC 출시 품질을 기준 빌드로 완성한 뒤 PlayStation 5, Xbox Series, Nintendo Switch/Switch 2로 확장한다. 콘솔 동시 출시를 현재 일정의 전제로 삼지 않되, 입력·레이아웃·저장 구조는 지금부터 콘솔을 막지 않게 만든다.
- **공통 표면 계약**: 960x600 자유 창 최소선, 1280x720 휴대/TV 저해상도, 1280x800 Steam Deck, 1920x1080, 2560x1440, 3840x2160, 21:9를 검증한다. 핵심 UI는 중앙 안전영역을 유지하며 QHD/4K에서 글자와 벡터 표면은 선명하고 CG/배경은 고해상도 마스터에서 내려온다.
- **입력 의미 계약**: 게임 로직은 South=확인, East=취소, West=보조, North=상세, LB/RB=그룹 이동이라는 의미만 소유한다. `ControllerHints`가 Xbox/Steam Deck, DualSense, Nintendo의 실제 레이블을 골라 표면에 표시하며 특정 플랫폼 글자를 로직에 하드코딩하지 않는다.
- **출시 현실**: Steam Deck 외 콘솔은 플랫폼 개발자 승인, NDA, 전용 SDK/devkit, 인증, Godot 콘솔 export template 또는 포팅 파트너가 필요하다. 따라서 플랫폼 신청·포팅·인증은 PC 데모의 외부 지표를 본 뒤 별도 예산과 일정으로 승인한다.
- **게이트**: 콘솔 준비도는 마우스 구조가 아니라 패드 단독 과업, 10-foot/휴대 가독성, 안전영역, 중단·재개, 포커스 복구, 해상도별 무왜곡 캡처로 판정한다.

## 2026-07-13 (회복은 랜덤 카드 네 장이 아니라 실제 시간이 지나야 하며, 결산은 카지노 밖에서 한다)

- **시간 계약**: `gambling_rock_bottom`에서 도움을 청한 뒤 `recovery_first_week`은 1주, 유혹 시험은 추가 3주, `recovery_one_month_clean`은 거절 뒤 1주 후 예약한다. 세 후속 이벤트는 weight 0/hidden인 deferred 전용이며, 재발 선택은 마지막 회복 비트를 예약하지 않는다.
- **엔딩 진실**: `beat_addiction`이 실제 한 달 버팀을 증명하므로, 근거 없이 “100일 이상”이라고 과장하지 않는다. 엔딩 제목은 `오늘의 동그라미 / One More Circle`, 산문은 첫 서른 개 이후 오늘까지 이어지는 반복을 말한다.
- **최종 이미지**: 도박의 유혹을 네온·칩·카드로 다시 상품화하지 않는다. 정본 고시원에서 민준이 오늘의 동그라미를 긋는 얼굴·펜 손·달력만 결산 행동으로 고정하고, 아버지·상철·NG+ 인물은 조건부 기억이므로 프레임 밖에 둔다.
- **게이트**: `CGRuntimeCheck`가 1+3+1주와 재발 비예약을 잠그고, KO/EN `ending-p1`은 기본/아버지 기억 변주의 정확한 CG와 950x430 행동 크롭을 검증한다.

## 2026-07-13 (거울 정합은 생성 모델의 네 몸 맞추기가 아니라 중복 몸을 없애서 보증한다)

- **실패 인정**: `jiyeon_man`의 이전 교정본은 좌우 순서를 맞추고 자세 계약을 문서화했어도, 실제 몸과 반사상이 별개로 생성되는 순간 머리·어깨·팔선이 다른 네 사람처럼 읽혔다. 체크리스트만으로 생성 오류를 정답으로 만들 수 없다.
- **구도 해법**: 카메라를 반사축 밖에 두고 거울 안의 민준과 지연만 한 번씩 보여준다. 조명 프레임·반사된 방 깊이·세면대 가장자리로 거울임을 증명하되, 실제 등과 두 번째 얼굴은 프레임 밖에 둔다. 따라서 비교해야 할 중복 포즈 자체가 없다.
- **연기 불변**: 민준은 화면 왼쪽에서 자기 눈을 보고, 지연은 화면 오른쪽에서 민준을 본다. 둘은 수평 어깨·직립 몸통·자연스러운 성인 비율을 유지하고 손은 950x430 크롭 아래에 둔다. 렌즈 응시·추가 인물·추가 반사·카메라 노출은 실패다.
- **게이트**: `ImageRegistry`는 `ending_jiyeon_man_v2.png`를 정확히 가리키고, acting manifest·P0 비주얼 바이블·KO/EN `ending-p0` 크롭·`CGRuntimeCheck`가 반사 전용 소유권을 잠근다.

## 2026-07-13 (외로운 부자는 실제 부자여야 하고, 같은 빈집도 상실의 원인이 다르면 다른 그림이다)

- **라우팅 진실**: `lonely_rich`는 실제 30억 강남 입성 뒤 사람이 남지 않은 엔딩이다. 다은과 결별했다는 이유만으로 강남 미달 런을 “부자”라 부르거나 방 세 개 강남 집을 서술하지 않는다. 공용 `_divorce_ending_for_assets()`가 30억 이상만 `lonely_rich`, 미달은 `ordinary_life`의 전용 `daeun_divorced` 변주로 보낸다.
- **미달 결산**: 강남 미달 이혼은 둘이 살던 집을 정리한 상자, 쓰지 못한 혼인신고서, 결별 합의서, 30억에 닿지 않은 숫자를 텍스트로 회수한다. 부자 CG를 빌리지 않고 일반 결산 카드를 유지해 “다은도 버리고 강남도 얻었다”는 거짓을 막는다.
- **공통 최종 물리**: 실제 `lonely_rich`의 기본/이혼/10억 경고/비혼 계산 변주는 모두 강남 밤, 네 자리 식탁, 1인분 배달, 세 빈 의자, 뒤집은 폰으로 끝난다. 조건부 다은·부모·법적 서류는 그림에 굽지 않는다.
- **`empty_house`와 분리**: `empty_house`는 아버지의 죽음, 소파, 두 개의 쓰지 않은 컵, 봉투·열쇠, 몸을 접은 상실이다. `lonely_rich`는 식탁과 선택의 누적으로 읽힌다. 둘 다 넓은 강남 집이라는 이유로 같은 CG나 구도를 공유하지 않는다.
- **게이트**: CG 소유권·두 자산의 경로 중복 금지는 `CGRuntimeCheck`, 자세/시선은 acting manifest, 한영 기본·이혼·미달 5화면은 `--qa=ending-p1`이 잠근다.

## 2026-07-13 (거울은 같은 포즈를 증명하고, 엔딩 CG는 모든 변주의 공통 사실만 소유한다)

- **거울의 판정 단위**: 인물 좌우 순서가 맞는 것만으로는 통과가 아니다. 실제 몸과 반사상은 한 사람의 단일 자세여야 하므로 머리-척추 축, 목 각도, 어깨 높이·기울기, 몸통 방향, 팔과 몸 사이 간격을 1:1로 읽을 수 있어야 한다. 독립 포즈처럼 보인 직전 `jiyeon_man` 후보는 폐기하고, 손은 양쪽 모두 미리보기 아래로 숨긴 최종본으로 교체한다.
- **엔딩 변주 공유**: `description_if_known`이 여러 개인 엔딩 CG에는 모든 변주가 보장하는 물리만 그린다. `late_call`은 민준·창원행 KTX·겨울비·전화·뺀 이어버드만 고정하고, 아버지와 상철·재혁은 음성 또는 기억으로 프레임 밖에 둔다.
- **장소 ID의 의미**: `ktx_window`는 실내 열차 창가이고 `hometown_train_station`은 지방역 승강장이다. 한 이미지가 둘을 대신하지 않으며, 바다 데이트·아버지 전화는 실내, 명절 선택은 승강장으로 각각 잠근다.
- **게이트**: P0 거울은 KO/EN `ending-p0`, P1 전화 엔딩은 KO/EN `ending-p1`, 열차/역 구분은 KO/EN `transport` 캡처와 `CGRuntimeCheck`의 exact path로 회귀를 막는다.

## 2026-07-12 (결별은 선택 전에 예고하지 않고, 떠나는 행동이 일어날 때만 보여준다)

- **결과 소유**: 다은의 도장과 지연의 떠나는 뒷모습은 각각 `arc_daeun_final_choice_decision` 배신 선택과 `arc_jiyeon_verdict_decision` 보냄 선택의 결과 CG다. 도입·전희 선택 화면이나 관계를 지키는 반대 선택에는 절대 노출하지 않는다.
- **공간 정본**: 두 사건은 일반 강남 야경이 아니라 각자의 신혼 공간에서 일어난다. 다은은 부엌에서 저녁을 만들고 있어 선택 전 초상을 숨기며, 지연은 `jiyeon_newlywed_home`의 실내 현관에서 실제 공동복도로 나간다.
- **행동 타이밍**: 다은 CG는 결과 문단 3의 도장, 지연 CG는 결과 문단 2의 문턱 통과에 맞춰 열린다. 그림이 산문보다 먼저 결별을 말하거나 반대 선택에 남으면 실패다.
- **법적·시각 정합**: 혼인신고 전 관계에 이혼서류를 발명하지 않는다. 다은은 혼인신고서에 쓰지 못한 도장을 둘의 생활을 정리하는 빈 합의서에 찍으며, 그림과 한국어·영어 산문은 모두 다은을 행동 주체로 둔다.
- **게이트**: `BREAKUP_VISUAL_BIBLE`, 이벤트/로맨스/연기 manifest, `CGRuntimeCheck`, KO/EN `--qa=breakup`이 같은 배경·초상 유무·분기·문단·텍스처를 잠근다.

## 2026-07-12 (엔딩 CG는 최종 삶을 소유하고, 거울은 물리를 속이지 않는다)

- **최종 삶 우선**: 프로포즈·결혼·병실 방문 같은 과정 CG를 엔딩에 재사용하지 않는다. P0 여덟 엔딩은 마지막 날의 생활, 관계, 공간을 각각 한 장씩 독점한다.
- **변주 공유 조건**: 한 엔딩의 `description_if_known` 변주가 여러 개여도 공통 사실만 이미지에 굽는다. `with_daeun`의 반지는 손 각도로 숨기고, `sangchul_reckoning`의 서류는 비워 경찰 진술/직접 청산 양쪽을 허용하며, 조건부 아버지·하객은 발명하지 않는다.
- **거울 물리**: 거울은 앞뒤 축을 반사하지 인물의 수평 순서를 바꾸지 않는다. `jiyeon_man`은 현실과 반사 모두 민준=화면 왼쪽, 지연=화면 오른쪽을 유지하고 카메라는 반사축 밖에 둔다. 좌우만 맞고 팔·어깨 자세가 다른 반사도 실패다. 두 사람은 현실/반사에서 같은 수평 어깨·직립 머리·정면 몸통·내린 팔을 유지하고, 손은 양쪽 모두 세면대 아래로 완전히 숨긴다. 반사 순서를 바꾼 버전, 팔 자세가 다른 버전, 실제 몸만 고개를 돌린 버전, 지연의 반사 고개만 돌아간 버전은 모두 폐기한다.
- **Black 가독성**: Black은 색·망점·잉크 표면을 무너뜨리지만 얼굴·손·커튼·창·문서는 지우지 않는다. 엔딩 CG 미리보기는 일반 배경보다 그림자 중간톤을 보존하고, 본래 지나치게 어두운 두 소스는 픽셀 위치를 바꾸지 않는 노출 보정만 허용한다.
- **소유권 게이트**: CG 키는 한국어 엔딩 원본에만 두고 영어는 텍스트 오버레이를 유지한다. 각 CG는 한 엔딩만 소유하며 1280×800, 연기 manifest, `ending-p0` KO/EN 정확 경로·430px 크롭, `CGRuntimeCheck`를 모두 통과해야 한다.

## 2026-07-12 (결혼의 비용은 선택 뒤의 장면에 남아야 한다)

- **선택권 우선**: 프로포즈 반지와 수락 반응은 플레이어가 상자를 꺼내기 전에는 보이지 않는다. 선택 결과 CG도 결과 문단의 정확한 순간에만 열고, 보류 경로에는 같은 CG를 재사용하지 않는다.
- **비용의 시각적 지속**: 다은의 800만원 소형 결혼과 3,100만원 풀 패키지는 같은 결혼식 이미지로 합치지 않는다. 준비 선택이 `daeun_wedding_small/full`로 남고 다음 결혼식의 식장·드레스·조명 차이로 이어져야 돈 선택이 숫자에서 끝나지 않는다.
- **변하지 않는 결산**: 어느 패키지를 고르든 꽉 찬 신부석과 드문 신랑석은 유지한다. 화려한 지출은 민준이 5년간 사람에게 쓰지 않은 시간을 채워 주지 못한다.
- **조건부 인물 분리**: 아버지·현수·다은 어머니처럼 생존/재회 플래그에 따라 달라지는 하객은 고정 CG에 식별 가능한 얼굴로 굽지 않는다. 산문 변주는 유지하고 배경 하객은 익명으로 남긴다.
- **지연 장면의 경계**: `arc_jiyeon_wedding_gap` 체인은 선택 전 계급 압력이지 프로포즈나 완성된 결혼식이 아니다. 따라서 웨딩드레스나 결과 하객을 보여주지 않고, 아이보리 플래닝 수트·장인·불균형한 빈 카드만 보여준다. 최종 비용·플래그·호감도 효과는 마지막 링크에서만 적용된다.
- **런타임·호환성**: 선택별 `result_cg_reveal_paragraph`, 이벤트 `cg_if_known`, 레거시 소형 결혼식 기본값을 데이터 계약으로 사용한다. 카메라·시선·손·핵심 오브젝트는 연기 manifest와 KO/EN 1280×800 캡처를 모두 통과해야 한다.

## 2026-07-12 (장소는 배경으로, 몸동작은 CG로 증명한다)

- **분류 기준**: 지갑 정류장과 붕어빵 노점은 민준을 굽지 않은 전용 배경이다. 반복 인물의 동일성이 필요 없고 장소·소품의 물리가 장면의 핵심이기 때문이다. 반면 설날은 `세배한다`는 몸동작 자체가 사건이라 배경+초상 조합이 아니라 1회성 CG를 쓴다.
- **정류장 정본(최종 교정)**: 벤치는 연석과 평행하고 좌석이 유리 너머 도로를 향한다. 따라서 카메라에는 좌판 앞면이 아니라 등받이 뒷면과 후면 지지대가 보여야 한다. 도로·점자블록·연석을 함께 보여 장소를 잃지 않으며, 지갑은 벤치 아래에서 실제 1280×800 대사창 위에 남아야 한다. 좌판이 카메라 쪽으로 뻗던 버전은 승객이 도로를 등지는 구조라 폐기했다.
- **노점 정본**: 겨울 서울 골목의 실물 붕어빵 틀, 완성 빵, 종이봉투, 어묵 국물과 김이 초상에 가리지 않는다. 상인은 익명 등/옆 실루엣으로만 두고 이름 있는 인물처럼 선명하게 만들지 않는다.
- **설날 정본**: 기존 창원 집 구조와 민준·아버지 얼굴/계급 신호를 유지한다. 부모는 분리 상태이므로 어머니는 등장하지 않고 아버지·친척 두 명만 민준의 세배를 본다. 민준은 두 무릎을 꿇고 겹친 손등 쪽으로 이마를 낮추며, 네 사람의 시선은 렌즈가 아니라 세배에 모인다.
- **게이트**: 장소/초상/CG/월을 `event_visual_contracts.json`에서 잠그고, CG 카메라·시선·몸 연기는 `cg_acting_manifest.json`에서 잠근다. KO/EN 대사와 선택지 크롭, 장소 앰비언스를 모두 통과해야 부채를 제거한다.

## 2026-07-12 (카페 폴더 주인과 김 부장은 한 사람이 아니다)

- **서사 정본**: `카페의 남자`는 폴더 주인이다. 그는 민준의 허세를 시험하고 정직하면 명함과 깨끗한 기회를 준다. `김 부장`은 그 폴더 안에 적힌 전화번호의 소유자다. 민준을 기억하지 못하고 재개발 입주권에 수수료 거품을 붙이는 별도 브로커다.
- **시각 정본**: 폴더 주인=`cafe_investor`(39세, 마른 각진 얼굴, 청록 오픈칼라, 차콜 정장, 검은 서류철). 김 부장=`cafe_broker_kim`(45세, 넓은 체형과 둥근 얼굴, 볼 점, 타우프 재킷, 버건디 니트, 휴대폰). 팀장·상철·재혁 얼굴을 어느 쪽에도 재사용하지 않는다.
- **등장 타이밍**: 폴더를 읽는 동안에는 인물을 숨긴다. 그가 돌아와 그림자를 드리우는 문단에서만 초상과 이름표를 reveal한다. 장기 콜백은 플래그가 아니라 실제 대사 주체에 따라 두 ID 중 하나를 쓴다.
- **번역 정본**: `카페의 남자`=`Man at the Cafe`, `김 부장`=`Manager Kim`. `Kim Bujang`을 혼용하지 않는다. 김 부장의 정제된 후속 제안은 “브로커 없이”가 아니라 “수수료 없이/No commission”다.
- **계약**: 최초 체인, 핵심 콜백, 명시 재등장 이벤트는 `event_visual_contracts.json`에서 배경·초상·문단 reveal을 잠근다. 새 콜백을 쓸 때 “그 남자”가 어느 쪽인지 먼저 결정해야 한다.

## 2026-07-12 (인물의 시간 경과는 같은 얼굴과 달라진 생활로 보여준다)

- **동일성 잠금**: 현수의 공시생·회계직·공무원 초상은 둥근 안경, 통통한 얼굴과 체형, 헝클어진 흑발, 절제된 미소를 공유한다. 직업 변주는 새 인물을 만드는 일이 아니라 같은 사람이 자리를 잡은 시간을 보여주는 일이다.
- **생활 단계 분리**: 터미널 막차까지는 후드 차림 공시생이다. 회계 경로는 남색 정장·버건디 타이·빈 명함, 공무원 합격 경로는 실용 재킷·연한 셔츠·글자 없는 명찰을 쓴다. 로고·기관명·가짜 글자는 넣지 않는다.
- **조건부 초상 계약**: 이벤트의 `portrait_if_known`은 `description_if_known`과 같은 첫 플래그 우선순위로 해석한다. 지문이 공무원 경로를 말하면서 회계 정장을 보여주는 식의 분리를 허용하지 않는다.
- **결과 장소 계약**: 전화 장면은 민준이 있는 방을 유지하고, 실제로 배웅을 선택한 결과에서만 터미널로 전환한다. `result_background` 전환은 BGM을 재시작하지 않고 장소 앰비언스를 함께 갱신해야 한다.
- **배경/인물 분리**: 터미널은 사람 없는 재사용 배경이고 현수는 투명 초상이다. 버스 문은 승강장을 향하며 차량 차로와 보행 동선이 물리적으로 이어져야 한다.

## 2026-07-12 (AP의 정본은 사물 그림이 아니라 실제 게임 세계의 스틸컷이다)

- **문제 재판정**: PNG 아틀라스를 넣어도 서류·휴대폰·책 같은 정물만 작게 보이면 플레이어는 여전히 픽토그램으로 읽는다. 파일 형식이 아니라 장면·크기·세계 연결이 문제였다.
- **정본**: 메인/하위 AP는 해당 행동이 열어 주는 실제 장소 또는 사건의 스틸을 사용한다. 면접은 면접실, 휴식은 고시원, 운동은 헬스장, 명상은 한강, 도박은 실제 경마장·홀덤룸·카지노 입구를 보여준다.
- **카드 문법**: 메인 주간 카드는 왼쪽 이미지를 168x106으로 카드 높이 전체에 붙인 목적지 프레임으로 사용한다. 하위 모달은 128x64 가로 스트립으로 밀도를 유지한다. 네모 프레임 안 작은 기호로 되돌리지 않는다.
- **기능 기호 분리**: SVG 픽토그램은 AP 비용, 잠금, 포커스, 뒤로가기와 진짜 이미지 누락 폴백에만 쓴다. 행동의 매력과 세계관을 설명하는 역할은 스틸컷이 맡는다.
- **Moral 계약**: Gray에서 장소와 제한된 색이 1초 안에 읽혀야 한다. Black은 같은 장면을 탈색·부식하고 White는 맑게 하지만, 어떤 밴드도 내비게이션 이미지를 검은 덩어리로 만들 수 없다.
- **이전 결정 정정**: 같은 날짜의 ‘오브젝트 중심 AP 아틀라스/장면 스트립’ 결정은 제작 경위로 남기되 런타임 정본에서는 이 결정이 우선한다.

## 2026-07-12 (AP 선택은 픽토그램이 아니라 장면 스트립이다)

- **진단**: 일러스트 파일을 넣어도 74x74/76x48 작은 프레임에 가두면 플레이어는 여전히 픽토그램으로 인식한다. 자산 교체만으로 웹 메뉴 감각은 사라지지 않는다.
- **표현 단위**: 주간 보드는 112x84, 하위 모달은 108x56의 가로 장면 스트립을 사용한다. 사람·도박·루틴·데이트와 경마·홀덤·스캘핑·카지노도 전용 사분면을 가져 고빈도 AP 경로에서 SVG를 제거한다.
- **기능 아이콘 분리**: 저장·닫기·루틴 내부 종류처럼 작은 조작 표지는 선형 아이콘을 유지한다. 삶의 행동을 고르는 카드만 이미지가 되고, 이미지가 기능 기호 역할까지 침범하지 않는다.
- **작은 이미지 노출**: 전체 배경용 Moral 셰이더는 썸네일 암부를 뭉개므로 전용 재질을 쓴다. Gray는 중간톤과 소품을 읽히게 하고, Black은 탈색·잉크 손상, White는 원본 색 회복을 유지한다.

## 2026-07-12 (상철의 인맥은 빈 고급 방이 아니라 사람의 망으로 보여준다)

- **공간**: 공개 고깃집이나 빈 기업 회의실이 아니라, 두 테이블 그룹이 명함을 주고받는 강남 레스토랑 개인실을 사용한다. 의자·식기·통로·창의 물리가 먼저 맞아야 한다.
- **배경 인물**: 이 장면은 모임이므로 무인 배경이 오히려 오류다. 다만 사업가들은 등·옆얼굴 위주의 익명 실루엣으로만 두고, 이름·고유 의상·선명한 얼굴을 가진 상철은 별도 초상 레이어가 독점한다.
- **구도**: 명함 교환과 정상 네트워크는 왼쪽/중앙, 그림자 투자자 그룹은 안쪽, 상철 초상은 비어 있는 우측에 놓는다. 두 선택지는 같은 프레임 안의 두 집단과 대응한다.
- **소리**: 공개 회식 소음이나 고시원 방음이 아니라 낮은 대화·잔·식기 질감에 가까운 `cafe` 앰비언스를 사용하고 자동 단언한다.

## 2026-07-12 (연말 장면은 같은 서울의 시간 변주이며 계절을 암묵적으로 두지 않는다)

- **공간 연속성**: Year 2는 기존 한파 골목의 같은 건축과 도로를 밤으로, Year 3은 기존 한강 산책로를 잎이 진 12월로, Year 4는 기존 옥상의 문·물탱크·난간·스카이라인을 유지하면서 빨래와 화분을 제거한 겨울로 변주한다.
- **인물 분리**: 세 배경에는 주인공을 굽지 않고 `player_cold_snap`을 별도 레이어로 사용한다. 같은 겨울 코트가 34-36세의 연말 연속성을 만들며 직업에 따라 야외 옷이 정장으로 바뀌지 않는다.
- **계절 이중 잠금**: 메인 디스패처의 턴 창구만 믿지 않고 네 연말 JSON 모두 `month:[12]`를 가진다. 비주얼 계약은 배경·초상·월을 함께 검사한다.
- **오디오 문맥**: 거리=서울 거리, 한강=한강, 옥상=서울 거리 앰비언스로 잠근다. 한국어 한 글자 `비` 매칭은 `비교하면`을 비로 오인하므로 실제 우천 조사/어절만 허용한다.

## 2026-07-12 (하위 AP 행동도 결과 축이 아니라 행동 판타지로 구분한다)

- **전용 12종**: 자소서·모의면접·부업·절약, 시장분석·창업·콘텐츠·이사, 독서·운동·명상·투자공부에 각각 다른 오브젝트 장면을 배정했다.
- **구분 기준**: 돈을 올리는가가 아니라 플레이어가 무엇을 하는가를 그린다. 투자=휴대폰 시세, 분석=인쇄 차트와 확대경, 부업=배달 가방, 절약=꺼진 멀티탭과 도시락처럼 실루엣부터 달라야 한다.
- **작은 화면 계약**: 76x48 크롭에서 사람·문자 없이 2~4개 소품으로 읽혀야 한다. 원본의 장식적 완성도보다 Steam Deck에서 행동이 1초 안에 구분되는지가 통과 기준이다.
- **회귀 게이트**: 한영 AP QA는 커리어 3종, 돈 4종, 자기계발 4종, 이사 1종의 실제 아틀라스 경로와 서로 다른 사분면 수를 검사한다.

## 2026-07-12 (반복 AP 행동은 클립아트가 아니라 세계의 작은 장면이다)

- **역할 분리**: 저장·뒤로가기·HUD 상태처럼 즉시 조작을 뜻하는 요소는 단색 기능 아이콘을 쓴다. 일·돈·자기계발·휴식처럼 플레이어가 매주 삶으로 고르는 핵심 행동은 오브젝트 중심 미니 일러스트를 쓴다.
- **첫 정본 4종**: 이력서와 사원증, 시세가 열린 휴대폰, 책·노트·운동 도구, 고시원 창·이불·물컵을 한 아틀라스에 고정했다. 사람·문자·로고를 빼고 76x48 크롭에서도 한눈에 읽히게 한다.
- **재사용 금지**: 돈 축이라는 이유만으로 투자 이미지를 부업·절약·시장분석에 반복하지 않는다. 전용 그림이 없는 하위 행동은 기존의 서로 다른 기호를 유지한다. 같은 이미지 반복은 픽토그램보다 더 빠르게 템플릿 UI처럼 보인다.
- **모랄 소속**: 행동 일러스트는 HUD 장식이 아니라 민준이 보는 세계의 일부다. Black/White 그레이딩을 받되 카드 크기·배치·패드 포커스는 어느 밴드에서도 흔들리지 않는다.

## 2026-07-12 (Black의 금빛은 보상이 아니라 남아 버린 유일한 색이다)

- **금빛의 대상**: Black에서 금속성 금빛을 허용하는 상시 UI는 현재 현금·총자산·목표액뿐이다. 버튼, 패널, 관계, 건강까지 금색으로 물들이지 않는다.
- **빛의 리듬**: 평소에는 작은 글자에서도 읽히는 단단한 금속색을 사용하고, 실제 자산 변화 때만 기존 값 변화 펄스로 짧게 번쩍인다. 상시 반짝임·큰 블룸·카지노식 파티클은 금지한다.
- **명도 불변**: Black은 화면과 얼굴을 못 보게 만드는 저노출 모드가 아니다. 조작면 회전·밀림과 과한 암막을 제거하고 탈색, 잉크 번짐, 계조 손상, 사람의 후퇴로 무너뜨린다. White도 흰 막을 더하지 않고 원본 색과 생활 디테일을 회복한다.
- **주간 지각**: 핵심 아크 다섯 장면에만 있던 지각 변주를 반복 AP 루프에도 드물게 배치한다. Black은 답하지 않은 이름보다 잔액·입금일·비용을 먼저 보고, White는 계좌보다 하지 못한 말·얼굴·도시 소리를 먼저 본다. 사실과 조작 정보는 어느 밴드에서도 동일하다.
- **이유**: 타락을 단순 암전으로 그리면 플레이어는 불편함만 느낀다. 세상은 읽히는데 돈만 유혹적으로 살아 있고 민준의 관찰 문장이 달라져야, 돈을 밝히는 인격 변화가 설명 없이 전달된다.

## 2026-07-12 (이벤트 비주얼은 추론이 아니라 계약이며, Gray는 Black의 약한 판이 아니다)

- **장면 계약**: 계절·장소·고정 의상이 지문에 명시된 이벤트는 `assets/event_visual_contracts.json`에서 배경·초상·허용 월을 잠근다. 태그 추론은 무명 일상 이벤트의 폴백일 뿐, 연말·가족·콜백·정점 장면의 정본이 될 수 없다.
- **부채의 가시화**: 지금 없는 그림을 비슷한 장소나 다른 인물로 덮지 않는다. 정상 감사는 제작 예정 부채를 공개하고 회귀는 막으며, 완료 선언은 `--strict` 부채 0에서만 가능하다.
- **잘못된 얼굴보다 무인 장면**: 반복 인물의 전용 초상이 없으면 배경과 지문만 보여준다. 회사 팀장을 카페 브로커로 재사용하는 것처럼 이름표와 얼굴이 다른 정본 인물을 가리키는 상태는 임시 재사용이 아니라 P1 동일성 오류다.
- **Gray 노출 계약**: 시작 상태는 차가운 기록 사진이지만 장소 구조와 제한된 원본 색이 읽혀야 한다. Gray를 거의 Black처럼 암전하면 이후 붕괴의 거리가 사라지고 제작한 배경도 보이지 않는다. Black은 탈색·표면 손상·사람의 후퇴, White는 실제 색·공간·생활음 회복으로 갈라지며 조작 UI는 안정적으로 남는다.

## 2026-07-12 (모랄 결과판은 사실이 아니라 주의 순서를 바꾼다)

- **사실 보존**: 동일한 선택은 어느 밴드에서도 같은 돈·능력치·관계 변화를 보여준다. 모랄 연출은 결과를 숨기거나 수치를 조작하지 않고, 민준이 무엇을 먼저 보고 무엇을 뒤늦게 알아차리는지만 바꾼다.
- **Black의 주의**: 돈·월수입·투자 감각·업무 성과·평판이 먼저 등장하고 마른 장부 접점음이 붙는다. 건강·정신·중독·사람의 관계는 뒤늦고 조금 흐리지만, 결과가 네 칸을 넘더라도 인간적 대가 최소 한 칸은 반드시 남긴다.
- **White의 주의**: 인물 관계와 건강·정신·사회성이 먼저 등장하고 천·호흡 같은 작은 생활음이 붙는다. 경제적 대가는 뒤늦고 조금 흐리지만 최소 한 칸을 반드시 남긴다. White는 손해를 감추는 보상 스킨이 아니다.
- **Gray의 주의**: 게임의 물질적 전제인 돈을 먼저 두되 관계를 그다음에 끼우고 모든 결과를 같은 강도로 보여준다. 어떤 항목도 도덕적 강조를 받지 않는다.
- **시간이 문법**: 첫 결과는 0.1초 먼저 도착하고 나머지는 순차적으로 열린다. 카드 제목·버튼·가독성은 안정적으로 유지하며 `Black`, `White`, 도덕 점수 같은 설명 라벨은 노출하지 않는다.
- **관계도 손익이다**: 결과의 `대가 있음/손실/성장` 판정은 능력치뿐 아니라 호감도 증감도 계산한다. 돈을 잃고 사람을 얻은 선택을 단순 손실이라고 부르면 주제 자체가 돈의 언어에 패배한다.

## 2026-07-12 (밴드 경계는 같은 방의 다른 얼굴로 기억한다)

- **고정 기억 프레임**: 모랄 밴드를 넘는 짧은 비네트는 현재 집이나 직업을 재현하지 않는다. 게임 시작의 고시원, 검은 크루넥, 같은 카메라로 잠깐 돌아가 민준의 얼굴만 비교한다. 이는 실제 이사가 취소된 장면이 아니라 자기 인식의 기억 프레임이다.
- **얼굴 연기 문법**: Deep Black은 굳고 정면을 겨누는 시선, Light Black은 아래로 처진 피로, Gray는 무표정한 중립, Light/Deep White는 억지 환희가 아닌 작은 미소를 쓴다. White 두 단계의 차이는 더 큰 웃음이 아니라 회복된 색과 생활 앰비언스로 만든다.
- **노출 고정**: 경계 비네트의 방은 모든 밴드에서 같은 배경 알파를 사용하고 장면 종료 시 일반 대시보드 값으로 복구한다. 비교 대상은 밝기가 아니라 표정·시선·색·소리다.
- **금지**: 현재 직업 정장, 다른 방, 악당 같은 일그러진 웃음, 성인(聖人) 후광을 경계 컷에 넣지 않는다. 같은 사람의 변화가 아니라 다른 캐릭터나 보상 스킨처럼 보이면 실패다.
- **이유**: “거울 속 얼굴이 낯설었다”는 문장이 일반 직업 초상과 어두운 필터 위에만 남으면 인격 붕괴가 설명문으로 끝난다. 반복 가능한 동일 프레임의 배우 연기가 그 문장을 실제 기억으로 만든다.

## 2026-07-11 (모랄은 노출값이 아니라 주의·관계·기억의 붕괴다)

- **명도는 가독성 계약**: Gray와 Black에서도 장소 구조·얼굴·시선·손동작을 읽을 수 있어야 한다. 전역 암전으로 선악을 표시하지 않는다.
- **Black의 주연**: 탈색·잉크 번짐·가장자리 손상·계조 압축·사람의 후퇴가 누적되고 돈 HUD만 반대로 또렷해진다. 문장·시선·오디오 변주는 정점 장면부터 단계적으로 소유한다.
- **White의 주연**: 흰 막이나 밝은 버튼이 아니라 실제 색·사람의 온기·공간 깊이·생활 소리가 돌아온다. 조작 UI는 어느 루트에서도 안정적으로 유지한다.
- **선택지 화면**: 선택 순간 기존 대화창을 접고 하단 선택 도크 하나만 사용한다. 배경/CG 위에 대화창과 선택지를 이중으로 쌓지 않는다.
- **부와 타락 분리**: 돈을 벌었다고 Black이 되는 것이 아니다. 착취·외면·배신이 민준의 주의를 사람에서 가격으로 옮긴다. Black은 더 부유하고 아름다울 수도 있지만 사람만 주변부로 밀려나야 한다.
- **지각 변주 문법**: `description_if_moral`과 `text_if_moral`은 같은 사건·같은 행동의 사실관계를 유지하고 관찰 대상과 자기합리화만 바꾼다. `description_if_known`의 구체적 기억이 항상 먼저다.
- **이유**: 만든 배경을 지우는 필터는 제작비를 버릴 뿐 아니라 “악함=어두운 화면, 선함=밝은 버튼”으로 주제를 축소한다. 플레이어가 느껴야 할 것은 화면 밝기가 아니라 민준이 사람과 세계를 보는 방식의 변화다.

## 2026-07-11 (첫날밤은 별도 집·동일 실내복·아침 문단 공개로 고정한다)

- **집은 관계와 계급을 말한다**: 다은과 지연의 첫날밤에 공용 `apartment`를 쓰지 않는다. 다은은 작은 서울 1.5룸형 신혼집, 지연은 재료와 비례로 부를 보여주는 고층 주거를 각각 소유한다.
- **계절보다 장면 연속성**: 두 장면은 실내 전용이라 전 계절 사용 가능하다. 대신 첫날밤 초상과 다음 날 CG는 같은 실내복을 유지하며, 이를 야외 사계절 의상 완비로 오해하지 않는다.
- **공개 시점**: 선택 직후 밤 결과 문단 0까지 신혼집과 초상을 유지한다. `result_cg_reveal_paragraph=1`에서 “다음 날 아침” 지문과 함께 CG를 연다.
- **POV 연기**: 민준은 그림에 중복 등장하지 않고 카메라가 그의 눈이 된다. 다은은 팬/계란말이를 보고 두 손으로 하나의 조리 행동을 완성한다. 지연은 민준의 시선을 알아챘기 때문에만 렌즈를 보며, 한 손으로 이불을 올리고 다른 손은 숨긴다.
- **시간 표면**: StoryMode의 남은 시간은 정수 나이 기반 `N년`이 아니라 메인 화면과 같은 월 계산을 쓴다. 첫날밤 QA는 다은 t200=11개월, 지연 t205=9개월 상태로 재현한다.

## 2026-07-11 (계절 의상은 월이 아니라 장면 계약으로 보장한다)

- **전역 갈아입히기 금지**: 달만 보고 모든 초상을 바꾸지 않는다. 실내외·직업·장소·날씨·연속 문단을 함께 보고 이벤트가 장면 전용 의상을 소유한다.
- **시골의 이틀**: 1편은 6~8월에만 시작하고 놓치면 다음 여름으로 이월한다. 시작된 2편은 월 경계와 무관하게 끝낸다. 기차·어머니 집·밤 버스는 동일한 연녹색 여름 사복을 공유한다.
- **결과 문단 공개**: 두 선택지가 공유하는 밤 버스 CG는 이벤트 루트 `result_cg`가 소유하며, `result_cg_reveal_paragraph=1`에서만 열린다. 밥상 결과 첫 문단부터 버스를 미리 보여주지 않는다.
- **외투 이월**: 지연의 좁은 방 문 앞 긴 코트는 1~4월 또는 10~12월에만 시작한다. 장면이 시작된 뒤 실내 후속편은 계절 경계와 무관하게 이어진다.
- **남은 부채**: 민준의 일반 초상은 직업·감정 기반이고 조연 다수는 단일 의상이다. 겨울 첫눈과 명시적 외부 장면부터 장면 계약을 확장하며, 현재 상태를 “전 인물 사계절 완료”라고 부르지 않는다.

## 2026-07-11 (결과 CG는 선택 뒤에, 고정 외투는 계절 안에서)

- **선택 결과 비주얼**: 선택으로만 성립하는 사진·키스·파괴·이탈 CG는 이벤트 루트 `cg`로 미리 보여주지 않는다. 선택지의 `result_cg` 또는 `result_background`가 결과 문단과 함께 전환하며, 둘을 동시에 지정하면 감사 실패다.
- **실제 UI 크롭 우선**: 원본 CG 구도가 아름다워도 1280×800 StoryMode에서 얼굴·손·핵심 행동이 선택지/결과판/대사창 뒤에 가리면 실패다. 지연 네 컷은 이 원칙 때문에 2×2에서 상단 가로 4분할로 재구성했다.
- **표정은 신원과 분리**: 기본 초상은 얼굴·헤어·나이·계급 신호만 잠근다. 우울한 기본 표정을 성공·친절·데이트 장면에 복제하지 않으며, 각 CG의 `cg_acting_manifest`가 민준을 포함한 장면별 표정을 명시한다.
- **계절 이월**: 고정 외투를 가진 남산·놀이동산 마일스톤은 서울의 온화한 달(3~5월, 9~11월)에만 발동한다. 임계 데이트 회차를 다른 계절에 넘기면 콘텐츠를 소멸시키지 않고 다음 적합한 데이트로 이월한다.
- **물리 구조 게이트**: 배경은 분위기보다 실제 사용 동선을 먼저 맞춘다. 2인 네 컷 부스는 벤치와 카메라/모니터가 반대 벽에서 서로 마주보고, 측면 입구가 그 축을 방해하지 않아야 한다.

## 2026-07-11 (장소 이동은 문단에 맞추고, 연속 스토리는 덮개 아래서 잇는다)

- **문단별 장소 규칙**: 케이블카→식당→전망대처럼 한 이벤트 안에서 실제 장소가 바뀌면 대표 배경 한 장으로 뭉개지 않는다. KR 원본 이벤트의 `paragraph_backgrounds`가 본문 문단과 1:1로 배경 ID를 소유하며, 수량 불일치와 런타임 순서 오류는 감사 실패다.
- **배경/CG 분리**: 이동·식사·전망대는 인물 없는 재사용 배경과 특별 초상을 사용하고, 선택의 감정 정점인 자물쇠 장면만 히로인별 1회성 CG를 사용한다.
- **연속 StoryMode 규칙**: 한 아크가 끝난 직후 같은 주에 다음 아크가 이미 예정돼 있으면 `MainGame`을 페이드인하지 않는다. 전환 덮개를 유지한 채 다음 StoryMode로 직행하고, 실제 스토리 큐가 끝난 경우에만 AP 화면을 공개한다.
- **이유**: 지문과 다른 장소, 혹은 두 스토리 사이 0.3초짜리 AP 화면은 기능상 오류가 아니어도 플레이어에게 AI 목업과 장면 로딩 버그로 보인다.

## 2026-07-11 (언어는 첫 장면보다 먼저, 기본 이름은 표시 언어를 따른다)

- **결정**: 언어를 고르기 전에는 현지화된 퍼블리셔 스플래시·오프닝·StartMenu를 재생하지 않는다. 기존 사용자도 새 `language_gate_seen` 키가 없으면 한 번 선택하고, 이후 실행은 저장 언어로 바로 진입한다.
- **입력 문법**: 최초 화면의 포커스 대상은 `한국어`와 `English` 두 개뿐이다. OS/저장 언어 쪽에 기본 포커스를 두고 패드 South/키보드/마우스가 동일하게 확정한다.
- **이름 규칙**: 정본 기본 이름 `김민준`/`Kim Minjun`은 세이브에 어느 형태로 남아 있든 현재 표시 언어를 따른다. 사용자가 직접 정한 이름은 번역하거나 덮어쓰지 않는다. NPC 이름표는 `ImageRegistry.get_person_info()` 한 경로에서 언어화한다.
- **이유**: 언어를 고르기 전에 영어 화면이 잠깐 보이거나, 한국어 UI 속 주인공 이름만 영어로 남는 순간은 번역 문제가 아니라 첫 신뢰를 깨는 출시 P0 결함이다.

## 2026-07-10 (CG 시선은 미감이 아니라 장면 계약)

- **결정**: 모든 활성 CG는 생성 전에 `camera_role`, 인물별 `gaze_target`, 그 시선을 지지하는 `pose_support`, 장면의 현재 동사를 선언한다. 눈동자만 상대에게 돌리고 몸은 카탈로그 포즈인 이미지는 실패다.
- **렌즈 응시 규칙**: 카메라가 민준의 눈인 명시적 `player_pov`에서만 허용한다. 2인 장면은 상호 시선 또는 각자 명시된 사물/풍경 목표가 있어야 하며, 초점 없는 정면 미소는 AI 티가 가장 크게 나는 결함으로 취급한다.
- **런타임 게이트**: `assets/cg_acting_manifest.json`은 `ImageRegistry.CG`의 활성 집합과 정확히 일치해야 한다. 새 CG를 추가하거나 경로를 교체하면서 카메라·시선·몸 연기 계약을 갱신하지 않으면 `tools/cg_acting_contract_check.py`와 통합 감사가 실패한다.
- **초상화 문법**: StoryMode 초상은 화면 오른쪽에 놓이므로 기본 대화 초상은 화면 왼쪽의 플레이어·대사 공간을 향한다. 이벤트 문장이 풍경/사물을 명시하면 그 목표로 시선을 바꾼다.

## 2026-07-10 (VN 클라이맥스 컬러 계층 + 반복 차량 정본)

- **전체 일본 미연시풍으로 갈아엎지 않는다.** 강남드림의 고유 자산은 한국 사회 현실과 `Gangnam Ink`의 절제된 기록 질감이다. 대신 일본 VN의 강점인 얼굴 판독성, 시선선, 장면 hold, CG reveal 문법을 핵심 장면에 차용한다. 결과는 **한국 사회 리얼리티 VN + 일본 VN식 장면 가독성**이다.
- **일상과 클라이맥스의 시각 계층을 분리한다.** 일반 배경·HUD는 저채도와 잉크 질감을 유지하고, 로맨스·엔딩·폭로 같은 독립 CG는 Gray에서도 의도된 색을 보존한다. Black에서는 같은 기억의 색과 공기를 회수하고, White에서는 명료도와 온기를 되돌린다. 밝은 버튼이 선함을 뜻하게 하지 않고 플레이어가 보는 세계 자체가 달라져야 한다.
- **CG는 장면 시간과 함께 reveal한다.** 한 이벤트 안에서 이동·준비·도착이 이어질 경우 `cg_reveal_paragraph`로 해당 시점까지 기존 배경과 초상화를 유지한다. 미래 장면의 그림을 첫 문장부터 띄우는 것은 정합성 실패다.
- **한지연의 차량은 반복 주연 소품이다.** `assets/VEHICLE_VISUAL_BIBLE.md`가 정본이며, 검은 메르세데스 S-Class급 장축 세단·좌측 운전석·검은 가죽·수평 대시보드로 고정한다. 실제 상표나 배지는 노출하지 않는다. 사고 CG가 외관, 지연 첫 키스 CG가 실내 정본이다.

## 2026-07-07 (Godot 네이티브 완성 원칙 — 유저 승인)

- **결정**: 연출을 포함한 게임의 완성을 **Godot 네이티브 기능 + 코드 네이티브 작업만으로** 달성한다. 애니메이션 제작 파이프라인(Live2D/스파인/외부 모션) 도입 안 함.
- **연출의 정의**: 타이밍(pace/beat/hold)·소리(amb cut/duck, sting 레이어)·정적·정지화면의 카메라(Ken Burns). 스키마와 정점 16장면 연출 대본 = `docs/SCENE_DIRECTION.md`.
- **기존 파이프라인 판정과의 관계**: 엔딩·결혼 CG(정지 이미지, Ink 그레이딩 통과)와 메인 테마+3변주 외부 소싱은 유지 — '움직임'이 아니라 '소재'라 원칙과 충돌 없음.

## 2026-07-07 (설교 방지 5원칙 — 주제는 강하게, 훈계는 0)

- **배경**: 유저 우려 — "도덕 주제가 플레이어를 가르치려 드는 것처럼 느껴지지 않을까. Press F to pay respects처럼 감정을 강요하는 게임이 되면 안 된다."
- **판정**: 구조는 이미 반(反)설교적 — tint 수치 비노출, 착한 선택에 실제 비용(강남 지연), 더러운 승리 허용 + 어둠 경로에 최상급 문장. 리스크는 구조가 아니라 **카피 규율**.
- **5원칙 (모든 신규 카피의 검수 기준)**:
  1. 서술자는 관찰만, 판단 금지 — 도덕적 의견은 인물의 입으로만, 인물들은 서로 반대해야 한다(지연=속도, 다은=온기, 현수=자기 속도, 상철=전부 도구). 게임은 논쟁이지 판결이 아니다.
  2. 어둠의 문장이 빛의 문장만큼 좋아야 한다 — 나쁜 선택에 벌칙성 카피는 훈계다.
  3. 대가 대칭 유지 — 착하게 사는 게 공짜가 되는 밸런스는 곧 설교다.
  4. 기록하되 지시하지 않는다 — "연락하세요" 금지, "연락 못 한 지 3주가 됐다"까지만. 명령은 튕기고 자기가 만든 진실은 박힌다.
  5. 유혹은 진짜 유혹이어야 — 어둠의 선택지가 매력 없으면 허수아비 설교다.
- **준거**: Papers, Please(훈계 0으로 공범화), 실패 사례 CoD "Press F"(감정의 QTE — 특정 순간 특정 감정 명령).

## 2026-07-07 (외부 파이프라인 판정 — 엔진 이전 금지, 이미지·음악만 선택적 외부화)

- **Unity/Unreal 이전: 금지.** 2D 텍스트 중심 인터랙티브 드라마 + 1,200+ 이벤트 + 자체 감사 체인(audit/밴드/ScreenshotQA)이 Godot 4.6에 완성돼 있다. 이전은 수개월 순손실이고 플레이어가 볼 가치는 0. Steam/Steam Deck 익스포트도 이미 검증됨.
- **이미지: 유일한 실질 갭 = 엔딩·결혼·핵심 씬 CG.** 외부 생성(AI/외주) → 사람 큐레이션 → **게임 내 Gangnam Ink 그레이딩 셰이더 통과**(스타일 통일 장치가 이미 게임 안에 있음) 3단 파이프라인만 허용. `docs/ENDING_ART.md` P0 큐부터. 배경/초상화 신규는 기존 Codex 로컬 파이프라인 유지.
- **VFX: 외부 불필요.** 코드 네이티브 절제 이펙트가 아트 디렉션 정본(GANGNAM_INK). 화려한 외부 VFX는 방향 위반.
- **사운드: SFX·앰비언스는 Codex 충분(25종+30종).** 갭은 BGM(7트랙/10시간+). 외부 지출 1건만: **메인 테마 + moral band 3변주(회색/검정/하양 — 같은 멜로디의 탈색 편곡)**. 작곡 외주 또는 AI 초안+큐레이션. 시각이 이미 하는 도덕 전이를 청각이 완성하는 최소 지출.

## 2026-07-07 (목표 지표 교체 — Metacritic 90 → Steam "압도적으로 긍정적")

- **결정**: 실행 지표를 Steam 유저 평가 95%+("압도적으로 긍정적")로 교체. Metacritic 90은 인디 내러티브 심에서 사실상 도달 불가(방향으로만 유지).
- **5레버 우선순위**: ①루프 압축(몽타주 — 리뷰 킬러 제거) ②데모 훅(플래시포워드 + 데모 내 dik 재독) ③클립 가능성(잔인한 통계 리캡 카드) ④EN 일관성(완료) ⑤오디오 정체성(moral band 음악 전이).
- **근거**: 3종 감사 — 데모가 셀링포인트(dik 재독·tint 붕괴)를 0회 노출 / Y-후반부 데드존(t121-144 최악) / EN 프로즈 4.4점(재작성 불요).

## 2026-07-07 (AP 축 정본 — 이중 구현 정합에서 확정)

- **배경**: Codex가 AP_REDESIGN.md를 읽고 축 시스템을 독자 구현 → main 병합 시 두 구현 정합 필요.
- **결정**: 주간 카운터=`action_axis_this_week` dict 단일. 등록은 **각 `_ap_*` 함수 내부 1회**(래퍼/성공 분기 재등록 금지 — 이중 집계 3곳 실사례). 분류: 인맥·VIP=**돈**(도구적 사교), 운동·명상=사람, 독서·투자공부=돈(Codex 세분화 채택), study 카드=mixed. 그라인드 마모(4주→정신-1·tint-1)에 **루프 드립 총합 상한 -20**(`loop_tint_spent`) — 캡 없으면 240주 그라인더가 도덕 예산(±100)을 루프만으로 태운다.
- **교훈**: 설계 문서의 "구현 예정"에는 담당 명시. main 병합 시 이중 구현 정합부터.

## 2026-07-07 (EN 표기 정본)

- 통화: 프로즈 내 "N billion/million won" (₩·KRW 접두 금지). **문장 첫머리는 스펠아웃**("Three billion won.") — 영어 규범상 문장을 숫자로 시작하지 않는다.
- 라면="ramyeon". 한국어 명사는 무설명 유지(oppa/jeonse/goshiwon), 단 문맥으로 못 푸는 것(예단)만 첫 등장 인라인 글로스. 9급="the Grade 9 civil service exam".

## 2026-07-06 (히로인 호칭·스피치 — 문법이 인물이다)

- **결정**: 다은→민준 "민준씨"+존댓말(진심의 격식). 지연→민준 "오빠"+**반말**(도도·직설) — 단 연애 확정 후부터, 알던 사이 구간은 존댓말. 민준은 두 여성에게 존댓말 기조(지연만 반말인 비대칭이 그녀의 계급적 자신감).
- **EN 대응**: 경어 등급이 없으므로 레지스터로 근사 — 다은="Minjun"+정중한 완결문, 지연="oppa"(로마자)+축약형·캐주얼. 다은=이름(사람) vs 지연=관계호칭(오빠) 대비가 EN에서도 성격 반전을 전달.

## 2026-07-06 (결혼은 잃을 수 있는 것 — 반전된 이혼 거울)

- **결정**: 다은은 **배신하면** 잃는다(상철의 시험 t182 + 최종 선택 t228 — 단 30억 미달 시에만 발화, 정직한 부자는 따뜻한 진엔딩). 지연은 **작게 살면** 그녀가 떠난다(t228 심판 — 붙잡으려면 자기를 잃어야 함: crossed_line). 결혼을 트로피가 아니라 Question A의 마지막 시험대로.
- 30억 캐스케이드에서 **배우자 실이 crossed_line→jaehyuk_way보다 먼저** 평가된다.

## 2026-07-03 (신규 반복 인물은 초상화와 장면 라우팅을 동시에 잠근다)

- **결정**: Claude가 신규 반복 인물이나 새 아크를 추가하면, Codex는 즉시 `ImageRegistry` 참조, 실제 초상화 파일, explicit `background`, ambience 라우팅, semantic audit mirror를 한 세트로 점검한다.
- **이유**:
  1. 새 인물의 텍스트가 좋아도 첫 등장 화면에서 초상화가 없거나 기존 인물 fallback으로 보이면 플레이어는 바로 목업처럼 느낀다.
  2. `background`가 JSON에 명시된 장면은 추론 규칙보다 그 값이 우선하므로, 신규 아크는 "텍스트가 말하는 현재 장소"를 이벤트 데이터에 직접 고정해야 한다.
  3. 반복 인물 초상화는 배경 합성 CG가 아니라 투명 배경 단독 portrait로 관리한다. 주연/반복 조연을 배경에 박아 넣으면 이후 장면 확장 때 정합성 비용이 폭발한다.
- **적용**: 이민서는 `npc_minseo.png`로 독립 초상화화하고, seminar/cafe/pre-ending office 장면 라우팅을 코드·데이터·audit에 모두 동기화했다.

## 2026-07-03 (선택적 가상화 — "안 변하는 한국=실명 / 변하는 수치=가상")

- **문제**: 실명(비트코인·삼성전자)은 리얼리티를 높이는 게 아니라 현실과 대조당할 리스크를 떠안는다. 게임 속 2027년 코인 가격이 실제와 어긋나면 몰입이 깨진다. 실명 시사(미 선거·전쟁)도 검증 지옥 + 특정 시점에 묶여 늙는다.
- **결정: 선택적 가상화.** ①**구조적 한국 현실**(서울·강남·전세·회식·민방위·고시원·정선 카지노·카카오톡·코스피 지수)은 **실명 유지** — 이게 게임의 자산(한국성). ②**변동하는 거래 자산**(주식·코인 18종)은 **투명 아날로그**로 가상화(삼성전자→한성전자, 비트코인→코어코인, 엔비디아→엔코어, 카카오 주식→다온 ※카카오톡은 보존, 나스닥/S&P→미국 기술주/대형주) — 한국 플레이어가 즉시 알아보되 실제 가격과 대조당할 근거가 없어 몰입 안 깨지고 안 늙음. ③**특정 시사**(미 선거 등)는 제네릭화("해외 큰 선거"). asset id는 불변(저장 호환), KR name + ASSET_TEXT_EN 동시 동기화.
- **근거 사례**: 디스코 엘리시움(가상 레바숄)·GTA(Liberty City) — 실명 없이도 더 리얼. 가상화는 몰입을 낮추는 게 아니라 세계관 소유권을 준다.
- **정선 카지노는 유지**: 강원랜드=한국인이 합법적으로 갈 수 있는 유일 카지노라는 구조적 현실이라 강남처럼 실명 유지.

## 2026-07-01 (영화급 승격 P2 — "이동" 대신 "부여"로 백워드 클라이맥스 해소)

- **문제**: 구조 분석이 "클라이맥스가 거꾸로 배치"라 진단(상철 대면 t≈60/Y2, 아버지 죽음 t≥100/Y3에서 조기 소진, Y4~5는 정산). 로드맵 P2는 "상철 대면·아버지 죽음을 Y4로 물리적 이동"을 제안.
- **결정: 아크를 물리적으로 이동하지 않는다.** 상철 대면은 하위 window 체인(known_offer t38~55 / known_reflex t50~59 / reckoning / y3_cost_of_knowing t65~90)과 다수 `description_if_known` 재구성·엔딩 분기·콜백이 전부 t60 타이밍에 묶여 있다. 트리거를 t150으로 옮기면 이 window들이 닫힌 뒤 발화 불가가 되어 **도달 불가 분기(구멍)** 가 대량 발생한다. 유저의 "구멍이 안생기게" 지시와 정면 충돌.
- **대안(채택): 도덕 크레셴도를 Y4~5로 이동.** 백워드 클라이맥스의 본질은 "감정/도덕 무게의 정점이 앞에 있다"는 것. 아크를 옮기는 대신, 이미 존재하는 Y4~5 피날레 비트(arc_final_year_start / arc_endgame_sixmonths / arc_37_reckoning / arc_37_burn_or_light / age_39_final / arc_year_three_half)와 연차 마커 몽타주(story_two~four_year / age_35 / arc_midpoint_reckoning / arc_goal_vertigo / arc_year_three_crossroads)의 선택지에 **money-가속(−)/stay-human(+) 축의 tint**를 부여. 이로써 Question A(그것이 되지 않고 닿을 수 있는가)의 도덕 궤적이 마지막 해에 정점을 찍는다 — 아크 이동 없이, 순수 additive, 구멍 0.
- **엔딩 꼬리 통합(35→8) 보류**: 다수 엔딩은 `finish_run` 호출·`_ending_cast_epilogue`·MetaProgression 도감·BGM 톤맵이 참조. 삭제는 orphan(구멍)을 만들고, 라이프심의 "엔딩 도감" 리플레이 가치를 훼손한다. 중복 축소는 리스크 대비 이득이 낮아 의도적으로 보류(방치 아님).
- **원칙**: tint는 게임플레이 키라 KR 소스에만 부여(EN 오버레이는 text-only). 밸런스 밴드는 tint가 asset/fail에 영향 없어 불변 — 검증 통과.

## 2026-07-01 (영화급 승격 로드맵 — 4축 진단 + P0 우선 실행)

- **배경**: 유저가 "이 스토리를 영화로 상영될 정도, 진격의 거인처럼 완벽한 인물 관계·서사로" 만들고 싶다고 요청. 4개 병렬 분석 에이전트(구조/주제/인물망/복선)를 돌려 전체 서사를 감사.
- **수렴 진단**: (1) **구조** — 클라이맥스가 거꾸로 배치. 상철 대면/청산이 t≈60(Y2), 아버지 죽음 t≥100(Y3)에서 조기 소진되고 Y3가 마일스톤 몽타주로 처지며 Y5가 정산/에필로그가 됨. (2) **주제** — 표면 질문 B("5년에 30억?")가 숨은·우월한 질문 A("아버지를 무너뜨린 그것이 되지 않고 닿을 수 있는가?")를 가림. 걸작 재료(상철 스파인/블랙 패스/밴드 비네트)가 런타임의 소수. (3) **인물망** — 허브-앤-스포크 + 단 하나의 AoT급 삼각형(아버지–박상진–상철–한PD건설–지연). (4) **복선** — `full_circle`이 실행된 적 없는 행위를 서술하는 최악의 구멍 외 재혁 씨앗 스킵·민서 주제 승자 전용 등.
- **결정**: 대규모 재배치(P2: 상철/아버지 죽음 Y4 이동, Y3 재구성, 엔딩 꼬리 통합)는 **보류**. 유저 승인 하에 **P0(저비용·고효과, 구조 미변경)만 우선 실행** — full_circle 배상 씬 신설, 재혁 씨앗 필수화, 프롤로그 대괄호 제거 + 질문 A 콜드오픈. P1(인물망 크로스빔 재혁↔상철·지연↔아버지, 민서 페이오프 완화)와 P2는 후속 세션에서 유저 승인 후 진행.
- **원칙 재확인**: 숨은 도덕 시스템(MORAL_TINT)·상철 진실은 UI 텍스트로 설명하지 않고 색/질감/본문 변주로만 체감시킨다. 질문 A도 오프닝에서 스포일러 없이(상철 미언급) 질문형으로만 심는다.
## 2026-07-03 (명시 배경은 새 전용 에셋 추가 후 반드시 재점검한다)

- **결정**: 이벤트 JSON에 `background`가 명시된 경우 런타임 추론보다 그 값이 우선한다. 따라서 새 배경 에셋을 추가했으면 `ImageRegistry` 추론만 고치지 말고 기존 explicit `background`까지 함께 마이그레이션한다.
- **이유**:
  1. 전용 배경을 만들어도 이벤트가 `street`/`cafe`/`goshiwon_room` 같은 낡은 명시값을 들고 있으면 실제 게임 화면은 계속 틀린다.
  2. 이 게임의 품질 문제는 "예쁜 이미지가 부족함"보다 "텍스트가 말하는 장소와 화면이 다름"에서 더 크게 발생한다.
  3. semantic audit은 자동 수정기가 아니라 후보 탐지기다. `네이버 카페`처럼 온라인 커뮤니티를 실제 카페로 오해하거나, `방탈출`의 "방 안"을 주거 방으로 오해하는 false positive는 규칙으로 분리한다.
- **적용**: 병원/연말정산/벚꽃/학원/수능/사주/예비군/찜질방/방탈출/주민센터/직장문화 이벤트의 explicit background를 교정했다. `content/events/korea_*` semantic review 라인은 0건으로 줄였다.

## 2026-07-01 (디지털/명절/기후/도서관 이벤트도 fallback 방치 금지)

- **결정**: 미세먼지, 추석 귀성길, 오픈채팅처럼 한국 생활 맥락이 강한 이벤트는 generic `street_rainy`/`subway`/`goshiwon_room` fallback에 두지 않고 전용 배경과 낮은 ambience를 연결한다. 이미 `library` 배경이 있는 열람실 이벤트는 새 배경 생성보다 전용 ambience를 먼저 붙인다.
- **이유**:
  1. 외국 유저는 `fine dust`, `Chuseok traffic`, `open chat` 같은 한국적 생활 신호를 텍스트로만 이해하면 피로하다. 화면과 소리가 먼저 상황을 설명해야 한다.
  2. 미세먼지는 비 오는 날이 아니라 건조하고 탁한 공기이며, 추석 귀성길은 지하철이 아니라 장거리 고속도로 정체다. 배경이 틀리면 사건 자체가 가짜처럼 보인다.
  3. 오픈채팅류는 실제 브랜드/앱 UI를 그대로 넣으면 상표·가짜 텍스트 리스크가 있으므로, 브랜드 없는 추상 채팅 화면으로 통일한다.
  4. `kx_gosi_study`는 요청 목록상 `daechi_library` 후보였지만 실제 텍스트가 고시원 복도라서 도서관 배경을 적용하지 않는다. 정합성이 예쁨보다 우선이다.
- **적용**: `fine_dust_sky`, `chuseok_highway`, `open_chat_screen` 배경과 `amb_fine_dust_city`, `amb_highway_traffic`, `amb_open_chat_room`, `amb_library_room` ambience를 추가했다. `kx_fine_dust`, `kx_chuseok_traffic`, `kx_open_chat`, `geojibang_chat` 명시 배경도 교정했다.

## 2026-07-01 (회식/폭염/장마/민방위는 배경·ambience·SFX를 세트로 교정한다)

- **결정**: 회식, 폭염, 장마, 민방위처럼 텍스트만으로 한국성이 강한 생활 이벤트는 일반 `restaurant`/`street` fallback에 두지 않고, 가능한 한 전용 배경·장소 ambience·짧은 상황 SFX를 함께 연결한다.
- **이유**:
  1. 외국 유저는 `hoesik`, `monsoon`, `civil defense drill`의 사회적 감각을 텍스트로만 배우면 피로하다. 첫 인상은 이미지와 소리가 담당해야 한다.
  2. `kx_hoesik`은 단순 식사가 아니라 직장 충성 의식이고, `kx_heatwave`는 단순 낮거리보다 생존 압박에 가깝다. generic 배경은 사건의 의미를 희석한다.
  3. 민방위/장마 같은 이벤트 큐는 길게 깔면 거슬리므로 ambience가 아니라 짧은 one-shot SFX로 넣고, 같은 이벤트 재렌더 시 반복 재생을 막는다.
- **적용**: `company_dinner_restaurant`, `heatwave_city` 배경과 `amb_seoul_street`, `amb_company_dinner`, `amb_heatwave_city`, `sfx_civil_defense_siren`, `sfx_monsoon_rain`을 추가했다. `kx_hoesik`, `kx_heatwave`, `kx_monsoon` 명시 배경도 정합성에 맞게 교정했다.

## 2026-07-01 (계절/운세/예비군 장소는 한국 문화 신호로 독립 배경화한다)

- **결정**: 벚꽃길, 사주카페, 예비군 훈련장 정문은 기존 `hangang_riverside`/`cafe`/`street` fallback에 두지 않고 전용 배경과 전용 ambience를 가진다.
- **이유**:
  1. 외국 유저에게 한국적 맥락을 설명문으로만 전달하면 텍스트 부담이 커진다. 장소 이미지와 소리가 먼저 맥락을 전달해야 한다.
  2. 벚꽃은 “놓쳐버리는 계절”, 사주는 “불안해서 기대는 도시 미신”, 예비군은 “한국 남성 생애선의 행정적 압박”으로 각자 서사 기능이 다르다.
  3. 예비군은 군대 전체가 아니라 `reserve duty` 장면에만 적용해야 하므로 broad `military` 배경과 분리한다.
- **적용**: `cherry_blossom_path`, `saju_cafe`, `military_base_gate` 배경과 `cherry`, `saju`, `military_gate` ambience 라우팅을 추가했다.

## 2026-07-01 (한국 문화 반복 장소는 배경과 ambience를 세트로 추가한다)

- **결정**: 학원가, 수능장, 주민센터, 찜질방처럼 외국 유저에게 한국적 장소성이 강하게 전달되는 반복 장소는 배경 이미지만 추가하지 않고 낮은 ambience까지 한 세트로 추가한다.
- **이유**:
  1. 시각만 맞고 소리가 fallback 방/비/사무실로 남으면 장면 몰입이 반쪽이 된다.
  2. 이 네 장소는 신규 인물/서사 정본과 무관한 재사용 가능 환경이므로 Claude 서사 작업을 기다리지 않고 안전하게 진행할 수 있다.
  3. 한국 특수성이 강한 장면일수록 외국 유저에게 “지금 어디에 있는지”를 텍스트 설명보다 감각으로 먼저 전달해야 한다.
- **적용**: `hagwon_street`, `suneung_test_hall`, `community_center`, `jjimjilbang` 배경과 `hagwon`, `school`, `public_office`, `jjimjilbang` ambience 라우팅을 추가했다.

## 2026-07-01 (반복 장소 ambience를 먼저 확장한다)

- **결정**: Claude가 인물/서사를 계속 다듬는 동안, Codex는 캐릭터 초상화/스토리 CG처럼 서사 락이 필요한 이미지는 보류하고 반복 장소 오디오를 먼저 확장한다.
- **근거**:
  1. 캐릭터/서사 에셋은 이름, 관계, 사건 위치가 바뀌면 곧바로 정합성 리스크가 생긴다.
  2. 반대로 지하철, 경마장, 카페, PC방, 헬스장, 편의점 ambience는 이벤트 텍스트가 바뀌어도 재사용 가능한 장소 레이어다.
  3. 플레이어는 화면만큼 소리에서도 “여기가 어디인가”를 체감한다. 장소음이 room/rain/casino 정도로 뭉개지면 고급 UI를 얹어도 목업처럼 느껴진다.
- **적용**: ambience를 5종(room/rain/hangang/office/casino)에서 11종으로 확장하고, `BGMPlayer`가 `ImageRegistry.infer_background_id()`와 태그/키워드를 통해 전용 장소음을 고른다.

## 2026-07-01 (장소 ambience는 화면 배경 추론을 따른다)

- **결정**: 이벤트에 명시 `background`가 없을 때도 장소 ambience는 `ImageRegistry.infer_background_id()`의 결과를 우선 참고한다. 즉 플레이어가 보는 장소와 듣는 장소는 같은 런타임 의미 라우터를 공유한다.
- **근거**:
  1. 배경 정합성은 이미 broad category보다 구체 장소 의미를 먼저 보는 방향으로 정리됐다. 오디오가 별도 키워드만 보면 영어 면접/한강 산책처럼 화면은 맞는데 소리는 고시원 방인 문제가 남는다.
  2. 인물/서사 데이터가 Claude 쪽에서 계속 바뀌는 동안에도, Codex는 외형 레이어에서 화면-소리 정합성을 안정적으로 보장해야 한다.
  3. 새 ambience 파일을 늘리기 전에는 기존 5종(room/rain/hangang/office/casino)을 더 정확히 쓰는 것이 우선이다.
- **적용**: `BGMPlayer._event_background_id()`를 추가하고, `BGMContinuityCheck`에 inferred office/Hangang ambience 회귀 케이스를 추가한다.
## 2026-06-30 (시작 화면 단순화 — 난이도/런테마 선택 제거, "Start" 단일화)

- **결정**: 시작 화면의 난이도(드라마/현실/지옥고) + 런 테마(자유/투자/인맥/청렴) **선택 UI를 제거**하고, 단일 시작("Start New Story")으로 통일한다. 게임은 항상 `현실`(검증된 밴드) + `자유런`(침묵 랜덤 부스트) 구성으로 시작한다.
- **근거**:
  1. 이 게임은 인터랙티브 드라마(고정 주인공 김민준)다. 첫 화면의 난이도+모디파이어 메뉴는 로그라이크 관습이라 "보는 게임" 정체성과 싸운다.
  2. 실측 결과 선택 7개 중 경험을 실제로 바꾸는 건 1~2개뿐이었다 — 난이도는 가장 눈에 띄는 시작자금 차이(200K)가 30억 대비 무의미하고, 런 테마 3종(자유/투자/인맥)은 1.35x 가중 + 미미한 시작 스탯이라 체감되지 않는다. "선택했는데 안 중요한" 것은 마찰 + 헛약속.
  3. 첫 화면은 스토리 한 줄 보기도 전에 선택 클러스터가 화면 절반을 차지해 첫인상이 시스템 게임처럼 읽혔다.
  4. 의존성 검사: 난이도 분기 콘텐츠 0개, 테마 의존 콘텐츠 2개(arc_ch1_theme 플레이버, 사소), 업적 의존 0개 → 제거 시 손실 거의 없음. 밸런스는 현실=검증된 구성이라 무영향.
- **구현 방식 (역할 분담)**:
  - **Claude(완료)**: `start_new_game` 기본값이 `현실`/`자유런`인 것 확인·고정(카드 제거돼도 단일 구성 보장). 난이도/테마 시스템 코드는 **삭제하지 않고 보존**(DIFFICULTY_DATA, RUN_THEMES, _apply_run_theme) — 향후 부활 대비.
  - **Codex(핸드오프)**: `scenes/StartMenu.gd`에서 Difficulty 카드 행 + Run Theme 카드 행 + 관련 라벨/설명을 제거하고, 왼쪽 컬럼을 훅 카피 → (여백) → Start로 재구성(Gangnam Ink 레이아웃). `start_new_game(...)` 호출은 인자 없이(또는 `현실`/`자유런` 명시) 그대로 둔다 — 선택 변수 기본값이 이미 그 값이라 동작 보장.
  - **보류(향후)**: `청렴런(도박 차단)`은 유일하게 teeth 있는 모드 → 삭제하지 않고 **첫 클리어 후 해금 챌린지 모드**로 부활 후보. 투자런/인맥런은 너무 약해 폐기 또는 자유런 랜덤 부스트로 흡수.


## 2026-06-29 (StoryMode 선택지의 시각 우선순위)

- **결정**: StoryMode/VN 선택지는 작은 스탯 미리보기보다 선택 문장, 포커스, 화면 분위기를 우선한다. 결과 변화는 선택 후 토스트와 후속 텍스트로 전달한다.
- **근거**:
  1. 이 게임의 핵심 선택은 숫자 최적화가 아니라 "내가 어떤 사람이 되는가"를 플레이어가 읽고 감당하는 순간이어야 한다.
  2. 작은 효과 아이콘은 Steam Deck/영어 화면에서 정보라기보다 잡음처럼 보이고, AI 목업 느낌을 강화한다.
  3. `MORAL_TINT`는 숫자 UI가 아니라 화면의 경도/명도/질감으로 체감되는 것이 목표다.
- **구현 방식**:
  - StoryMode 선택지에서 효과 미리보기 라벨을 제거한다.
  - 버튼은 matte numbered choice + 강한 패드 focus border로 조작성을 보장한다.
  - 선택지가 뜰 때 초상화는 뒤로 물러나고, 선택 후 결과 화면으로 돌아올 때 다시 복귀한다.

## 2026-06-28 (Gangnam Ink 최종 표면 언어)

- **결정**: 강남드림의 최종 비주얼 언어를 `Gangnam Ink`로 고정한다. 기본 화면은 콘크리트 회색/차콜/잉크 검정/창백한 흰색 축이며, 금색·초록·파랑은 브랜드색이 아니라 좁은 의미 신호로만 사용한다.
- **근거**:
  1. 여러 세대의 AI 이미지와 코드 기반 UI가 섞여 있어, 개별 에셋의 예쁨보다 전체 화면을 하나의 필름으로 통과시키는 통일 장치가 더 중요하다.
  2. 게임의 핵심 주제는 "30억을 향해 가며 내가 무엇이 되는가"이므로, 색과 질감이 단순 장식이 아니라 `MORAL_TINT`의 가시화여야 한다.
  3. 유저 피드백상 모노톤 전환이 기존 화려한 색보다 더 고급스럽게 읽힌다. 개발 한계를 가리기 위해서가 아니라, 게임의 주제와 제작 현실을 동시에 살리는 방향이다.
- **구현 방식**:
  - `docs/GANGNAM_INK_ART_DIRECTION.md`를 신규 정본으로 추가.
  - `background_grade.gdshader`가 배경을 desaturation 위에 종이결, 잉크 번짐, edge burn으로 통일한다.
  - `moral_surface.gdshader`의 Black 부식색을 브라운 rust에서 차가운 흑회색 ink/concrete로 교체한다.
  - 향후 신규 이미지 프롬프트는 Gangnam Ink prefix를 먼저 적용한다.

## 2026-06-24 (아이템 시스템 → 서사 유물로 전환)

- **결정**: 상점(shop) UI 제거. 28개 스탯 부스터 아이템을 3개 서사 유물로 교체. InventorySystem.gd 엔진은 보존.
- **근거**:
  1. 기존 28개 아이템은 스탯 부스터(체력 +6, 지력 +5, etc.)로 "상점에서 돈 내고 스탯 올리기" 구조 — 드라마처럼 보는 게임 방향과 충돌.
  2. 어떤 이벤트도 아이템을 부여하거나 참조하지 않아 사실상 죽은 시스템이었음.
  3. 인벤토리 패널이 "소지품"이라는 이름으로 UI에 있는데, 이걸 "이야기 속에서 받은 것들"로 바꾸면 오히려 서사 깊이가 생김.
- **구현 방식**: 
  - 상점 버튼 `row.add_child()` 제거(엔진 보존, 진입만 막음).
  - items.json를 3개 서사 유물로 교체: 임상철의 명함 / 다은 포스트잇 / 아버지 23초 통화 화면.
  - GameState.apply_effects()에 `give_items` 핸들러 추가 — 이벤트 선택지에서 직접 유물 지급 가능.
  - 3개 핵심 아크 씬에 `give_items` 배선: arc_sangchul_01_meet(전 선택지) / arc_daeun_04_morning(choice 1) / arc_father_01_call(choice 3 — 23초 통화).
- **확장 방향**: 이후 아크가 추가될 때 유물을 함께 배선. 유물은 인벤토리 패널에 "증거판"처럼 쌓인다.



- **결정**: 이중 정신 축(`stress` 높을수록 나쁨 / `mental` 높을수록 좋음)을 폐지하고 `mental` 단일 축으로 통합. `stress` 변수 제거.
- **근거**:
  1. 두 바가 같은 심리 상태를 반대 방향으로 표현해 플레이어 혼란 + 디자인 모순(같은 휴식이 stress↓·mental↑를 둘 다 일으켜 효과가 이중 계상).
  2. 게임오버는 원래 `mental<=0` 단일 조건이었고, `stress`는 게임오버에 직접 연결되지 않은 반쪽 스탯(BGM·비네팅·일부 조건에서만 사용)이었음.
  3. `pressure_stress` 난이도 계수는 `apply_monthly_pressure()`에서 아예 읽히지 않던 죽은 코드였음.
- **구현 방식**: **적용 계층 리다이렉트**. JSON 600여 개 이벤트의 `"stress"` 키와 `min/max_stress` 조건을 그대로 두고, 적용·조건·가중치 코드에서 전부 mental로 변환(`stress X` → `mental -X`, `max_stress N` → `mental < 100-N`).
  - 이유: 600여 개 이벤트 일괄 치환은 회귀 위험이 크고, 데이터 의미("이 선택은 스트레스를 준다")는 보존하는 게 작성성에 유리. 콘텐츠 작성자는 여전히 `"stress"`로 쓸 수 있다.
- **밸런스 판단**: stress 통합으로 mental 풀에 순 -1068 추가(기존 +3597 → +2529)되나, 밸런스 회귀 밴드(무직 실패 100%·직장 0%·베팅 30억 14.8%) 전부 통과. 휴식 액션이 강해지고 그라인드 액션(이력서/면접/창업)이 정신력을 직접 소모하는 변화는 "번아웃" 드라마 톤에 부합한다고 판단.
- **미반영(기술 부채)**: `tools/balance_sim.py`는 독립 Python 재구현이라 여전히 내부 stress 모델 유지 — 게임 엔진과 별개라 통합 검증과 무관. 추후 정리 대상.

## 2026-06-15 (실명 브랜드 회피와 서울 랜드마크 사용 기준)

- **정선 카지노는 허구 명칭**: 실제 카지노 브랜드/장소 실명과 로고를 production 텍스트·에셋에 직접 쓰지 않는다. 카지노는 "정선 카지노"라는 허구 명칭과 일반 `CASINO` 사인, 산속 리조트/테이블 게임 문법으로 표현한다.
- **랜드마크는 장면 의미일 때 사용**: 한강, 남산타워 같은 서울 공공 랜드마크는 외국인에게도 즉시 읽히는 도시 신호라 적극 활용한다. 단, 관광 포스터처럼 남발하지 않고 이벤트 텍스트가 실제로 한강/남산/서울 야경을 말할 때만 우선 라우팅한다.
- **구체 장소가 행동 카테고리보다 우선**: "한강을 달렸다"는 운동 카테고리라도 헬스장이 아니라 한강 배경이다. "한강 벤치에서 명상"도 명상/옥상이 아니라 한강 배경이다. 플레이어가 보는 것은 행동 분류가 아니라 현재 장소다.

## 2026-06-15 (미니게임 독립 품질 기준)

- **품질 기준 상향**: 경마·홀덤·투자·카지노는 보조 모달이 아니라 강남드림의 대중적 재미를 담당하는 핵심 게임이다. 각 미니게임은 "이것만 떼어도 작은 유료 게임으로 팔 수 있나?"를 목표 기준으로 삼는다.
- **정선 카지노 범위 포함**: 새로 구현 중인 정선 카지노 계열 게임 전체를 이 기준에 포함한다. 현재 블랙잭·바카라가 대상이며, Claude가 이후 추가하는 카지노 게임도 룰 구현 직후 반드시 연출/사운드/세션 UX 품질 패스를 거친다.
- **상업 품질 기준**: 유저 기준은 "플래시게임 수준이 아니라 2만원짜리 게임처럼 보여야 한다"이다. 따라서 미니게임 완료 판정은 룰 작동이 아니라 테이블 레이아웃, 조작 반응, 애니메이션, SFX, 승패 연출, 세션 기록, 반복 플레이 감각까지 포함한다.
- **우선순위**: 룰의 복잡도를 먼저 늘리지 않는다. 플레이어가 5초 안에 느끼는 손맛, 즉 입력 반응, 애니메이션, 사운드, 승패 연출, 숫자 변화의 타격감, 재도전 욕구를 먼저 올린다.
- **경마 1차 적용**: 경마는 베팅홀 정적 화면에서 트랙 배경으로 전환하고, 말 실루엣 위에 기수/새들, 흙먼지, 속도선, 선두 교체 실황을 얹는다. 전용 고해상도 스프라이트 시트와 말발굽/관중 SFX는 다음 품질 패스의 대상이다.
- **홀덤 1차 적용**: 홀덤은 중앙 팟 표시, 칩 버스트, 페이즈/액션 배너, 카드 공개 펄스를 추가해 텍스트 중심 모달에서 테이블 게임에 가까운 감각으로 올린다. 다음은 칩이 실제 위치로 이동하는 애니메이션, 딜러 사운드, 쇼다운 슬로모션/핸드랭크 배너다.
- **투자 1차 적용**: 스캘핑은 캔들, 진입/청산 마커, 손익 배너로 "내가 어느 타이밍에 맞았는지/틀렸는지"를 보이게 한다. TradingFloor는 평균단가선과 체결 피드백으로 자산 운용 화면도 게임처럼 반응하게 한다.
- **확장 순서**: 투자 프레젠테이션 1차 → 정선 카지노 블랙잭/바카라 실기 QA와 카드·칩·딜러 연출 → 경마/홀덤 전용 SFX·스프라이트 고도화 순서로 진행한다. 전부 한 번에 넓히기보다 하나씩 "팔 수 있는 감각"까지 끌어올린다.

## 2026-06-15 (이미지 라우팅과 게임감 연출의 경계)

- **장소 키워드는 카테고리보다 우선**: 이벤트 category가 `investment`/`gambling`이어도 본문이 카페·회사·부동산·편의점·지하철·홀덤·경마를 명확히 말하면 해당 장소 배경을 먼저 쓴다. 플레이어는 장르 카테고리가 아니라 현재 장면을 본다.
- **하지만 전부 자동 수정하지 않는다**: 회식, 회상, 전화, 뉴스 확인처럼 "말하는 주제"와 "실제 장소"가 다를 수 있다. 그래서 semantic audit 잔여 REVIEW는 빌드 실패가 아니라 수동 QA 리스트로 둔다.
- **게임감은 새 시스템보다 반응 레이어부터**: 지금 강남드림은 선택 결과, 도박 승패, 큰 돈 변동의 감각이 약했다. 대규모 룰 개편보다 플래시/흔들림/펄스/SFX를 얹는 쪽이 기존 콘텐츠를 보존하면서 체감 품질을 빠르게 올린다.

## 2026-06-13 (런타임 배경 의미 매핑 우선순위)

- **문제 진단**: 에셋 파일 자체가 정합성을 통과해도, 런타임 배경 선택이 `social → cafe`, `health → hospital` 같은 broad category를 먼저 적용하면 텍스트와 화면이 충돌한다. 실제 QA에서 `집들이` 결과 지문의 "방 안을 둘러봤다"가 카페/거리 배경처럼 보이고, 운동/헬스장 지문이 직전 병원 배경을 물고 갈 수 있음을 확인했다.
- **결정**: 배경 자동 추론은 broad category보다 구체적인 장소 의미를 먼저 본다. `집들이`/방/옆 건물/현재 주거는 housing background, `gym`/`exercise`/헬스장/운동은 운동 계열 배경, 병원은 명시 hospital 태그나 병원·의사·검진·응급실 같은 의료 텍스트가 있을 때만 선택한다.
- **추론 범위**: 배경은 현재 장면의 `id`, `title`, `description`, `category`, `tags`로 결정한다. 선택지/결과문까지 섞으면 아직 고르지 않은 선택지의 장소가 시작 배경을 오염시킬 수 있으므로 배제한다.
- **런타임 일원화**: MainGame과 StoryMode는 같은 `ImageRegistry.infer_background_id()` 규칙을 사용한다. 루틴 비네트는 이벤트 객체가 아니므로 별도 비네트 배경 선택을 갖고, 직전 이벤트 배경을 상속하지 않는다.
- **후속**: `gym` / `exercise` alias는 `gym_interior.png` 전용 배경으로 교체 완료. 앞으로 운동 이벤트가 병원/옥상으로 빠지지 않는지 semantic audit과 실기 QA로 확인한다.

## 2026-06-13 (P3 오디오 생성/검증 기준)

> **폐기됨 (2026-07-23):** 아래 로컬 파형 합성 방식은 출시 오디오에 사용할 수 없다. 현행 정본은 이 문서 맨 위의 실제 녹음·실악기 샘플 결정이다.

- **문제 진단**: 외부 음악 생성 서비스 결과물을 기다리면 오디오 패스가 막히고, 기존 파일 중 `bgm_gosiwon.ogg`는 Theora video로 인식되는 등 런타임 안정성 위험이 있었다. 또한 코드에서 `buy`, `sell`, `tab_open` SFX를 호출하지만 `AudioManager`에는 매핑이 없어 해당 액션이 무음이었다.
- **결정**: 현재 production P3 패스는 `tools/generate_audio_assets.py`의 deterministic local synthesis로 생성한다. BGM은 Ogg Vorbis 44100 Hz stereo, SFX는 WAV 44100 Hz mono를 기준으로 한다. 외부 작곡/효과음으로 교체하더라도 같은 파일명, 같은 runtime key, 같은 loop 정책을 유지해야 한다.
- **검증 기준**: `tools/AudioAssetCheck`가 `BGMPlayer.TRACKS`와 `AudioManager._SFX_FILES` 전체를 `AudioStream`으로 로드할 수 있어야 한다. 코드에서 새 `AudioManager.play("...")` 키를 추가하면 먼저 `_SFX_FILES`에 매핑하고 이 체크를 통과시킨다.
- **루프 정책**: menu/goshiwon/main/apartment/crisis/ending BGM은 import loop ON, victory는 one-shot이므로 loop OFF다.

## 2026-06-13 (Steam 키아트 타이포 합성 규칙)

- **문제 진단**: 이미지 생성 모델에 게임 제목을 직접 넣기 요청하면 한글/영문 로고가 깨지거나 비슷한 글자처럼 왜곡될 위험이 높다. Steam 캡슐은 작은 크기에서 제목 가독성이 매출에 직접 영향을 주므로, 그림은 좋아도 텍스트가 깨지면 production 자산으로 부적합하다.
- **결정**: 마스터 키아트는 텍스트 없이 생성하고, Steam 캡슐/헤더의 `GANGNAM DREAM` / `강남드림` 제목은 로컬 폰트로 후합성한다. 이렇게 하면 이미지 생성 품질과 타이포 가독성을 분리해 관리할 수 있다.
- **적용**: `gangnam_dream_keyart_rooftop.png`는 1920x1080 textless master로 교체했고, `steam_capsule_main.png`, `steam_header.png`, `steam_capsule_small.png`는 같은 마스터에서 크롭한 뒤 로컬 폰트 타이틀을 얹어 제작했다.

## 2026-06-13 (공공장소 배경 실루엣 허용 규칙)

- **문제 진단**: "배경은 인물 없는 장소" 원칙을 모든 장소에 절대 적용하면 PC방, 경마장, 홀덤 클럽, 식당, 도서관처럼 원래 사람이 있어야 자연스러운 공간이 비어 보인다. 반대로 이전 배경처럼 얼굴/손/전경 인물이 선명하면 반복 포트레이트와 충돌하고 특정 장면 CG처럼 읽힌다.
- **결정**: 배경 원칙을 "반복 인물 금지"로 정교화한다. 고시원, 가족집, 병원실, 투자 책상처럼 정합성이 민감한 공간은 person-free를 유지한다. 공공장소는 작고 어두운 익명 실루엣, 뒷모습, 모니터/가구 뒤에 가린 seated figure, 군중 텍스처를 허용한다.
- **실패 기준**: 얼굴이 보이거나, 전경에 크게 서 있거나, 손/팔이 장면의 주체가 되거나, 한 인물이 주연/조연처럼 읽히면 그 이미지는 reusable background가 아니라 CG 후보로 판정한다.
- **적용**: `library`, `restaurant_korean`, `pc_bang_interior`, `racetrack_betting_hall`, `holdem_club_interior`, `hometown_train_station`, `seoul_rainy_street`를 이 기준으로 교체했고, 배경 감사 상태를 PASS로 갱신했다.

## 2026-06-12 (CG 런타임 표시 규칙)

- **StoryMode CG 우선순위**: 이벤트에 `cg`가 있으면 background 추론보다 먼저 전체화면 CG를 표시한다. CG는 이미 특정 사건의 인물+공간을 포함하므로 별도 포트레이트 프레임을 숨긴다. 이름표와 텍스트 박스는 유지해 대사/서술 흐름은 그대로 읽히게 한다.
- **엔딩 CG 이중 표시**: 엔딩 모달은 화면 중앙을 크게 덮기 때문에 CG를 배경으로만 깔면 핵심 장면이 가려진다. 엔딩 `cg`는 배경 이미지로도 쓰고, 모달 내부에 와이드 프리뷰를 한 번 더 넣어 플레이어가 반드시 컷을 보게 한다.
- **헤드리스 검증**: `tools/CGRuntimeCheck`는 MainGame 전체 월 루프를 띄우지 않고, StoryMode 실제 이벤트 렌더와 MainGame CG 경로/프리뷰 헬퍼를 직접 검증한다. 장면 전환과 월 이벤트 추첨이 QA 도구를 흔들지 않게 하기 위해서다.

## 2026-06-12 (인게임 크롭 QA 방식)

- **크롭 QA는 CPU 합성 툴로 고정**: Godot headless dummy renderer에서는 SubViewport 스크린샷 텍스처가 비어 실제 렌더 캡처가 안정적으로 불가능했다. 크롭 QA의 목적은 "현재 UI 수학에서 어떤 부분이 잘리는가"이므로, `tools/VisualCropQA`가 PNG를 직접 읽고 MainGame/StoryMode/CG의 배치/스케일 규칙을 CPU로 합성하는 방식으로 전환했다. 이렇게 하면 로컬/CI/headless 어디서든 같은 결과가 나오고, 최종 라이브 UI QA는 별도 실제 실행 체크로 보완한다.

## 2026-06-12 (late_night 배경 remap)

- **문제 진단**: `late_night_room.png`는 큰 창문과 다른 침대/책상 배치를 가진 별도 방이다. 고시원 밤, 정신력, 야간 고민 이벤트에서 이 배경이 뜨면 유저가 지적한 "고시원 구조가 계속 바뀌는" 문제가 재발한다.
- **결정**: 초기 감사 직후 `late_night` 배경 ID와 `MainGame.BG_NIGHT_ROOM`는 정본 `goshiwon_room.png`로 임시 매핑했다. 이후 `late_night_room.png`를 새로 그리지 않고 `goshiwon_room.png` 기반 4am 색보정 변형으로 재생성해 같은 방 구조를 보존했고, runtime 매핑을 다시 `late_night_room.png`로 복구했다.
- **후속**: 정말 원룸/아파트 밤 장면이 필요하면 `oneroom_apartment.png` 계열을 명시적으로 사용하고, 고시원/초반 정신력 이벤트는 반드시 `goshiwon_room.png` 구조를 유지한다.

## 2026-06-12 (반복 보조 NPC 투명화)

- **문제 진단**: 고시원 원장, 팀장, 성준, 정보상은 반복 호출되는 ID가 있는 조연인데도 기존 PNG는 실제 방/사무실/경마장 배경을 포함하고 있었다. 알파 채널은 있었지만 모서리 알파가 255라 합성용 포트레이트가 아니었다.
- **결정**: 이름/ID로 반복 등장하는 조연은 주연급과 같은 투명 포트레이트 규칙을 적용한다. 배경 포함 예외는 정말 1회성 엑스트라에만 허용한다.
- **정본 교정**: 박성준은 금융권 연락처가 아니라 34세 고교 친구이자 9급 공무원 3년차로 시각화한다. 팀장과 헷갈리지 않도록 안경을 제거하고, muted cardigan/jacket, 체크 셔츠, ID 목걸이로 구분한다.
- **구분성 보강**: 팀장은 흰 셔츠·넥타이·팔짱·안경의 권위 실루엣을 유지한다. 성준은 부드러운 얼굴, 안경 없음, 공무원 ID, 체념 섞인 미소로 구분한다.

## 2026-06-12 (김민준 직업별 의상 포트레이트)

- **문제 진단**: 주인공이 취업해도 계속 고시원 기본 스웨트셔츠로 보이면 플레이어의 사회적 이동이 시각적으로 체감되지 않는다. 반대로 `main_character_30s.png`처럼 방 배경이 박힌 이미지를 상승 상태 기본값으로 쓰면 배경/포트레이트 레이어 분리 원칙을 다시 깨뜨린다.
- **결정**: 김민준의 평상시/결의/수트 계열 포트레이트는 `ImageRegistry.get_player_context_portrait()`에서 직업 티어와 카테고리로 동적 선택한다. 무직은 기본복, survival 직업은 알바/캐주얼, 일반 사무/교육/기술직은 사무직, 대기업·금융·영업·티어3+는 정장으로 표시한다.
- **감정 우선순위**: 충격·마일스톤·위험 상태는 의상보다 감정 전달이 중요하므로 shocked/happy/tired 포트레이트가 우선한다. 추후 품질 고도화 시에는 직업 의상×감정 매트릭스를 별도 제작한다.
- **레거시 처리**: `main_character_30s.png`는 방 배경이 박혀 있으므로 default runtime status portrait에서 제외한다.

## 2026-06-12 (현수 호감형 재디자인)

- **문제 진단**: 현수는 민준과 구분되도록 재디자인했지만, 첫 재디자인은 36세 이상 중년처럼 보이고 비주얼노벨 조연으로서 정 붙일 매력이 부족했다. 동시에 `CANON_MAP.md`/`STORY_BIBLE.md`의 36세 설정은 실제 첫 등장 이벤트의 "스물여섯 정도" 및 민준을 "형"이라 부르는 대사와 충돌했다.
- **결정**: 현수 정본을 26-27세 고시원 옆방 공시생 후배로 고정한다. 체형은 통통하게 유지해 민준과 한눈에 구분하되, 둥근 안경·부드러운 얼굴·어색한 반미소로 호감형 VN 조연성을 살린다.
- **에셋 원칙**: 현수의 구분성은 머리색이 아니라 체형, 안경, 얼굴형, 올리브 후드, 버건디 줄무늬 티셔츠로 확보한다. 중년 아저씨상, 개그성 비호감 체형, 민준과 같은 검은 스웨트셔츠 실루엣은 금지한다.
- **이벤트 정합성**: 현수는 고등학교 친구나 옆방 선배가 아니다. 현수 이벤트에서는 민준을 `형`으로 부르는 후배 말투를 유지하고, 일반 친구 이벤트가 `cast_effects.hyunsu`를 건드리지 않도록 한다.

## 2026-06-12 (한지연 31세 정본 재확인)

- **점검 결과**: 활성 메인 아크의 한지연은 이미 31세/서른 초반, 긴 검은 머리, 크림 수트, 검은 Mercedes-Benz S-Class급 세단, 투자와 로맨스가 섞인 위험한 히로인으로 정렬되어 있다.
- **레거시 처리**: `relationship_events.json`의 구 `jiyeon_meet`→`jiyeon_confession` 체인은 production 첫 만남이 아니며 비활성 상태로 유지한다. 재활성화 금지. 필요하면 메인 아크 플래그(`arc_jiyeon_*`)와 현재 stage 체계로 새 이벤트를 작성한다.
- **금지**: 40대 투자 멘토, 박지연 오기, 단발 중년 비즈니스 멘토 이미지는 모두 폐기 설정이다. 임상철이 멘토 역할을 맡고, 한지연은 강남 세계의 문턱과 위험한 로맨스 축을 맡는다.

## 2026-06-12 (정본 맵과 확장 게이트)

- **문제 진단**: 게임이 여러 번 피벗되면서 런타임 정본, 최신 바이블, 과거 브리프, 작업 로그가 서로 다른 사실을 일부 보유했다. 예: 기본 시작 자금은 코드/프롤로그/CLAUDE 기준 50만원인데 `STORY_BIBLE.md`에는 300만원이 남아 있었다.
- **결정**: `docs/CANON_MAP.md`를 운영용 정본 맵으로 추가한다. 새 콘텐츠/DLC/비주얼 작업은 이 파일의 인물·아크·폐기 목록·확장 게이트를 먼저 확인한다.
- **우선순위**: `CLAUDE.md` 현재 상태와 런타임 코드/JSON이 1차 정본, `CANON_MAP.md`가 운영 정본, `STORY_BIBLE.md`가 서사 바이블이다. `GAME_DESIGN.md`, 과거 `ASSETS_BRIEF.md`, 로그성 문서는 역사 참고로만 본다.
- **확장 원칙**: DLC나 주기 업데이트는 "이미지/이벤트 먼저"가 아니라 canon delta → state/ID 예약 → 에셋 규칙 → JSON/코드 → audit/인게임 QA 순서로 진행한다.

## 2026-06-12 (가족 배경 이미지 격리)

- **문제 진단**: `family_living_room.png`는 보기 좋은 가정집이지만, 벽에 큰 대가족 초상 액자가 걸려 있어 민준 가족 정본과 충돌한다. 민준은 아버지 보증 사기 이후 6년을 갚았고, 아버지와 어머니는 분리되어 있으며, 서울 고시원에 혼자 산다. 대가족 액자는 다른 게임의 가족사를 암시한다.
- **결정**: 해당 이미지를 production 가족 배경에서 격리한다. `dad_house`와 `BG_FAMILY` 기본 매핑은 새 가족 배경이 생길 때까지 `restaurant_korean.png`로 돌린다. 이후 같은 날 새 가족 거실을 재생성해 정합성 검수 후 `family_living_room.png`로 재연결했다.
- **재생성 기준**: 가족 집 배경을 다시 만들 경우 창원/지방 노동자 가정의 낡고 조용한 거실, 부재와 죄책감의 분위기, 작은 낡은 가족사진 1개 이하만 허용한다. 대가족 단체사진, 넓은 전망창, 부유한 인테리어, 화목한 대가족 신호는 금지한다.

## 2026-06-12 (이미지는 story fact로 취급)

- **문제 진단**: 생성 이미지의 소품은 장식이 아니라 서사 정보다. 액자 속 가족 수, 모니터 개수, 창문 크기, 차량 브랜드, 운전석 방향이 텍스트와 충돌하면 플레이어는 게임이 자기 설정을 모른다고 느낀다.
- **결정**: 모든 신규 이미지 작업은 `docs/ASSET_CONTINUITY_CHECKLIST.md`를 통과한 뒤에만 production 매핑에 연결한다. 실패한 이미지는 "아깝다"는 이유로 재사용하지 않고 `quarantined` 상태로 둔다.
- **운영 원칙**: 배경은 인물 없는 장소, 반복 인물은 투명 포트레이트, CG는 특정 사건의 예외 컷이다. DLC/주기 업데이트도 같은 게이트를 따른다.

## 2026-06-12 (한지연/가족 거실 production 복귀)

- **한지연 포트레이트**: `npc_mentor`, `npc_jiyeon_warm`, `npc_jiyeon_cold`는 같은 얼굴/의상/나이를 유지한 투명 포트레이트로 교체했다. 이제 구 단발 중년 멘토 이미지는 production에서 제거된 것으로 본다.
- **가족 거실**: 새 `family_living_room.png`는 낡은 창원 노동자 가정 거실, 작은 사진, 작업복, 낮은 식탁, 좁은 현관을 중심으로 하며 대가족 액자와 부유한 인테리어를 제거했다.
- **재연결 기준**: 새 이미지가 `docs/ASSET_CONTINUITY_CHECKLIST.md` 기준을 통과했으므로 `dad_house`와 `BG_FAMILY`를 다시 `family_living_room.png`로 연결한다.

## 2026-06-12 (김민준 핵심 포트레이트 투명화)

- **문제 진단**: 주인공 포트레이트가 고시원/폰/방 조명을 내장하면 이벤트 배경과 합성될 때 공간이 중복된다. 특히 `main_character_happy`처럼 폰을 든 포즈는 장면과 무관한 소품 문제를 만든다.
- **결정**: 김민준 핵심 5표정은 같은 옷/얼굴의 배경 없는 투명 포트레이트로 교체한다. 파일명 `main_character_neutral_goshiwon.png`는 코드 호환을 위해 유지하지만, 의미상 더 이상 고시원 배경 이미지가 아니다.
- **후속**: `main_character_30s`와 `main_character_50s`는 상승/에필로그 슬롯이므로 별도 단계에서 정본화한다.

## 2026-06-12 (한지연 캐릭터 정본)

- **역할 결정**: 한지연은 단순 투자 멘토가 아니라 강남 세계의 문턱, 빠른 상승의 유혹, 계산과 진심이 섞인 로맨스 히로인이다. 임상철이 멘토 역할을 담당하므로 한지연을 멘토형 인물로 그리면 인물 기능이 중복되고 서사 긴장이 약해진다.
- **비주얼 결정**: 31세 금수저 여성으로 고정한다. 긴 검은 머리, 날카로운 눈, 크림/블랙 테일러드 수트, 예쁘고 위험하고 고혹적인 인상. 단발 중년 비즈니스 멘토상은 금지한다.
- **차량 정본**: 첫 접촉 사고는 검은 메르세데스 벤츠 S클래스급 세단으로 고정한다. 이전 텍스트의 흰색 BMW 문구는 현재 CG와 충돌하므로 폐기한다.
- **이벤트 정본**: 한지연 첫 만남은 `arc_jiyeon_01_crash` 메인 아크가 유일한 정본이다. 구 `relationship_events.json`의 랜덤 `jiyeon_meet` 체인은 같은 첫 만남을 중복 생성하므로 비활성화한다.
- **에셋 액션**: `assets/cg/jiyeon_crash.png`는 현 정본에 비교적 가깝다. `npc_mentor.png`, `npc_jiyeon_warm.png`, `npc_jiyeon_cold.png`는 이후 같은 날 투명 포트레이트로 교체했다.

## 2026-06-12 (주연 포트레이트와 배경 레이어 분리)

- **문제 진단**: 주연 초상화 안에 고시원/사무실/거리 배경이 함께 들어가면 같은 인물이 등장할 때마다 방 구조, 조명 위치, 이불 패턴, 창문 크기, 생활수준이 흔들린다. 이는 개별 컷 퀄리티 문제가 아니라 게임 전체 세계관 정합성을 깨는 구조적 문제다.
- **결정**: 반복 등장 인물은 배경 없는 투명 포트레이트로 관리하고, 장소는 인물 없는 배경 이미지로 별도 관리한다. Godot에서 배경 레이어 위에 인물 포트레이트를 얹는 방식이 정본이다.
- **범위**: 김민준, 김다은, 한지연, 최재혁, 임상철, 아버지, 어머니, 강현수 및 반복 등장 조연은 투명 포트레이트 대상이다. 단발 조연/엑스트라성 인물은 제작 효율상 배경 포함 초상화를 임시 허용한다.
- **CG 예외**: 특정 장면을 연출하는 CG(`start`, `ending_father`, `jiyeon_crash` 등)는 인물+배경 합성을 허용한다. 대신 해당 CG는 공간 구조, 경제 상태, 인물 위치, 소품 정합성을 통과해야 한다.
- **투자 배경 분리**: 일반 투자/재테크 장면은 `investment_phone.png`처럼 폰·노트북·작은 책상 스케일로 표현한다. 멀티모니터 트레이딩룸은 스캘핑 미니게임, 퀀트/전문 투자자, 후반 고급 장면 전용으로 격리한다.

## 2026-06-12 (고시원 공간 연속성 규칙)

- **큰 창문 금지**: 고시원은 원룸/옥탑방처럼 넓은 전망창이 있는 공간이 아니다. 시작 CG에서 강남 스카이라인을 큰 창밖 풍경으로 보여주면 "고시원에서 강남을 바라본다"는 비현실적 공간이 되어 게임의 출발선(좁고 답답한 1.5평 생존 공간)을 훼손한다.
- **책상 위치 기준**: `goshiwon_room.png`와 `start.png` 모두 같은 구조를 따른다. 낮은 접이식 책상은 침대 오른쪽 벽이 아니라 침대 발치/화면 하단 전경에 둔다. 이 구도를 고시원 정본으로 고정한다.
- **강남 암시 방식**: 강남은 창밖 실경이 아니라 폰 화면, 벽에 붙은 이미지, 뉴스 썸네일, 목표 노트 같은 작은 오브제로만 암시한다. 핵심 감정은 "바로 보이는 풍경"이 아니라 "손에 닿지 않는 목표"다.
- **생성 검수 기준**: 앞으로 고시원 관련 배경/CG는 이 구조와 다르면 폐기한다. "고시원 느낌"이 아니라 "같은 방"처럼 보여야 한다.

## 2026-06-11 (감사 체계 확장 — 유지보수성의 구조적 해법)

- **문제 진단**: 게임이 커질수록 버그·서사 모순·유지보수 비용이 증가. 근본 원인은 "진실의 다중화" — 같은 사실(파산 기준, 플래그, stage 이름, 직렬화 대상)이 코드·JSON·문서·시뮬에 중복돼 있고 일치를 강제하는 장치가 없음. AI 세션 기반 개발은 생성 속도가 사람의 검토 속도를 넘어서므로 이 어긋남이 조용히 누적된다.
- **원칙**: 버그를 하나 고칠 때마다 그 버그의 "클래스 전체를 잡는 검사"를 audit에 추가한다 (플래그 교차 검증이 선례 — 도입 시 15건 일괄 검출). 이번 4종도 도입 즉시 9건 검출.
- **화이트리스트는 코드에서 동적 파싱**: effects/conditions 허용 키를 하드코딩하지 않고 apply_effects·_check_conditions의 match 문에서 추출 — 코드가 진실이고 목록은 자동 동기화. 하드코딩 목록은 그 자체가 또 하나의 중복된 진실이 되기 때문.
- **stage 정본 파일**: cast_stages.json은 검사용 선언이지만, 새 stage를 "먼저 선언하고 쓰는" 워크플로를 강제해 서사 상태 공간을 명시적으로 관리하게 한다.
- **밸런스 밴드는 시드 고정**: 결정적이므로 밴드 이탈 = 코드 변경의 파급. 의도된 조정이면 BALANCE.md 기록과 밴드 갱신을 같이 하도록 메시지에 명시.

## 2026-06-11 (파산 기준 순자산화 + 대출 시스템)

- **파산 -3천만이 과했던 이유**: 유저 피드백("3천 빚졌다고 파산은 심해") + 문서/UI/엔딩 텍스트가 전부 -1억 기준으로 쓰여 있었는데 check_game_over 상수만 -3천만이었음. 코드를 설계값(-1억/-2억)으로 정렬.
- **판정을 현금 → 순자산으로**: 대출 도입 시 "빌린 현금"이 통장에 있으면 현금 기준 파산은 영원히 안 옴. 순자산(현금+포트폴리오-대출원금) 기준이면 빌린 돈을 날리는 순간 파산선에 다가감 — 레버리지의 위험이 정직하게 작동.
- **대출 한도 설계**: 1금융 = 월소득×12 (직장 신용) — 기본 연봉이면 한도 전액 손실해도 순자산 -5,700만으로 파산선 위(빚만 남은 채 생존). 4티어 고연봉은 한도 ~1.3억이라 전액 손실 시 파산 가능. "판이 클수록 추락도 깊다"가 자연 스케일링됨. 제2금융은 무직도 가능한 3천만/연 21.6% — 빠르지만 비싼 돈.
- **승리 조건 부풀림 방지**: get_total_asset_value가 대출원금을 차감하므로 대출로 30억 달성·자산 마일스톤·재산 칭호를 찍을 수 없다.
- **신용등급 도입 (유저 지시)**: 고정 한도·금리 대신 신용점수(고용·근속·소득·순자산·평판 − 부채비율·바닥이력) → 1~10등급이 한도·금리를 결정. **변동금리**를 택한 이유: 대출 시점 고정금리는 "빌리고 나면 끝"이라 긴장이 없다. 매달 현재 등급으로 이자를 재계산하면 실직·자산 손실이 보유한 빚의 이자 상승으로 즉시 전이 — 현실의 "빚의 악순환"이 메커니즘으로 작동하고, 등급 관리(근속·자산·상환)가 지속적인 인센티브가 된다. 저장 데이터도 원금만 있으면 돼서 세이브 호환이 단순해지는 부수 이점.

## 2026-06-11 (아크와 자원 게임의 역할 분리)

- **딜레마**: 아크 선택이 엔딩을 직접 가르면 플레이어가 아크 선택지만 최적화하고 자원/스탯 관리가 무의미해진다. 반대로 아크가 엔딩에 영향이 없으면 "내 선택으로 결말이 달라진다"는 체감이 사라진다.
- **결정**: "엔딩의 크기는 숫자가, 표정은 관계가 정한다." (1) 엔딩 티어(승리/파산/번아웃)는 자산·스탯이 단독 결정 — 아크만으로는 30억이 안 모이고 인물 접촉은 AP를 소모하므로 아크 추구 자체가 자원 관리의 일부. (2) 아크 상태는 엔딩 *에필로그*를 변형 — 같은 승리도 따뜻한 승리와 공허한 승리로 갈림. (3) 아크 보상은 런 중 경제 이득으로 환류(월간 패시브, 정보 이벤트) — 아크와 숫자 게임이 경쟁이 아닌 보완 관계. (4) 관계가 엔딩을 *뒤집는* 게이트는 lonely_rich/empty_house 두 개만 유지 — 전부 게이트화하면 아크가 체크리스트가 된다. 페르소나의 팰리스(본 게임)/코프(관계) 구조와 동일한 원리.
- **상철 정보 이벤트 EV**: 신뢰 보상인 만큼 일반 시장 베팅보다 유리하게(성공률 0.62, 실패 시 손실 50%) 설계하되, "확실한 건 없다"는 대사로 손실 가능성을 명시 — 멘토 신뢰가 무위험 치트가 되지 않게 한다.

## 2026-06-10 (플래그 교차 검증 — 반복되는 불일치의 구조적 해결)

- **왜 도입했나**: 아크/선택지를 들여다볼 때마다 수정거리가 계속 나왔다. 원인은 게임의 모든 연결(아크 진행, 패널 표시, 엔딩 게이트, 이벤트 조건)이 문자열 플래그로 돼 있는데 set와 read의 일치를 검증하는 장치가 전무했기 때문. 사람이 조심하는 방식은 이미 실패했으므로(도입 시점 잠재 버그 15개) audit.py에 4번 검사로 기계화했다.
- **ERROR 기준**: "읽기만 하고 아무도 set 안 하는 플래그"만 ERROR. 반대 방향(set만 하고 안 읽음)은 내러티브 기록용 플래그가 많아 보고하지 않는다.
- **empty_house 엔딩 재정의**: 원래 father.passed_away cast 플래그를 읽었으나 아버지 사망 이벤트가 존재하지 않아 영원히 도달 불가였다. 사망 메커니즘을 새로 만드는 대신 "30억 입성 + 관계 전무 + 아버지 비화해 = 빈 집 / 관계 전무 + 화해 = lonely_rich"로 분리해 기존 콘텐츠만으로 도달 가능하게 했다.
- **아버지 아크 구버전 삭제**: life_events.json의 arc_father_01~04(랜덤 풀 진행)와 arc_events.json의 arc_father_01~05(연출 진행)가 같은 스토리를 독립적으로 중복 진행했다. 연출 진행 쪽이 더 깊고(5단계) 결정적이므로 구버전 4종을 삭제. 좋은 텍스트를 잃지만 "아버지가 두 번 아프다"는 내러티브 파탄보다 낫다.
- **옛 설계 마일스톤 삭제**: 시작 20세/은퇴 55·65세 전제의 스토리 이벤트 7종(마흔다섯·쉰·예순 등)은 수정이 아니라 삭제. 33→38세 5년 구조에서는 대응 시점 자체가 없다.

## 2026-06-10 (6차 QA — 세이브·엔딩·아크 결과 내러티브)

- **SAVE_VERSION 3 / 직렬화 누락 수정**: `run_theme`·`unlocked_stat_thresholds` 두 필드가 `serialize()`에 없었다. 로드 후 런 테마가 항상 "자유런"으로 표시되고, 임계값 해금이 재발동되는 증상. whitelist 방식 직렬화 특성상 파싱 에러 없이 조용히 기본값으로 덮여 오랫동안 발견 안 됨. 구 세이브 호환은 `run_theme_categories`에서 테마를 역추론하는 compat 로직으로 처리.
- **엔딩 9종 활성화 — `finish_run()` 누락 원인**: 기존 엔딩 설계 문서에는 25개 엔딩이 기술돼 있었으나 `check_game_over()`에 실제 `finish_run()` 호출이 없었다. `creator_success`는 `creator_success_unlocked` 플래그를 체크했는데 해당 플래그를 SET하는 코드가 어디에도 없었다 — 드라마 피벗 과정에서 setter가 누락된 것. 더 단순한 `creator_viral` 플래그(이벤트 JSON에서 실제로 set됨)로 교체.
- **arc 결과 내러티브 구조**: `_last_opportunity_result`를 읽는 코드는 있었으나 실제로 분기에 활용되지 않았다. `win_flag`/`lose_flag` 키를 opportunity spec에 추가하여 결과를 플래그로 영속화하고, `_next_arc_id()`에서 해당 플래그를 읽어 follow-up 이벤트로 라우팅. result 이벤트에는 `min_turn:9999`를 설정해 랜덤 풀 진입 차단.
- **SimRun OPP 파라미터 불일치**: SimRun에서 사용하던 OPP 2개가 실제 게임 이벤트와 달리 음수 EV였다. 시뮬 승률이 실제 게임보다 낮게 측정되는 원인. 실제 아크/투자 이벤트 파라미터로 교체해 시뮬-게임 일치도 높임.

## 2026-06-10 (스팀 출시 데스크톱 폴리시)

- **export_presets.cfg 커밋**: 원래 `.gitignore` 대상이었으나, 이 프로젝트의 프리셋에는 서명 키/비밀정보가 전혀 없고 빌드 재현성(어느 머신에서나 `build.sh` 한 줄로 빌드)이 더 중요하므로 저장소에 커밋한다. 향후 코드사인 키를 넣게 되면 그때 분리한다.
- **Windows 단일 exe (embed_pck=true)**: Steam 업로드와 배포 단순화를 위해 pck를 exe에 임베드한다. `application/modify_resources=false`로 rcedit 의존성을 제거해 어느 환경에서나 export 가능하게 한다.
- **ESC는 시스템 메뉴 전용**: ESC가 모든 모달을 닫게 하면 이벤트 선택/월 결산 흐름이 의도치 않게 스킵될 수 있다(`_close_modal()`이 `_begin_month()` 등 사이드이펙트 보유). 따라서 ESC는 시스템 메뉴 열기/닫기에만 반응하고, 콘텐츠 모달은 명시적 버튼으로만 닫는다.
- **전체화면 설정 별도 파일**: `AudioManager`가 `gangnam_dream_settings.json`을 통째로 덮어쓰므로, 키 충돌(서로 키 누락 덮어쓰기)을 피해 디스플레이 설정은 `gangnam_dream_display.json`로 분리한다.
- **웹 빌드에서 전체화면 비활성**: 브라우저가 F11과 전체화면을 자체 관리하므로 웹 feature 감지 시 DisplayManager 입력 처리와 토글 UI를 모두 끈다.

## 2026-06-09 (이미지 생성 모델/프롬프트 파이프라인)

- **기본 이미지 모델**: 유저 지시에 따라 `tools/generate_assets.py`의 기본 모델을 `gpt-image-2`로 설정한다. 공식 문서에서 확인 가능한 모델명과 실제 계정 노출 모델명이 다를 수 있으므로 `--model` 옵션으로 `gpt-image-1.5`, `dall-e-3` 등으로 즉시 전환 가능하게 했다.
- **스타일 접두 방식**: 기존 배경 3종에서 추출한 `STYLE_SUMMARY`를 Master Style Guide보다 앞에 배치한다. 기존 캐릭터 포트레이트는 잘못된 스타일/연령대라 분석 기준에서 제외하고, 검증된 배경 분위기만 일관성 기준으로 사용한다.
- **생성 실패 정책**: 에셋 생성은 대량 작업이므로 한 이미지 실패가 전체 작업을 중단하지 않도록 경고 후 다음 에셋으로 진행한다. 최종 성공/스킵/실패 상태는 `assets/ASSET_INDEX.md`에 기록한다.
- **API 호출 방식**: `openai` SDK가 설치된 환경에서는 SDK를 우선 사용하되, 로컬 개발 환경에서 패키지 설치가 누락돼도 진행할 수 있도록 `requests` 기반 fallback을 둔다.
- **최종 스타일 피벗**: 전체 기존 이미지 세트가 여러 생성 세대가 섞인 상태라, 기존 특정 파일을 그대로 추종하지 않고 완전 애니/한국 만화풍 비주얼노벨 아트로 통일한다. 배경은 애니 배경 미술, 캐릭터는 serious manhwa/VN 포트레이트로 맞추며, photoreal/DSLR/피부 모공/카메라 보케는 금지한다.
- **실사용 에셋 우선순위**: 카드/칩처럼 코드에서 실제로 쓰지 않는 이미지보다 `ImageRegistry`, 이벤트 JSON, `SplashScreen.gd`가 직접 참조하는 에셋을 먼저 완성한다. 홀덤 카드/칩은 현재 `HoldemClub.gd`가 코드로 직접 그리므로, 실제 홀덤 디자인 이미지로 재작업하려면 먼저 UI 연결 방식을 결정한다.
- **주인공 나이 표현**: 게임의 핵심 시간축은 33세→38세 5년이므로 50대 초상화는 런타임 핵심 컷이 아니라 레거시/에필로그 슬롯으로 유지한다. 초반에는 기본·피로·결의 표정을 쓰고, `main_character_30s`는 실제 상승 상태에서만 사용한다.

## 2026-05-16 (Init)
- Use `/Users/junheelee/Documents/GitHub/GangnamDream` as the active development folder.
- Use GitHub Desktop for daily commit, branch, and push workflow.
- Use Godot 4.6 as the target engine version.
- Treat `docs/` as the source of truth for requirements and planning.

## 2026-05-16 (Prototype Improvement Pass)

- **엔딩 ID 기준**: `endings.json`의 `id` 필드를 정식 키로 확정. `GameState`에서 호출하는 ID는 반드시 `endings.json`과 일치해야 한다. 이 문서를 진실의 원천으로 삼는다.
- **65세 엔딩 분기**: 은퇴 시 자산 5억 이상은 `stable_success(B)`, 미만은 `ordinary_life(C)`로 분기. 단순 나이 도달보다 자산 결과를 중시하는 설계.
- **result_text 필수화**: 모든 이벤트 선택지에 `result_text`를 반드시 채운다. 빈 문자열은 품질 기준 미달로 간주.
- **투자 UI 설계**: 고정 10만원 매수 대신 3단계(10만/50만/100만) 선택. 전량 매도 대신 분할(25%/50%/전량) 선택. 리스크 표시는 점(●/○) 기호 사용.
- **모달 구조**: 스크롤 가능한 바디 + 고정 헤더(타이틀+X). 모달 내 콘텐츠가 많아져도 접근 가능하도록.
- **스탯 색상 경고 기준**: 건강/정신력 50↓ 노란색, 30↓ 빨간색. 스트레스 60↑ 노란색, 80↑ 빨간색. 게임 오버 조건(0)보다 충분히 앞서 경고.
- **PM 워크플로**: 대화에서 발생하는 모든 개발 요건은 Codex가 `docs/`에 직접 반영. 커밋 메시지도 세션 단위로 관리.

## 2026-05-28 (RPG/Roguelike Pass)

- **행동력 소비 타이밍**: 투자 모달 오픈이 아닌 실제 매수·매도 실행 시에만 AP를 차감하도록 변경. 사용자가 시황을 분석하고 판단을 내리는 행위 자체에 비용을 부과하는 것은 게임 흐름을 방해한다고 판단.
- **무료 행동(free actions)**: 시장 분석(`_ap_market_analysis`)처럼 정보 습득에 해당하는 행동은 `free: true`로 표시해 AP=0에서도 사용 가능하게 함. 행동력 시스템이 정보 접근을 막는 구조는 피한다.
- **월별 크라이시스 확률**: 보너스 6% + 크라이시스 18% = 총 24% 발동률. 매달 확정 이벤트 외 추가 변수를 도입해 예측 불가능성을 높이는 로그라이크 핵심 루프. 3턴 이후 시작(온보딩 보호).
- **레버리지 마진콜 기준**: 포지션 가치가 원금 노출(leveraged_amount × 2)의 35% 이하일 때 강제청산. 실제 증거금 유지 비율(통상 10-20%)보다 완화된 값으로 설정 — 극단적 패널티는 게임오버보다 리커버리 전략을 강요하는 방향이 더 재미있다고 판단.
- **스탯 임계값 RPG 성장**: 30/50/70 세 단계로 설정. 초반 30은 빠른 달성으로 보람을 주고, 70은 장기 목표. 중간 50이 핵심 콘텐츠(무료 시장 분석, 레버리지)를 열어주는 분기점.
- **크래시 확률 상향 근거**: 테스터 피드백 "너무 무난함"에 대응. 시장이 예측 가능하면 전략적 긴장감이 없음. 더 잦은 폭락 위협이 레버리지/현금 비율 결정을 의미있게 만든다.
- **이벤트 반복 완화 전략**: 단기적으로는 최근 기억 창 확장(14→25)과 유니크 description으로 체감 반복을 줄임. 근본적 해결은 이벤트 pool 확장이나 동적 생성이지만 현 단계에서는 이 수준이 적절.

## 2026-06-01 (트레이트 제거 — 성향 시스템으로 일원화)

- **트레이트 시스템 폐기(option A)**: 인터랙티브 드라마로 피벗하면서 StartMenu의 트레이트 선택을 없앴고, 그 결과 `current_trait`은 실제로 어떤 패시브 분기와도 매칭되지 않는 죽은 값이 됐다. "흙수저 생존본능" 단일 트레이트를 살려 패시브를 부여하는 대안(option B)도 검토했으나, 드라마는 "선택한 특성"이 아니라 "플레이 중 쌓인 행동"으로 캐릭터성을 표현하는 방향이라 트레이트 개념 자체가 불필요하다고 판단. 변수·분기·데이터(`traits.json`)·메타 해금까지 전면 제거.
- **성향(tendency)으로 일원화**: 직장/투자/창업 성향을 행동으로 누적하고 임계점(1위 ≥12, 2위와 격차 ≥4)에서 '자각'하며 1회 패시브를 부여하는 시스템이 트레이트의 역할(시작 정체성/패시브)을 대체한다. 선택지가 아닌 누적 행동이 정체성을 만든다는 드라마 톤과 일치.
- **메타 해금 경계**: 트레이트 해금만 제거하고 업적(achievement)·칭호(title) 해금은 그대로 유지. 칭호 중 "인맥왕"·"번아웃 생존자"는 옛 트레이트와 이름만 겹칠 뿐 별개 시스템이라 보존.
