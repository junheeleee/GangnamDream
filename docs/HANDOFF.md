# HANDOFF.md — 세션 인수인계

> **50줄 상한.** 넘으면 오래된 것을 `WORK_LOG.md`나 `DEMO_FIXLOG.md`로 내린다.
> 상태는 이 파일이, 라우팅은 `CLAUDE.md`가 소유한다.

## 지금 무엇을 하는 중인가

작품 방향은 **관계 서사 게임**으로 확정됐다. 데모 구현(`ORDER-57` E)은 계속하되,
그 위에 장면 계층·작업 단위·정합 기반이 깔렸고 이제 **표면과 서명**이 붙는다.

## 사용자가 정한 것 (2026-07-30)

- **외주에 0원.** 인물 디자인·작곡·UI 아트·유료 서체를 사지 않는다. 전부 Codex와
  무료 라이선스(OFL·CC0 등 임베딩 허용)로 만든다. **새 세션이 인력·구매를 전제한
  계획을 세우지 않는다.** 근거는 `DECISIONS.md` 2026-07-30 항목.
- 생성 자산의 결함은 품질이 아니라 **평균성**이다. 더 잘 그려서 이기지 않고
  동일 후처리·고정 서명·채택률로 평균에서 밀어낸다.
- 관계 서사 확정. 주간 루프는 유지하되 **배분 화면은 폐기**한다.
- 로맨스는 이정표가 아니라 **사이의 밀도**로 10배.
- **범위를 줄이지 않는다.** 작업은 **작은 단위로 확실하게**.
- 중요 장면은 일반 이벤트 취급을 받지 않는다. 챕터마다 한 권을 읽은 느낌.
- **목업 금지.** 기능마다 킬링포인트가 있어야 한다(`ORDER-62`).

## 이 저장소의 반복 패턴 — 이것만은 알고 시작하라

**정본은 훌륭한데 강제가 없다.** 세 건이 같은 모양이었다.

- `UI_ART_DIRECTION`이 2026-07-12에 이모지 의존을 진단 → 18일 뒤 이모지 572개 그대로.
- `IP_VISUAL_IDENTITY`에 주연 6인 서명표 완성 → 그 문서를 읽는 검사 도구 0개.
- `STEAM_QUALITY_BENCHMARKS`에 대조 규칙 존재 → 실행된 대조 시트 0개.

**지켜지는 것은 검사가 있는 것뿐이다.** 새 규칙을 쓰기 전에 이미 있는 규칙에
검사를 붙였는지 먼저 확인한다.

## 다음 단위 3개

1. `ORDER-60` 배치 1·2 — 프롤로그와 데모 24주를 새 정본으로 재판정. **판정 오더이지
   수리가 아니다.** 모든 작업의 시작점.
2. `ORDER-63` 배치 1 — 표면 계측기. 오늘 값을 기준선으로 박지 않으면 배치 3의
   성공을 판정할 수 없다.
3. `ORDER-64` 배치 1 — 서명표를 `identity_signature.json`으로 승격. **내용을
   발명하지 말고 옮긴다. 빈 칸을 찾는 것이 산출물의 절반이다.**

## 알아야 할 함정

- **오더는 한국어, `assets/*VISUAL_BIBLE.md` 등 자산 정본은 영어다.** 한쪽 언어로만
  검색하면 있는 규칙을 없다고 오판한다. 이번 세션에 네 번 틀렸다.
- `CODEX_QUEUE.md`는 부팅 예산 12KB이고 현재 11,995다. **여유가 거의 없다** —
  행을 더하려면 완료분을 먼저 압축한다.
- `art_master_audit.py`는 전체 git 히스토리를 요구한다(얕은 클론 실패).
- `audit.sh`는 오토로드·`class_name` 등록을 전제한다. 새 클론은
  `godot --headless --import`를 먼저 돌린다.

## 열려 있는 사용자 결정

- **콘텐츠 등급·심의** (`ORDER-61` 배치 1). 도박 미니게임 4종의 현행 유지·완화·분리.
  등급 판단은 사업·법무 결정이라 에이전트가 내리지 않는다.

건강·정신력은 없애지 않고 **자원에서 상태로** 바꾼다. 별도 오더로 다룬다.
