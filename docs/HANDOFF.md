# HANDOFF.md — 세션 인수인계

> **50줄 상한.** 넘으면 오래된 것을 `WORK_LOG.md`나 `DEMO_FIXLOG.md`로 내린다.
> 인수인계 문서가 이력이 되면 아무도 읽지 않는다. 상태는 이 파일이, 라우팅은
> `CLAUDE.md`가 소유한다.

## 지금 무엇을 하는 중인가

작품 방향을 **관계 서사 게임**으로 확정하고 그 전제 위에 정본과 인프라를 다시
세우는 중이다. 데모 구현(`ORDER-57` E)은 Codex가 계속하되, 그 위에 장면 계층·
작업 단위·정합 기반이 새로 깔렸다.

## 직전 결과

- `ORDER-57` E 21~24주 진행 중. 밀도 REWORK와 생계 백필이 month 6 구현보다
  먼저 확정되어야 한다.
- CI가 1,294회 만에 초록불. 세 겹의 고장(Actions 분 소진 / `.godot` 미임포트 /
  얕은 클론)을 전부 수리했다.
- 표적 감사 `tools/audit_select.py` 가동. 문서 변경 0.9초, 전체 감사는 10분.

## 사용자가 정한 것 (2026-07-30)

- **관계 서사 게임으로 확정.** 주간 루프는 유지하되 **플레이어를 앉히는 배분
  화면은 폐기**한다. 시간 배분은 장면 안의 선택으로 들어간다.
- **주간 루프 자체를 없애지는 않는다** — 5년·경제·예약 콜백이 전부 주에 걸려 있다.
- 로맨스에 힘을 준다. 이정표 3개가 아니라 **사이의 밀도**로 10배.
- **범위를 줄이지 않는다.** Codex를 밤새 굴려서라도 완성한다.
- 작업은 **작은 단위로 확실하게**. 많은 분량을 한 번에 하면 디테일이 떨어진다.
- 중요 장면은 일반 이벤트와 같은 취급을 받지 않는다. 6비트로 끝내지 않고
  연출·사운드가 필수다.
- 챕터마다 한 권을 읽은 느낌이 나야 한다. 전체는 이어가되 각 장이 닫힌다.
- 이민서를 정본에 등재. 인물 성경 단위는 8개(재혁·아버지도 각각 분리).

## 다음 단위 3개

1. `ORDER-60` 배치 1 — 프롤로그 전 장면을 새 정본 여덟으로 재판정. **모든
   작업의 시작점이며 수리가 아니라 목록을 만드는 일이다.**
2. `ORDER-60` 배치 2 — 데모 1~24주 재판정. 여기까지 받고 나머지를 연다.
3. `ORDER-59` 단위 1 — `relationship.daeun.phase` fact 정의와 배선.
   정의만 하고 배선하지 않으면 실패다(write-only 기준선 0).

건강·정신력은 없애지 않고 **자원에서 상태로** 바꾼다 — 회복에 대가를 붙이고,
수치가 아니라 표면으로 읽히게 하고, 독자를 늘리고, 4장부터 회복 상한이
내려간다. 별도 오더로 다룬다.

## 알아야 할 함정 (이번에 새로 발견한 것만)

- **오더는 한국어, `assets/*VISUAL_BIBLE.md` 등 자산 정본은 영어다.** 한쪽
  언어로만 검색하면 있는 규칙을 없다고 오판한다. 이번 세션에서 세 번 틀렸다.
- `art_master_audit.py`는 전체 git 히스토리를 요구한다. 얕은 클론에서 실패한다.
- `audit.sh`는 오토로드와 `class_name`이 등록된 상태를 전제한다. 새 클론은
  `godot --headless --import`를 먼저 돌려야 한다.
- 부팅 예산은 파일별로 걸린다(`CODEX_QUEUE.md` 12KB). 긴 규격은 자기 문서로
  분리하고 큐에는 라우팅만 남긴다.

## 열려 있는 사용자 결정

- **콘텐츠 등급·심의** (`ORDER-61` 배치 1) — 도박 미니게임 4종을 현행 유지할지,
  표현을 완화할지, 분리할지. 조사 뒤 선택지가 올라온다. 등급 판단은 사업·법무
  결정이라 에이전트가 내리지 않는다.

그 밖에는 없다. 2026-07-30에 시스템 척추(`narrative_spine.json` 장별 `system`), 엔딩
계층(`ENDING_CONTRACT.md`), 도덕 축의 대가 계약(`MORAL_TINT.md` §2), 판매 톤
(`STEAM_PAGE.md` §1.5 — 성공의 대가를 선언하지 않고 상황으로 제시)이 전부
확정됐다.
