# HANDOFF.md — 세션 인수인계

> **50줄 상한.** 넘으면 오래된 것을 `WORK_LOG.md`로 내린다.
> 상태는 이 파일이, 라우팅은 `CLAUDE.md`가 소유한다.

## 지금 상황

**Codex 토큰이 없어 사용자가 콘텐츠 작업을 의도적으로 멈췄다.** 그래서
2026-07-30은 규칙과 판정을 먼저 세우는 데 썼다. 이것은 실패 모드가 아니라
실행 용량이 0인 창에서의 정상 순서다.

## 사용자가 정한 것 (2026-07-30)

- **외주에 0원.** 인물 디자인·작곡·UI 아트·유료 서체를 사지 않고 Codex와 무료
  라이선스(OFL·CC0 등 임베딩 허용)로만 만든다. **새 세션이 인력·구매를 전제한
  계획을 세우지 않는다.** 근거는 `DECISIONS.md` 2026-07-30.
- 생성 자산의 결함은 품질이 아니라 **평균성**이다. 동일 후처리·고정 서명·채택률로
  이긴다.
- **장면의 단위는 체인이다**(`SCENE_TIER` §0). 계약은 비트가 아니라 체인 전체에.
- 관계 서사 확정. 배분 화면 폐기, 주간 루프는 유지. 로맨스는 **사이의 밀도**로.
- **범위를 줄이지 않는다.** 작업은 **작은 단위로 확실하게**. **목업 금지.**

## 다음 순서 — 토큰 복구 뒤

1. **`ORDER-57` E** — 밀도 REWORK + 생계 백필 + **`direction` 백필 43건**.
   **데모를 닫는 유일한 작업이다.**
2. `ORDER-63` 배치 1~3 — 표면 계측기 → 물성 정본 → `.theme` 단일 출처. 데모 화면만.
3. `ORDER-62` — 기능 생존·킬링포인트 판정.

**도덕 축 정답 선택 413건과 `ORDER-60` 배치 3~7은 데모 출고 뒤로 미룬다.**
둘 다 무게중심이 2~5장이라 데모에 안 보이고, 지금 열면 복구된 토큰이 데모 밖으로
샌다. 측정은 [`DEMO_TIER_AUDIT.md`](DEMO_TIER_AUDIT.md)에 보존돼 있다.

## 2026-07-30 판정 결과

- 프롤로그·데모 V2 모두 **P0 0건.** 자산 미등록 0, 한영 누출 0, 루트 무결성 0.
- **`ORDER-57`은 구조를 옳게 짓고 아직 채우지 않았다.** 도덕 축 정답 선택이
  데모 6% 대 전 구간 28% — 재구축이 실제로 효과가 있었다는 첫 측정 증거다.
- 남은 결함은 **밀도(장면 21/27이 1비트)와 연출(39/43 누락)** 둘뿐이고 E가 소유한다.

## 이 저장소의 반복 패턴

**정본은 훌륭한데 강제가 없다.** `UI_ART_DIRECTION`이 2026-07-12에 진단한
이모지 의존은 18일 뒤 572개 그대로고, `IP_VISUAL_IDENTITY`의 주연 6인 서명표를
읽는 검사는 0개이며, `STEAM_QUALITY_BENCHMARKS`의 대조 규칙은 실행 산출물이 없다.
**지켜지는 것은 검사가 있는 것뿐이다.** 새 규칙을 쓰기 전에 있는 규칙에 검사를
붙였는지 먼저 본다.

## 알아야 할 함정

- **플래그는 `conditions`가 아니라 dik 산문 키로 소비된다**(`arc_drama.json:228`).
  `conditions`만 훑으면 살아 있는 플래그를 휴면으로 오판한다.
- **오더는 한국어, `assets/*VISUAL_BIBLE.md` 등 자산 정본은 영어다.** 한쪽
  언어로만 검색하면 있는 규칙을 없다고 오판한다. 2026-07-30에 네 번 틀렸다.
- 부팅 예산: `CODEX_QUEUE.md` 12KB·`WORK_LOG.md` 40KB·`CLAUDE.md` 18KB.
  큐는 레거시 `[~]` 행을 사람 판정만 남겨 압축한다. 로그는 `history/`로 내린다.
- `art_master_audit.py`는 전체 git 히스토리를 요구한다(얕은 클론 실패).
  `audit.sh`는 `godot --headless --import`가 선행돼야 한다.
- `MANUAL_SAVE_CHECK`는 타자기와 경합해 flaky했다. 2026-07-30에 절대 문자 수를
  문단 대비 비율로 바꿔 고쳤다.

## 열려 있는 사용자 결정

- **콘텐츠 등급·심의**(`ORDER-61` 배치 1). 등급 판단은 사업·법무 결정이라
  에이전트가 내리지 않는다.
