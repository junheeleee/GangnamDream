# 데모 재판정 — `ORDER-60` 배치 2

> 2026-07-30, Claude. 판정 오더의 산출물이며 **수리하지 않는다.** 여기 오른 항목은
> 별도 오더가 소유한다. 범위는 사용자 결정으로 **데모 V2 경로만**이다(아래 참조).

## 배치 1에서 이어받은 질문

프롤로그에서 **선택 4장면 중 3장면이 착한 쪽에 비용 없이 `tint`를 줬다**.
표본이 작아 계통인지 우연인지 알 수 없었으므로, 배치 2는 그 결함을 24주가 아니라
**전 구간에 기계로 측정**하는 것에서 시작했다.

## 측정 방법

`tint`가 오르는데 음수 효과가 없는 선택`을 세는 것은 부정확하다. 프롤로그가
그 반례였다 — `story_prologue_dad`의 착한 선택은 수치상 무비용이지만 산문이
대가를 지고 있었다.

그래서 **지배(dominance)** 로 다시 정의했다.

```
한 선택이 나머지 전부보다
  모든 축에서 같거나 낫고(tint·mental·money·health·평판·지능·사교·운·호감)
  최소 한 축에서 낫고
  후속 이벤트도 플래그도 다르지 않다
                                    → 순수 정답 선택
```

후속이나 플래그가 갈리면 **길이 닫히므로 대가가 있다**고 보고 제외했다.

## 측정 결과

| 범위 | 선택 2+ 사건 | 순수 정답 선택 | 비율 |
|---|---:|---:|---:|
| **전 구간** | 1,484 | **413** | **28%** |
| **데모 V2**(`ORDER-57` 재구축) | 33 | **2** | **6%** |

중간값도 남긴다: 지배 선택이 있는 사건은 639개이고, 그중 226개는 후속·플래그로
길이 갈려 대가가 있다. **나머지 413개가 아무 구분 없이 한 선택이 우월하다.**

> **2026-07-31 정정 — 정본은 이제 413이 아니라 도구가 세는 값이다.**
> 위 413은 되살릴 수 없는 일회성 스크립트가 냈다. 같은 정의를
> [`tools/project_dashboard.py`](../tools/project_dashboard.py)의
> `dominant_index()`로 다시 구현해 커밋했고, 지금 값은 **414(27%)**다.
> 축 아홉·엄격 우월·후속/플래그 동일이라는 정의는 그대로이며, 호감 합산과
> 인물별 분리, 동률 허용 여부를 바꿔 봐도 414에서 움직이지 않는다.
> 한 건의 차이는 사라진 스크립트와 대조할 수 없어 원인을 특정하지 못했다.
> **재현되지 않는 수치를 정본으로 두지 않으므로, 앞으로는 도구가 소유한다** —
> `docs/STATUS.md`가 매번 다시 세고 `audit.sh`가 낡으면 실패시킨다.

### 이것이 뜻하는 것

**결함은 계통적이지만, 데모 V2에는 거의 없다.** `ORDER-57`이 다시 지은 구간은
이 축에서 전 구간보다 4~5배 건강하다. 문제는 **재구축되지 않은 기존 콘텐츠**에
몰려 있다 — 결혼 아크, 3·5년 확장, 콜백, 챕터 테마.

그리고 데모 V2에서 걸린 2건은 **둘 다 `tint`가 0**이다
(`v2_daeun_small_commitment`, `v2_daeun_third_greeting`). 도덕 축이 아니라
관계 진전의 지배이므로 성격이 다르다.

### 지배 선택의 전형 (전 구간)

| 사건 | 정답 선택 | 효과 |
|---|---|---|
| `arc_daeun_final_choice_decision` | "우리 그냥... 좀 늦게 가요." | `tint +10`, `mental +20` |
| `arc_father_04_visit` | "아버지 잘못 아니에요." | `tint +9`, `mental +23` |
| `arc_daeun_proposal_answer` | "우리부터 약속하고 싶어요." | `tint +8`, `mental +25` |

**대가가 없는 정도가 아니라 이중 보상이다.** 착한 쪽이 숨은 축과 보이는 자원을
동시에 얻는다. `MORAL_TINT.md` §2의 `어떤 선택도 순수하게 좋을 수 없다`와
정면으로 어긋나며, 사용자가 명시적으로 배제한 `누가 봐도 착한 행동 → 점수 상승`
그 자체다.

**다만 이 셋은 감정의 정점이라 `mental` 상승 자체는 서사적으로 옳다.** 고칠 것은
상승이 아니라 **밀어낸 것의 부재**다. 이 판단은 수리 오더가 장면별로 해야 한다.

---

## 범위 — 사용자 결정 (2026-07-30)

`runtime_default=false`라 판정 대상이 둘이었다. **사용자가 A를 택했다 — V2만
판정한다.** 폴백(기존 Decision/Echo/Quiet 전체판)은 안전망이지 판정 대상이 아니며,
`ORDER-57` E 완료 후 V2가 데모가 되는 것이 정본이다.

아래는 그 범위에서 남은 기준(계층·자산·시점·문체)의 판정이다. 대상은 번들 56개와
거기서 도달하는 이벤트 43개다.

---

## 번들의 실체 — 56개 중 장면은 29개다

| 유형 | 수 | 뜻 |
|---|---:|---|
| `action_id` 보유 | **27** | 생계·성장·회복·지원 **행동**이다. 결과 카드이지 장면이 아니다 |
| `existing_roots` 보유 | **27** | 기존 이벤트를 재사용하는 장면 |
| `planned_scene_id`만 | **2** | 아직 집필되지 않았다 |

루트 참조 무결성은 깨끗하다 — **존재하지 않는 이벤트를 가리키는 번들 0개**.

## P1-C · 장면의 절반 이상이 1링크다

`SCENE_TIER` §0에 따라 번들 하나가 장면 하나다. 그 기준으로 재면:

**`existing_roots`를 가진 장면 27개 중 21개가 1링크 체인이다.** T1의 하한
`최소 3비트`는 물론 T2의 `대화 왕복 최소 3회`에도 미달한다.

| 장면 | 비트 | 선택점 |
|---|---:|---:|
| `cafe_world_glimpse` | 10 | 6 |
| `sangchul_world_meet` | 4 | 2 |
| `hyunsu_first_meet` · `hyunsu_player_reachout` · `opening_interview_math` · `temptation_consequence` | 2 | 1~2 |
| **나머지 21개** | **1** | 0~1 |

1링크인 것에는 `daeun_world_meet`, `jiyeon_world_meet`, `jaehyuk_world_meet`,
`father_first_call`, `first_temptation_boss`가 포함된다. **주연의 첫 만남과 1장의
boss가 한 비트다.**

이는 `CLAUDE.md`가 이미 아는 `authored 슬롯 2/3/3/4/1/1(합 14)` 문제와 같은
뿌리이되 한 겹 더 나쁘다 — **슬롯 수가 모자란 것에 더해, 있는 슬롯의 대부분이
단일 비트다.**

## P1-D · 연출이 사실상 없다

**V2 도달 이벤트 43개 중 39개에 `direction`이 없다(91%).**
가진 것은 `first_temptation_boss`와 `sangchul_world_meet` 계열 넷뿐이다.

`core_loop_v2_events.json`의 신규 집필 16개는 **전부 0**이다.

프롤로그가 11링크 중 7을 가진 것과 대비된다. **새로 쓴 구간일수록 연출이 없다** —
렌더러는 이미 있으므로 이것은 능력이 아니라 누락이며, `SCENE_TIER`가 T1·T2에
요구하는 계약이다.

## 통과한 것

| 항목 | 결과 |
|---|---|
| **자산 존재** (6) | 배경 10종·초상 14종 전부 `ImageRegistry` 등록. **미등록 0** |
| **한영 패리티** (7) | 43/43 EN 존재, **한글 누출 0** |
| **루트 무결성** | 존재하지 않는 이벤트를 참조하는 번들 0 |
| **도덕 축** (3) | 2/33(6%). 위 측정 참조 |

시점·지식(4·5)과 문체(7)는 표본으로만 확인했고 자동 판정 근거가 없다. **전수
판정하려면 `ORDER-59`의 지식 원장이 먼저 서야 한다** — 지금은 화자가 무엇을 아는지
기록할 자리가 없어 기계로 못 재고, 사람이 43장면을 읽는 것은 이 배치의 범위가
아니다.

## 이 배치의 결론

**데모 V2는 정합은 통과하고 밀도는 미달한다.** 자산·한영·루트 무결성은 결함 0이고
도덕 축도 6%로 건강한데, **장면 21/27이 1링크이고 연출이 39/43 비어 있다.**

바꿔 말하면 `ORDER-57`은 **구조를 옳게 지었고 아직 채우지 않았다.** 규칙 위반이
아니라 미완성이며, 이는 `ORDER-57` E가 이미 소유한 작업(`장면 밀도 REWORK`)과
같은 지점이다. 새 오더로 중복 발행하지 않는다.

P0은 **없다.**

## 배치 1·2 종합 — 소유 배정 (2026-07-30 확정)

| 항목 | 범위 | 소유 |
|---|---|---|
| `direction` 누락 | 프롤로그 4 + 데모 39 | **`ORDER-57` E** (§E 연출 백필) |
| 장면 1링크 21건 | 데모 V2 | **`ORDER-57` E** 밀도 REWORK |
| 시점·지식 전수 판정 | 데모 V2 | `ORDER-59` 지식 원장 선행 필요 |
| 도덕 축 정답 선택 413건 | 전 구간 | **미배정 — 데모 출고 뒤** |

**신규 오더는 하나도 내지 않았다.** 연출과 밀도는 E가 이미 같은 파일을 선언했으므로
별도 오더면 소유자가 둘이 되고, 그것이 큐 프로토콜이 막으려는 충돌이다.

**도덕 축 413건은 데모 출고 뒤로 미룬다.** 무게중심이 2~5장이라 데모에서 보이지
않고, 지금 오더로 열면 복구된 Codex 토큰이 데모 밖으로 샌다. 측정이 이 문서에
남아 있으므로 기다려도 잃는 것이 없다.

같은 이유로 **`ORDER-60` 배치 3~7(1장 25~48주, 2~5장)도 데모 출고 전 우선순위가
아니다.** 전부 데모 밖 조사이고, 거기서 나올 가장 큰 발견은 이미 전 구간 측정으로
확보했다.
