# ORDER-252 — 표적 작업 결과

[x] ORDER-252 — 2026-09-13. 비저자 Poincare LOCAL work_unit GO.

source `37fdd2ee7e42cf1e39a2d7c73d6f933fd9d19ac6`, tree
`89141836d5021d37eea7b16d7c93b61a8e80d57a`; clean 검토 HEAD
`35ad63a3d11b250bb7dcecaef8e195fa6036ad64`에 결속한다.

## 결과와 경계

ZH numeric hook6줄/351B와 self1. 원형 역복원exact, 기타391 top-level 보존.
direct 정상2·유효변조10·OFF4, isolated static 정상2·유효변조2 PASS.
생산자/독자: static_ui_coverage → canonical validate_text → 기존 exact dice helper.
별도16/4는 gameplay20이 아니며 실제 forwarding3·unknown owner0이다.
같은 live ZH self12446/AUDIT_OK가 통과했고 번역·원문·수용38818 변경0이다.

같은 최종 검사12는97.135701417초/1577입력불변, 전체수용38818 L1은
오류0/72.575284875초/1194불변이다. 세 단위에 한 실행을 범위별 결속한다.
actual405는 기존19입력이 그대로인 component 증거만 재사용했고 재실행0이다.
첫 실패 원형·입력·stdout/stderr·독립 HOLD는 보존하며 후속 PASS와 구분한다.
서사 위치/계층/선택 포기: 해당 없음 — 번역·검사 소비자, gameplay 변경0.
일회성 작업 지시이며 새 규범 없음. 공개GO1·인간OPEN45·본편HOLD 유지.
원어민·렌더·인간플레이·물리패드·새 원격CI·외부출시의 GO가 아니다.
자동 검사는 계약 증거이지 재미·깊이·문체 또는 인간 관찰 증거가 아니다.

[독립 판정](../agent_reviews/ORDER-252.json) · SHA `cd73e9bbf90a0142238dd2a93bd56e5312986943975f6b2ff607854d8c58e5c4`

## 선언·진행 원문 보존

# 중국어 직접 UI 감사의 주사위 수량 연결

#### [~] ORDER-252 직접 UI 수량 연결

[~] 착수 — 2026-09-13, Codex. clean 검토 HEAD
`aba171280d571659024d075ee8f86c2501c60f09`, 제품 source
`4cda91a6011f085d0ab59dc64a82ee815cbf0390`에서 발견했다.
250 첫 named11은10PASS/1FAIL이며 ZH의 실제 같은 오류2를 self가 다시 보고해4줄이다.
`order250-named-first.json` 3720385B/f1d70100bd4f2763171637ca6e6523d86ac21527e7cf528efeb847a9f1e913ec.
source23·fullself262·나머지8검사는 통과했고, 전체38818 수용 L1 오류0과 구분한다.

## 원인·최소 범위

`zh_translation_audit.static_ui_coverage`는 이미 canonical UI key를 넘긴다.
`ui::0924::8f82759e883c`는 진단 alias이며 key routing 결함이 아니다.
직접 `validate_text`의 numeric 분기에251의 exact dice adapter 연결이 없다.
원문·번역이 아니라 이 한 소비자 연결을 고친다. 일반 수사/분류기 규칙은 유지한다.
없으면 정확한 번역2가 직접 감사에서 거부된다. 게임 선택·1년/5년·저장 상태 변경0이다.

## 소유권

- Rawls: `tools/zh_translation_audit.py`의 `validate_text` numeric-only 작은 hook.
  기존 `_ui_dice_title_numbers`를 재사용하고 원문 script/돈/token/LF 검사는 그대로 둔다.
  static/context key 구성·진단 alias·기존12446 self 기대·기존 함수 원형은 그 hook 밖 불변이다.
- Plato: `tools/zh_dice_title_consumer_self_test.py` 신규 좁은 회귀1. 먼저 정상/변조/OFF와
  직접 validator·실제 static_ui_coverage 관측을 literal로 봉인한다.251의 기존40 기대를 바꾸지 않는다.
- ROOT: `tools/audit_scope.json`에 신규 검사 등록·252 명시 차선(기존11+신규1)을 추가한다.
  중복 실행 대신 같은 최종 source의 고유12로250/251/252 범위를 각각 결속한다.
- ROOT 운영: CLAUDE·큐2·active/archive252와250/251 진행 연결·WORK_LOG·STATUS·
  현지화 backlog·agent_review_decisions·agent_reviews/ORDER-252.json.
- Poincare: 비저자 원인·사전대조·코드·실제 결과·최종source 독립 검토.

제품3파일만 변경한다. fulltool/fullself·MG9 source/self·UI3·수용38818·게임 코드·조건·
자산·폰트·project·인간 원장·실제405 입력은 변경0이다. cyclic import/CLI 초기화도 읽어 확인한다.

## 실행·마감

첫 named 실패는 보존한다. 작은 고정집합의 직접경로 수리 전 관측을 먼저 남기고,
구현 뒤 같은 집합에서 정확한 수량은 허용·수량 변조와 원문 독립 진단은 계속 거부한다.
실제 UI dictionary override는 메모리에서만 사용하며 파일·사용자 저장은 쓰지 않는다.
source/key OFF는 원래 직접 validator 진단과 같아야 한다. 영수증·번역문 재작성은 하지 않는다.
최종 source에서 같은38818 L1과 명시12를 한 번씩 실행한다. 이전10PASS를 새12PASS로
속이지 않고 최초 실패·새 결과와 입력 바이트를 분리한다. 실제405는19입력 불변일 때만
기존 component 증거를 재사용하며 로컬 full audit·240주·새 Godot 실행0이다.
독립 작업단위 판정 후 닫는다. 일회성 연결 수리이며 새 일반 숫자 규범은 없다.
공개GO1·인간OPEN45·본편HOLD 및 원어민/렌더/인간 관찰 미실시는 유지한다.

## 첫 표적 관측

고정 direct16/static4의 첫 baseline은 정상 각각0/2이며 유효변조0이다.
원형 `order252-baseline-first.json` SHA58a5a5e3, exit0은 capture 성공만 뜻한다.
그 뒤 numeric hook6줄을 적용했고 제거하면 이전 ZH1684275B/SHA7e57de05로 exact 복원된다.
첫 post `order252-post-first.json` 401364B/SHA878c2ca90f244414d8af9dcec7d2a67d1b6611976d71155175536332fc74fae1:
exit0/0.527039458초, direct 정상2·유효변조10·OFF4, static 정상2·유효변조2 PASS.
외부1194/내부10 입력 불변, 실제 canonical forwarding3·provider/cache 복원, 예외0이다.
최종 clean source의 전체38818 L1·고유12·독립 단위판정은 아직 실행 전이다.
