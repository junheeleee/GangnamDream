# 남은 전문화·선택·인생 칭호 현지화

#### [~] ORDER-255 마지막 11칭호 세 언어 현지화

[~] 착수 — 2026-09-13, Codex. ORDER-254는 독립 최종 검수 GO 뒤 3fd955a에서
내부 마감했다. 이 선언 커밋 뒤 아래 범위의 사적 병렬 저작·독립 검토를 시작한다.
제품 적용·검사·공식 수용은 ROOT가 직렬 수행한다. 현재 신규 번역 수용은 0이다.

## 하나의 표시 계약

MetaProgression의 남은 11칭호 name/desc 22면을 JA·zh-CN·zh-TW로 연결한다.
39/50 → 50/50은 이 함수의 준비된 name/desc 분기만 뜻한다. 보너스·상단 버튼·
route/profile/run_theme, 전체 UI·원어민·렌더·본편 GO를 닫는 숫자가 아니다.

대상 ID는 spec_elite_title, spec_quant_title, spec_founder_title, clean_run_title,
network_run_title, temptation_resist_title, high_road_title, father_peace_title,
love_chosen_title, investigator_title, white_gangnam_title의 정확한 11개다.
원문은 private `post254-remaining-title-source.json`의 현재 b5c 기준 22쌍을 따른다.
그 표의 plain-text digest는 공식 leaf/source hash가 아니다. lookup 연결 뒤 실제
공식 수집값을 별도로 결속하며, 과거 표의 지문으로 대체하지 않는다.

깊이 3문: 제거하면 11칭호의 영어 fallback이 남는다. 24주·1년·5년의 획득·저장·결말은
바뀌지 않는다. bonus/top·나머지 UI와 경쟁하지만 원문 수리 254 뒤 작은 name/desc
계약을 닫는다. 직접 KO에서 66값을 저작하고 비저자가 전량 검토한다. EN은 중간 번역이
아니라 호환 표면이다. 22키는 현재 UI3·수용 원장에 없으며, 실제 연결 뒤 공식 수집·
대조로 그 경계와 신규 수량을 다시 확인한다.

## 보존할 사실

- 청렴은 시작의 의사와 같은 종료 기록행의 30억 이상 자산이다. 무도박 완주나 도덕적
  완성을 발명하지 않는다.
- 아버지는 과거의 실제 화해다. 공통 계절 시한·현재 생존·사망 인증이 아니며,
  254에서 승인한 KO/TITLE_EN 네 문구는 그대로 보존한다.
- Network의 인맥 도움은 정본의 테마이며, 개별 원조나 현재 소유 확정으로 강화하지 않는다.
- Love는 실제 제한된 과거의 답을 보존한다. 미응답으로 지우거나 영구 잔류·결혼으로
  확대하지 않는다.
- Highroad는 실제 신고·증언·구금 기록이며, 새 물리적 이송이나 유죄 판정을 발명하지 않는다.
- White는 저작된 과거 엔딩의 등기 장면이며, 기존 현재 소유·라우팅 정합 채무는 남긴다.
  이번 번역으로 그 원인까지 고쳤다고 하지 않는다. 한국 30억원의 통화·수량도 유지한다.

## 소유권 — 제품·검사·수용 11파일 상한

Chapter 무수정 source3의 호환 근거는 private
`post254-remaining-title-three-source-feasibility.json`
(SHA d90bde94c0a9e28856336a38bc0a5d4cd62c25b25fe819b24c32091106bf40cd)이다.
실제 동적 호출과 과거 읽기 경계를 검토한 설계 근거이지, 미래 구현의 PASS는 아니다.
이 근거를 반영하여 다음 정확한 11경로로 고정한다.

1. source private: `autoloads/MetaProgression.gd` — 선택 lookup 22만 추가.
2. source private: `tools/ja_translation_pipeline.py` — 새 현재 전단·3개 호출 지점 연결.
3. source private: `tools/meta_title_locale_successor.py` — 새 MP/JA 전단·기존 공개 API 위임.
4. self private: `tools/meta_title_locale_successor_self_test.py` — 새 대조와 원형 보존 wrapper.
5. language private: `locale/ui_ja.json` — 승인된 신규 22값.
6. language private: `locale/ui_zh-CN.json` — 승인된 신규 22값.
7. language private: `locale/ui_zh-TW.json` — 승인된 신규 22값.
8. runtime private: `tools/MetaTitleLocaleCheck.tscn`.
9. runtime private: `tools/run_meta_title_locale_qa.sh`.
10. ROOT: `tools/audit_scope.json`.
11. ROOT: `content/meta/full_game_localization.json`.

ROOT가 모든 제품 적용·Git·실행·공식 수용을 맡는다. 운영 갱신은 위 제품 상한과
구분하여 CLAUDE, 큐 요약·본 active/archive, WORK, STATUS, 부모 backlog,
agent_review_decisions·agent_reviews/ORDER-255.json에 한정한다.

MainGame·GameState·LocaleManager·Chapter checker, 조건·카탈로그 KO/TITLE_EN,
모든 사건·엔딩, audit.sh·공개 후보·project·human, 기존 UI값과 수용 38,878개는
원형 보존한다. 신규 66개와 해당 수용 batch 외의 원장 내용은 바꾸지 않는다.
source3 설계가 실제 구현에서 성립하지 않으면 Chapter를 몰래 추가하지 않고 먼저
사양과 소유권을 다시 검토한다.

254 마감 전에는 같은 제품 파일을 동시에 편집하지 않는다. 에이전트의 private 산출은
실제 적용과 분리하며, ROOT가 전체 읽기·승인 뒤 직렬 적용한다. 각 실행의 입력 핀을
고정한 동안에는 관련 제품 쓰기를 멈춘다. 기존 CI와 새 로컬 실행의 증거도 분리한다.

## 영향 검증 계약

새 전단은 실제 최신 MP/JA whole raw와 정확한 22 owner·KO/EN·API/context를 먼저
검증한 뒤 각각 b5c/e2d 원형으로만 역복원한다. 그 뒤 현재 desc 전단 → edbc →
기존 MG9 → 과거 체인을 호출한다. b5c를 건너뛰어 승인된 설명 수리를 지우지 않는다.

JA의 새 chain·현재 통계·역사 self 연결은 기존 3개 호출 지점에 한정한다. 현재 22면을
먼저 검증한 뒤, MP/JA 과거 읽기와 saved-current mg9 API 위임을 같은 좁은 복구
context에서 제공한다. 예외에서도 읽기·API·출력 상태를 복구한다. 현재 공개 inventory와
export에는 실제 새 22면을 그대로 남기며, 역사 투영 결과로 현재 원문을 숨기지 않는다.

Chapter의 동적 module 조회와 raw 선검사·hash 결속·원래 등록 인자를 유지한다.
새 한 단계의 이전 핀은 b5c/e2d지만, 공개 mg9 등록 의미는 기존 afe8/504b이고
Chapter의 최종 과거 등록은 a366 그대로다. 잘못된 raw는 과거 체인에 도달하지 못한다.
정상 raw의 잘못된 claim은 source 유효성과 구분하여 원 claim을 유지하고,
미소유 경로는 원래 오류·identity를 보존한다. 기존 함수·registry·기대·고정표와
별도 fulltool/fullself 등 검사기 의존성의 원형 핀을 덮지 않는다.

source 대조는 구현 저자와 별도의 에이전트가 구현 전에 입력·기대값·정상 base를
고정한다. 정상 MP/JA, whole rollback, 선택 owner/KO/EN/API/context/중복,
비선택 조건, LF/CRLF, 위조 관측, 이전 핀, OFF를 원인별로 검토하되 숫자를 채우지 않는다.
첫 baseline 원형 → 승인 구현 → 같은 입력의 첫 post 순서다. 실제 정상 base가
통과해야 유효 변조로 센다. 예외·첫 실패·stdout/stderr·전후 입력 핀을 보존한다.
새 현재 guard 뒤에서만 기존 254의 18 → 250의 23 → 248의 19 → 245의 24 →
244의 18을 명시적 역사 뷰로 실행한다. 원래 모집단과 기대를 줄이거나 다시 쓰지 않는다.

## 현재 관측과 미래 소비자 설계의 구분

현재 254는 source `c2b2f873a83823d85927d521780b0fa80ab1858d`에서 실제 415 관측의
첫 실행이 PASS했다. 원형은 `order254-actual-first.json`이며, 새 255에 대한 PASS나
254 최종 마감 완료를 대신하지 않는다. 기존 410과 새 설명 5, nested 관측은 서로
구분한다.

미래의 415 + 105 = 520은 아직 설계 예측이다. 신규 구성은 언어당 getter 11,
old39, custom, 도감 locked/unlocked, 조건, 반환·중복, toast/log, 월간 false 소비자,
엔딩 cache, 언어·저장의 21관측 × 5다. 조건 32 × 5 = nested 160도 별도다.
자연 플레이 160/520회나 11칭호 동시 획득 증거가 아니다. full snapshot과 hash형
상태 증거·중복 출력을 분리한다.

승인된 언어표와 소비자별 비언어 기대를 실행 전에 독립적으로 고정한다. 105나 520을
맞추려고 의미 없는 사례를 보태거나 줄이지 않는다. 고정 전 의미 범위가 달라지면 먼저
사양을 조정하고, 고정 후 첫 결과를 보존한다.

old aggregate 20과 254 DESC의 JA/ZH 3관측은 새 11칭호의 현재 dictionary를 먼저
검증한 뒤 선택된 typed name/desc만 과거 뷰로 돌린다. 실제 새 관측을 함께 남기며,
KO/EN, KO 조건 6·lifecycle, parent 5·라벨 110은 보존한다. 저장 ID·조건·범주·희귀도·
비선택 값의 기대는 언어 변경에 맞춰 느슨하게 만들지 않는다.

ROOT의 첫 소비자 실행은 고정된 최종 모집단, 두 raw log 오류 검사, 격리 입력 19,
상태·디스크 보존을 함께 확인한다. 이어 공식 22 × 3 export/check/import와 기존
38,878개 raw 역보존·신규 66개만의 수용을 결속하고, 새 clean source의 전체 L1·
고유 영향 검사·비저자 최종 판정으로 마감한다. 첫 실패가 있으면 원형을 남기고 해당
원인만 별도 판단하며, 통과한 모집단의 중복 재실행으로 증거를 부풀리지 않는다.

모든 실행은 ROOT만 맡으며 로컬 full audit·240주·사용자 저장·원격 재실행은 하지 않는다.
공개 GO 1·인간 OPEN 45·본편 HOLD와 원어민·렌더·물리 관찰 한계를 유지한다.
이 단위는 원문 사실·저장·i18n의 기존 정본을 적용하는 일회성 작업이며 새 규범은 없다.
