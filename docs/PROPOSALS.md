# PROPOSALS.md — 에이전트가 사용자에게 올리는 제안

> **열린 제안 5건 상한, 결정 없이 21일이면 만료.** `context_manifest_check.py`가
> 강제한다. 열린 제안은 [`STATUS.md`](STATUS.md)에 자동으로 실려 사용자가 이미
> 보는 화면에 나타난다 — **읽히지 않는 제안 목록을 하나 더 만들지 않기 위해서다.**

## 왜 상한과 만료가 있는가

이 저장소의 반복된 실패는 **선언만 있고 소비자가 없는 정본**이다. 제안도 똑같이
썩는다. 상한이 없으면 목록이 길어지고, 길어지면 안 읽히고, 안 읽히면 제안하는
행위 자체가 무의미해진다.

그래서 **자리가 다섯 개뿐이다.** 여섯 번째를 올리려면 기존 하나를 먼저 닫아야
하고, 그 판단은 제안하는 에이전트가 한다. **무엇을 제안하지 않을지 고르는 것이
제안의 절반이다.**

만료되면 버리지 않고 [`POST_LAUNCH_NOTES.md`](POST_LAUNCH_NOTES.md)로 내린다.
**살아 있는 목록과 언젠가의 목록을 섞지 않는다.**

## 무엇을 제안하는가 — 부딪힌 것만

**제안 자격은 "일하다 실제로 부딪혔는가"다.** 브레인스토밍에서 나온 아이디어는
여기 오지 않는다. 그건 `POST_LAUNCH_NOTES.md`다.

작업 중 발견한 것은 셋 중 하나로 처리한다.

| | 어떻게 | 무엇이 |
|---|---|---|
| **1. 그냥 한다** | 제안 없이 같은 커밋에서 고친다 | 오타, 깨진 링크, 죽은 주석, 검사 오탐, 실패 메시지에 고치는 명령 추가, `audit_scope.json` 등록 누락, 재생성 문서 갱신, 방금 자기가 헤맨 것을 스킬에 한 줄 |
| **2. 하면서 적는다** | 진행하되 근거를 커밋 메시지와 `WORK_LOG`에 남긴다 | 작업 범위 안이지만 판단이 갈릴 수 있는 것. 사용자가 나중에 뒤집을 수 있어야 한다 |
| **3. 멈추고 제안한다** | 여기 올리고 **그 건은 착수하지 않는다** | 정본 규칙, 스토리·인물·관계 변경, 밸런스 밴드 밖 수치, 큐에 없는 신규 시스템, 파일·기능 삭제, 범위 확장, 반쪽 기능의 완성 또는 제거, 등급·법무 |

**3번을 올리고 답을 기다리는 동안 멈추지 않는다.** 그 건만 빼고 나머지 작업 단위를
끝낸다. 제안 하나가 세션 전체를 세우면 제안 비용이 너무 비싸진다.

## 형식 — 30초 안에 결정할 수 있어야 한다

```markdown
### P-7 · <결론 한 줄> [열림 2026-08-01]

- **부딪힌 자리** — 어떤 작업 중 무엇이 걸렸는가. 파일·줄로.
- **제안** — 무엇을 하자는 것인가.
- **대가** — 하면 드는 비용 / **안 하면 계속 무엇을 내는가.**
- **권고** — 한다 · 미룬다 · 버린다 중 하나. 이유 한 줄.
- **결정** — (대기)
```

**`권고`는 필수다.** 선택지만 늘어놓고 사용자에게 고르라고 하는 것은 판단을
떠넘기는 것이다. 근거와 비용을 대고 하나를 고르되, 최종 결정권은 사용자에게 둔다.

**`대가`의 뒷절이 핵심이다.** "안 하면 계속 무엇을 내는가"를 못 쓰겠으면 그건
제안이 아니라 취향이다. 올리지 않는다.

사용자가 `결정`을 채우면 그 줄이 정본이다. 설계 결정이면
[`DECISIONS.md`](DECISIONS.md)로, 작업이면 큐로 옮기고 여기서 지운다.

---

## 열린 제안

*(없음)*

## 닫힌 제안

> P-1~P-3은 사용자가 2026-08-03에 권고 순서대로 진행하도록 승인했다. 결정
> 이유는 `DECISIONS.md`, 실행 범위는 `ORDER-69`·`ORDER-70`·`ORDER-75`가
> 소유한다.

### P-1 · V2 생계·성장 루틴을 생활 리듬 +1로 조정한다 [열림 2026-08-03]

> **승인 뒤 실측 정정:** 종전 `약 16주`와 `45/25/24/21/18/15`는 필수 clean
> 사건, 21주 아버지 신호, 24주 추적·첫 청구서 효과를 모두 포함하지 않은 값이다.
> 결정의 방향은 유지하되 `ORDER-69`는 시작부터 full-route와 48주 이월을 다시
> 계산한 값만 완료 증거로 쓴다.

- **부딪힌 자리** — `ORDER-57` E 후보를 최신 `main`에 재통합하며
  `content/meta/demo_core_loop_v2.json`의 합법 루틴 조합과
  `GameState.apply_monthly_pressure()`를 실제 시작 수치로 함께 계산했다. 현재
  `생계+성장`은 주간 정신력 -2와 월말 현실·고시원·무직·잔고 압박이 겹쳐
  보수 경로도 약 16주에 `mental_break`로 끝난다. 기존 시뮬레이터는 정신력을
  0 델타로만 계산하고 E 런타임 검사는 21주에 90으로 재설정해 이 실패를 숨긴다.
- **제안** — V2의 `livelihood.unemployed`, `livelihood.employed`, `growth`
  루틴 정신 효과를 각각 `-1`에서 `+1`로 바꾸고 회복 `+3`은 유지한다. 시작
  정신력 60·건강 65, 실제 여섯 번의 월말 압박, 세 루틴 조합, clean·dirty
  세 경로를 같은 시뮬레이터와 Godot 실행 검사로 잠근다. 5년 공통 월말 압박과
  `finish_run` 순서는 바꾸지 않는다.
- **대가** — V2 루틴 세 값과 설명, 정적·런타임 원장을 함께 고쳐야 한다. 적용
  뒤 가장 약한 dirty-return 경로도 월말 정신력이 `45/25/24/21/18/15`로
  24주를 통과하지만 여유는 작다. **안 하면** 합법 선택만 한 플레이어가 데모
  정점 전에 강제 종료되고, 출시 검사가 계속 거짓 안전을 보고한다.
- **권고** — **한다.** 세 조합을 보존하면서 V2의 이중 스트레스만 상쇄하고,
  회복·과로·현금 압박의 선택 가치를 가장 적게 훼손한다.
- **결정** — 승인. 권고대로 진행한다 (2026-08-03, `ORDER-69`).

### P-2 · 사람 테스트 전용 release flavor로 V2를 연다 [열림 2026-08-03]

- **부딪힌 자리** — E 후보의 F 진입 경로를 확인하니 `runtime_default=false`이고
  시작 메뉴의 V2 버튼은 debug 빌드에서만 보인다. 현재 `build.sh playtest`는
  release export라 더블클릭이나 일반 실행으로는 기존 5년판만 열린다.
  `gangnam_demo` feature는 기존 데모의 24턴 제한에는 쓰이지만 V2 활성화나
  V2 시작 메뉴 진입에는 쓰이지 않는다.
- **제안** — retail 기본 전환은 계속 닫고, 별도
  `core_loop_v2_playtest` release feature에서만 V2 진입 버튼·빌드 표식·분리된
  테스트 세이브를 노출한다. 같은 RC를 터미널 인자 없이 외부 테스터가 실행하게
  하고 사람 GO 뒤에만 일반 새 이야기 기본값을 바꾼다.
- **대가** — export preset·빌드 정체성·세이브 네임스페이스·진입 회귀 검사가
  하나씩 늘어난다. **안 하면** 정상 사용 방식의 release RC로는 24주 데모를
  플레이할 수 없어 F 사람 판정이 다른 게임을 시험한다.
- **권고** — **한다.** 출시 기본값을 성급히 켜지 않으면서도 실제 배포물과 같은
  최적화 빌드에서 24주 데모를 검증할 수 있는 가장 좁은 경로다.
- **결정** — 승인. 권고대로 진행한다 (2026-08-03, `ORDER-70`).

### P-3 · 24주 첫 청구서를 ‘데모 전용 T1 정점’으로 등록한다 [열림 2026-08-03]

- **부딪힌 자리** — `demo_collision`은 `kind: boss`와 ORDER-57의 데모 정점을
  맡지만, 실제 `v2_demo_first_bill`은 1이벤트·1선택 지점·대화 0회이고 전용
  배경·연기 초상·오디오 큐도 없다. 반면 `docs/SCENE_TIER.md`의 T1은 최소
  3비트와 8회 대화를 요구하고, 1년 챕터의 정식 보스는 48주
  `arc_year1_close`라 같은 이름으로 묶을 수 없다.
- **제안** — 이를 1년 결말이 아닌 **24주 데모 전용 T1 정점**으로 명시한다.
  기존 8개 선택 인덱스·효과·의무/지원 영수증은 그대로 두고
  `청구서에서 하나를 고름 → 종이 수첩에 이번 달을 정리함 → 수첩을 덮고
  다음 달을 남김`의 3비트 체인과 전용 책상 클로즈업·민준 결정 연기 초상,
  기존 실녹음 `paper_handle`·`pen_write` 큐를 붙인다. 혼자 있는 정점에는
  ‘8회 대화’ 대신 ‘8개 극적 턴(독백·행동·대화 패널)’을 허용하되 다른 T1에는
  자동 확장하지 않는다.
- **대가** — KO/EN 후속 2장면, 전용 시각 자산 2종, 연출·오디오 계약과
  peak-chain 감사를 보강해야 한다. **안 하면** 24주 종료가 카드 한 장으로
  닫혀 플레이어가 정상 속도로 플레이하며 쌓은 24주의 선택을 결산하는
  정점으로 느끼기 어렵고, 이름만 boss인 상태와 T1 정본의 모순이 계속 남는다.
  정상 독해 75~95분은 `[첫 실행 재조정]` 목표이며 실측 완료로 간주하지 않는다.
- **권고** — **한다.** 48주 챕터 결말의 위상을 침범하지 않으면서 데모 자체의
  완결감과 출시 트레일러·스크린샷에 필요한 대표 장면을 확보한다.
- **결정** — 승인. 권고대로 진행한다 (2026-08-03, `ORDER-75`).
