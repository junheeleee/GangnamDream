extends Node
## The isolated choice board only needs UIStyle's click callback to resolve.
## It owns its paper/stamp feedback locally and never loads product audio state.


func play_ui_click() -> void:
	pass
